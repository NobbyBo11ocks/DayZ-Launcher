# 09 · Decisions and verification log

Append-only. Format: `D-nnn · date · decision · evidence (S-ids) · status`. Every patch that changes protocol, launch, Steam, versions or performance behaviour gets an entry **before** the code change, with the source re-checked that day.

| ID | Date | Decision / check | Evidence | Status |
|---|---|---|---|---|
| D-001 | 2026-09-21 | Windows-only target; Linux dropped | user instruction | accepted |
| D-002 | 2026-09-21 | Stack: Tauri 2.11.6 + Rust 1.98 + Svelte 5.57 + Vite 8.3 + TS 7.0; NSIS per-user installer; WebView2 downloadBootstrapper | S-20…S-34, S-41 | accepted (ADR-001) |
| D-003 | 2026-09-21 | Primary server list from Steamworks `ISteamMatchmakingServers`; A2S direct for ping/details/mods; DZSA/BattleMetrics optional | S-25, S-15, S-18 | accepted (ADR-002) |
| D-004 | 2026-09-21 | DayZ RULES escape table is `01 01→01`, `01 02→00`, `01 03→FF`; the velvetcache summary's `→02/→03` is rejected | S-12 `sequences.go`, S-14, S-42 live (447→443 bytes, 0 trailing) | verified |
| D-005 | 2026-09-21 | Fragment keys are 2 bytes `[index 1-based, total]`; sort by index and concatenate | S-12, S-14, S-42 (`1/4…4/4`) | verified |
| D-006 | 2026-09-21 | DayZ payload order: version u8, overflow u8, dlcFlags u16, dlcHash u32×popcount, modCount u8, mods{hash u32, idLen u8 & 0x0F, id LE, nameLen u8, name}, sigCount u8, sigs{len,str}, desc{len,str} | S-12, S-14, S-42 | verified |
| D-007 | 2026-09-21 | Match mods by Workshop ID only; names differ across meta.cpp / mod.cpp / DZSA | S-41 (CF vs Community Framework vs @CF), S-42 | verified |
| D-008 | 2026-09-21 | `-mod=` uses absolute junction paths, `;`-separated, one quoted argument, in server-reported order | S-41 RPT logs | verified |
| D-009 | 2026-09-21 | Reuse the official `!Workshop\@<meta.cpp name>` junction convention; create only missing junctions | S-41 (20 live junctions) | accepted |
| D-010 | 2026-09-21 | Spawn `DayZ_BE.exe` (not `steam://run`) with working dir = game folder, arguments `0 1 1 -exe DayZ_x64.exe "-mod=…" …`; wait on that process for exit code | S-41 `Launcher.log` `GameExecutor: Starting the game, process path: …\DayZ_BE.exe` / `parameters: 0 1 1 -exe DayZ_x64.exe "-mod=…"` (2026-07-23 23:56:31), S-13 agrees | verified (Q1 closed) |
| D-014 | 2026-09-21 | Official launcher initialises Steamworks (`Steam init result: Online`), uses the UGC download manager (`Item was installed: <id>`) and caches PBO/bisign signatures; our Steamworks-based design mirrors it | S-41 `Launcher.log` | verified |
| D-015 | 2026-09-21 | Frontend is plain Svelte 5 + Vite (index.html, `src/main.ts`, `App.svelte`), not SvelteKit; kept the template's `src-tauri/` (icons, build.rs) and rewrote the rest | S-46; docs/05 §7 "no runtime deps beyond @tauri-apps/api" | accepted |
| D-016 | 2026-09-21 | TypeScript pinned to 6.0.3 instead of 7.0.2 because `svelte-check` 4.7.6 peers on `^5 \|\| ^6` | S-47 | accepted, revisit |
| D-017 | 2026-09-21 | Rust installed with rustup-init (`-y --default-toolchain stable --profile default --no-modify-path`), SHA-256 verified against rust-lang; `%USERPROFILE%\.cargo\bin` prepended to the user PATH manually | S-49, S-23 | done |
| D-018 | 2026-09-21 | `steam_api64.dll` will be taken from the vendored `steamworks-sys` redistributable folder and bundled as a Tauri resource (Q6 closed) | S-48 | accepted |
| D-019 | 2026-09-21 | Tauri config: NSIS only, per-user, WebView2 `downloadBootstrapper` silent, strict CSP (`'unsafe-inline'` only for styles), `withGlobalTauri` off, capability `core:default` only, window 1280×800 min 960×600, lib crate-type `rlib` only, release profile fat LTO + abort + strip | S-33, S-34, docs/05 §7 | accepted |
| D-020 | 2026-09-21 | Vite 8 is Rolldown/Oxc based: `build.minify` is `"oxc"` (esbuild is no longer bundled; `minify: "esbuild"` fails with "Cannot find package 'esbuild'") and the top-level `esbuild` option is replaced by `oxc`. docs/05 §7 corrected | `node_modules/vite/dist/node/index.d.ts` lines 2884, 3540–3545; build error output | verified |
| D-021 | 2026-09-21 | M0 build results: `svelte-check` 0 errors; Vite bundle 36.49 kB JS (14.44 kB gzip) + 2.11 kB CSS (0.97 kB gzip); `cargo build` debug 52.8 s cold; `npm run tauri build` release 2 m 09 s cold; `dayz-launcher.exe` 4 064 768 B; NSIS installer `DayZ Launcher_0.1.0_x64-setup.exe` 1 315 556 B (budget < 15 MB). Tauri CLI fetched NSIS 3.11 and nsis_tauri_utils 0.5.3 with hash validation | build output | verified |
| D-022 | 2026-09-21 | Bundle identifier changed from `com.dayzlauncher.app` to `com.dayzlauncher.desktop` (Tauri warns `.app` suffix conflicts with macOS bundles; harmless on Windows but cheaper to fix before first install) | `tauri build` warning | accepted |
| D-023 | 2026-09-21 | M0 idle memory baseline, release exe, 7 s after launch, 1280×800: host WS 25.4 MB / commit 5.9 MB; WebView2 browser 114.5 / 39.1, gpu 63.7 / 75.1, renderer 65.5 / 30.5, utility 35.3 / 11.4 and 17.0 / 7.5, crashpad 9.5 / 2.2 (WS / commit, MB). Totals: WS 331 MB, commit 171.7 MB, 7 processes. Budget metric redefined to private bytes (docs/05 §6) | `Get-Process` + `Win32_Process` measurement script | verified |
| D-024 | 2026-09-21 | IPC smoke test: `npm run tauri dev` → Vite ready in 734 ms, debug rebuild 3.63 s, stderr shows `[ipc] app_info called from the webview` within ~8 s; process tree killed cleanly (0 leftover app or Vite processes). The `eprintln!` is `#[cfg(debug_assertions)]` only | dev log | verified |
| D-025 | 2026-09-21 | Exe version must be read from the `StringFileInfo\<lang><cp>\ProductVersion` string (translations from `VarFileInfo\Translation`, then 0409/04B0, 04E4, 0000); `VS_FIXEDFILEINFO` has 16-bit fields and cannot hold DayZ's build 163709 (the compiler rejected the literal in a test). Live result: `1.29.0.163709` → game form `1.29.163709` | `windows-sys` `VS_FIXEDFILEINFO` definition; live diagnostics | verified |
| D-026 | 2026-09-21 | `HKCU\Software\Valve\Steam\ActiveProcess\pid` is stale (16884 vs live steam.exe 15176); Steam liveness = ToolHelp enumeration for `steam.exe` with image path under `SteamPath`. The registry pid is kept as `registryPid` for display only | `Get-Process steam` vs registry read | verified |
| D-027 | 2026-09-21 | Junction inventory uses `symlink_metadata` + `FILE_ATTRIBUTE_REPARSE_POINT` + `junction::get_target` (reads reparse data, works on dangling targets). `junction::exists` follows the link and dropped the 16 dangling `!Workshop` entries on this machine; the inventory now lists them and warns | junction 2.0.0 `internals.rs`; PowerShell listing (20 junctions, 4 targets exist) | verified |
| D-028 | 2026-09-21 | M1 crates pinned: winreg 0.56.0, keyvalues-parser 0.2.4 (pest 2.9.2), junction 2.0.0, thiserror 2.0.20, windows-sys 0.61.2 (already in Tauri's tree; features Foundation, Storage_FileSystem, System_Threading, System_Diagnostics_ToolHelp) | Cargo.lock, S-26–S-28 | accepted |
| D-030 | 2026-09-21 | M1 in-app smoke test: `npm run tauri dev` → Diagnostics view invoked the command inside the WebView; debug trace `[ipc] diagnostics: steam_running=true libraries=2 dayz=true workshop_items=4 junctions=20 warnings=1 in 8 ms` within ~8 s; process tree killed with 0 leftovers | dev stderr | verified |
| D-031 | 2026-09-21 | Legacy Steam Master Server Query Protocol is retired: `hl2master.steampowered.com` and `hl1master.steampowered.com` are NXDOMAIN on the system resolver, 1.1.1.1 and 8.8.8.8; `Steam\config\MasterServer2.vdf` is not shipped any more; community dates the shutdown to Oct 2025. `a2s/master.rs` and `tools/master_query.js` (written and unit-tested against the wiki samples) were deleted rather than kept as dead code. List fallbacks are now: user-supplied Steam Web API key, then DZSA | S-50 | verified |
| D-032 | 2026-09-21 | M2 fixture set captured with `tools/a2s_capture.js` from 51.81.8.81:27017: `wildlandz.info.bin` 207 B, `wildlandz.rules.0.bin` 609 B (single datagram, 13 rules, 4 fragments, 12 mods, 12 signatures, 0 trailing bytes), `wildlandz.player.bin` 6 B, plus `wildlandz.expected.json` from the reference JS parser. Node tools converted to ESM (`tools/lib/a2s.js`) because the root package.json is `type: module` | S-42, capture output | verified |
| D-033 | 2026-09-21 | DayZ sends A2S_RULES as one datagram regardless of size: 121-mod server 4 587 B (43 rules, 34 fragments, 95 signatures), 105-mod server 5 309 B (49 rules, 40 fragments, 197 signatures); no split header seen. Client receive buffer is 64 KiB; split reassembly kept per spec. New keyword `allowedFilePatching` added to `DayzTags`. Fixtures `kingofgames.*`, `volatile.*` | `tools/a2s_capture.js` 188.24.17.159:27016 and 51.89.173.29:2303 | verified |
| D-034 | 2026-09-21 | A2S_PLAYER on DayZ: one entry per connected player, empty name, score 0, real duration (1963.8 s, 946.1 s …). Use entry count as an independent head-count vs INFO `players`; never expect names (Q3 closed) | same captures | verified |
| D-035 | 2026-09-21 | The per-mod u32 hash in the DayZ RULES payload is a content hash: CF 1559212036 = `0x68AFC6C1` on all three captured servers. Keep it in `Mod` for future build-mismatch warnings; matching stays by Workshop ID | fixtures | verified |
| D-036 | 2026-09-21 | DZSA's full list is fetchable outside a browser when a browser User-Agent is sent (HTTP 200, 24.5 MB, 0.6 s; plain fetcher UA gets 403). 18 678 servers, 18 672 unique addresses, 2 030 with players > 0, 18 211 on 1.29.163709. Used only as the sweep-benchmark address source; product fallback per D-031 | `curl -A "Mozilla/5.0 … Chrome/153"` | verified |
| D-037 | 2026-09-21 | Live INFO sweep over the 18 678 DZSA addresses (per-query sockets, no pacing): 256-wide/2 s → 12 561 ok, 6 116 timeouts, 53.7 s, RTT p50 30 / p90 99 / p99 251 / max 399 ms; 256-wide/1 s → 9 326 ok, 41.0 s; 512-wide/1 s → 3 803 ok, 30.4 s; 1024-wide/1.5 s → 2 014 ok. Loss grows with burst size (consumer NAT/ISP), not with server latency, so the client gained send pacing (default 400 datagrams/s) and defaults of 128 concurrency, 1 s timeout, 1 retry. The "20 000 in 15 s" acceptance is withdrawn: Steamworks supplies the list with pings (ADR-002); our fan-out serves visible rows, favourites and on-demand RULES. One address in the list is `0.0.0.0:2303` (io error 10049), correctly classified | `tests/live_sweep.rs` runs | verified |
| D-038 | 2026-09-21 | **Fake population is widespread.** Of 9 326 responding servers, 5 405 report 61–127 players in A2S_INFO (e.g. `XOMA PvP 120/127`, `Divide Horizon 120/120`), yet DZSA counts only 2 030 servers with any players and A2S_PLAYER returns real per-player entries (D-034). The UI must show the PLAYER head-count as the player number for any server it displays and flag INFO/PLAYER mismatches; INFO `players` is only a hint for sorting before verification | sweep histogram, D-034 | accepted |
| D-039 | 2026-09-21 | Paced re-run after a 5-minute cool-down (256-wide, 2 s, 400 pps): 11 219 ok, 7 458 timeouts, 94.9 s, RTT p50 27 / p99 280 ms; the same settings after only 90 s cool-down gave 6 491. Confirms the NAT flow-table explanation (recovery with rest) and shows pacing at 400 pps neither hurts nor helps success within run-to-run variance (11.2k vs 12.6k unpaced). Defaults stay conservative (400 pps, 128 concurrency, 1 s, 1 retry); the product never sweeps the full list | `tests/live_sweep.rs` run 6 | verified |
| D-040 | 2026-09-21 | `steam_api64.dll` vendored at `src-tauri/resources/steam_api64.dll` (317 080 B, SHA-256 `eb17909a76668cf9ae0b92a618a34a50f6c73d3a6787cb4dd8ce36a8b10bfb75`, copied from `steamworks-sys` 0.13.0 `lib/steam/redistributable_bin/win64`, Steamworks SDK 1.64 redistributable). `build.rs` copies it next to dev and test binaries; `bundle.resources` maps it beside the installed exe (verify placement in M7, Q14) | S-48, crate source | accepted |
| D-041 | 2026-09-21 | Steam caps `RequestInternetServerList` at **10 000** servers per request (observed `total=10000` exactly on an unfiltered query and again on `noplayers`). Refresh now runs partitions: `hasplayers` (2 988 total / 2 684 ok / 37.9 s) then `noplayers` (10 000 capped / 9 171 ok / 131.9 s); 12 988 total in 169.8 s. Steamworks init 454–529 ms after launch; warm start renders 13 464 cached rows 677–705 ms after launch (budget < 1 s). Empties need a further split (Q15) | dev smoke logs | verified |
| D-042 | 2026-09-21 | Auto-refresh re-triggered on every page load/launch because the 60 s throttle only knew the in-process time; `SteamWorker` now seeds `last_done` from the cache's `last_refresh` meta and compares unix seconds | dev smoke log run 2 | fixed |
| D-043 | 2026-09-21 | Fake population: the memory patch inflates A2S_INFO only; PLAYER stays truthful (55/126 sampled servers: INFO 52–127 vs PLAYER 0) or is suppressed entirely (25/126 never answer PLAYER). Detection rules R1–R7 in docs/11; verified head-count becomes the displayed number, inflated/unverifiable hidden by default | S-51–S-54 | accepted |
| D-044 | 2026-09-21 | Cache statistics after the first partitioned refresh (`tools/cache_stats.js`, 13 491 rows): INFO-populated 11 563 (61+: 8 747); genuinely empty 1 928 of which no3rd 325 / 3PP 1 603, privHive 1 928, password 784; empties by map chernarusplus 1 117, deerisle 198, enoch 142, namalsk 95, sakhal 85, banov 27, Bitterroot 27, Pripyat 24; eight IPs host 176–395 "populated" servers each; 11 174 rows ping < 60 ms | script output | verified |
| D-045 | 2026-09-21 | Steam's `hasplayers`/`noplayers` filters use the master's authenticated player count, which the A2S memory patch does not affect: `hasplayers` = 2 988 servers, while ~8 900 INFO-"populated" servers arrived via `noplayers`. `ServerRow.steam_empty` records the partition; `inflated() = steam_empty && players > 0` (rule R0) ships in M3 with a default "Hide inflated" filter. Empty servers are now fetched per map (10 maps from D-044) plus a capped catch-all (Q15 closed). Cache schema bumped to v2 (drop-and-recreate policy pre-release) | D-041 partition totals, D-044 | accepted |
| D-046 | 2026-09-21 | Full 12-partition refresh on a fresh cache: 36 679 total / 33 605 responded / **27 037 inflated** in 473 s. `hasplayers` 3 018 (0 inflated, 38 s); `noplayers`+chernarusplus 10 000 capped (7 677 inflated), +enoch 10 000 capped (8 524), +namalsk 10 000 capped (8 990), +sakhal 2 287 (1 610), +deerisle 1 078 (236), +banov 157, +Bitterroot 139 (0 inflated); the last four requests got `NoServersListedOnMasterServer` (Steam throttling back-to-back list requests). Warm start with 33 575 rows: 734 ms; relaunch throttle confirmed. Decisions: automatic refresh = `hasplayers` only; "Full refresh" is manual and runs the map partitions with a 3 s gap and early stop after 2 empty master answers | dev smoke log run m3f/m3g | accepted |
| D-047 | 2026-09-21 | M4 verification pass, first attempt (2 821 servers, INFO+PLAYER back to back, 400 pps): verified 1 732, inflated 7, synthetic 3, unverifiable 676, offline 403 in 60 s. **Corrected by D-050**: the 676 and 403 were burst loss, not host firewalls. Fixes: the automatic pass sends PLAYER only (Steam's INFO is a minute old at most), offline targets are retried with INFO at a 2.5 s timeout, and only the automatic pass emits `servers:verify-done` (an on-demand check of 25 visible rows had masqueraded as the full pass). R4 keeps a safety clause: an "unverifiable" verdict never hides a row Steam vouched for (`steamEmpty == false`) | dev smoke logs m4a/m4b | fixed |
| D-050 | 2026-09-21 | M4 verification pass after the fixes (2 858 servers): **verified 2 840, inflated 5, synthetic 3, unverifiable 6, offline 4 in 33 s**; on-demand checks of 25 visible rows 0.1–2.1 s. The 8 fake-among-populated cases are the "N ghost + real players" variant of the memory patch (S-51) that rule R0 cannot see; R3/R5 catch them | dev smoke log m4c | verified |
| D-048 | 2026-09-21 | M4 measurements (release): exe 5.99 MB, installer 2.26 MB; cached list of 2 886 rows rendered 748 ms after launch; host private 65–79 MB with `steamclient64.dll` + `gameoverlayrenderer64.dll` loaded in-process by Steamworks; all-process commit 294–315 MB (gpu 99, renderer 77, browser 41); host CPU 0.1 % idle with the list loaded. Budgets in docs/05 §6 revised to < 90 MB host / < 330 MB all with the attribution recorded; Q16/Q17 opened for the M7 perf pass | `Get-Process` + module listing | accepted |
| D-049 | 2026-09-21 | M4 UI: frameless window with custom title bar (permissions `core:window:allow-{minimize,toggle-maximize,is-maximized,close,start-dragging}`), virtualised grid (36 px rows, viewport + 8 overscan, roving keyboard selection, `aria-sort`), filter bar persisted in localStorage, details pane with live INFO/RULES/PLAYER and installed-mod ticks from the Workshop inventory, Slate/Light themes with four accents. Verification rules R0–R5 wired end to end | code, svelte-check 0 warnings | accepted |
| D-051 | 2026-09-21 | M5 end-to-end on this machine (`tests/live_join.rs`): Steamworks ready in 100 ms; `ISteamUGC` details for 1819514788 ("Ear-Plugs", 109 548 B); subscribe + high-priority download + install in 1 265 ms (states pending → downloading → installed); `@Ear-Plugs` junction created in `!Workshop` (20 → 21, official ones untouched); `DayZ_BE.exe 0 1 1 -exe DayZ_x64.exe -connect=193.25.252.87 -port=2302 -name=LauncherTest -skipintro -nosplash` started against a verified vanilla server and both BE and `DayZ_x64.exe` were alive after 30 s, then killed by the test. Side effect: the test Steam account is now subscribed to Ear-Plugs | test output | verified |
| D-052 | 2026-09-21 | M5 design: sync jobs run on the Steam thread (subscribe callback → `DownloadItem(high_priority)` → poll `GetItemState`/download info every 250 ms, re-kick idle items every 5 s, 45 min ceiling); Workshop details paged by 50 (`kNumUGCResultsPerPage`); junction names `@<meta.cpp name>` with a `(id)` sibling on clashes, never deleted; `-name` defaults to the Steamworks persona (Q13 closed, no `loginusers.vdf` parsing needed); launch settings in `%APPDATA%\com.dayzlauncher.desktop\settings.json` | docs/02 §4–6, crate source | accepted |
| D-053 | 2026-09-21 | M6 live checks (`tests/live_m6.rs`): the official `FavouriteServers.xml` on this machine parsed (1 entry, its server live at 138 ms); direct connect by **game** port must match the INFO reply's EDF game port because one host runs several servers — `51.81.8.81` answers on 27016 (game 2302, "WILDLANDZ Livonia") and 27017 (game 2402, "Green County"); a first version accepted the first answer and picked the wrong server. Rule now: typed port as query port first, else scan game+1..+3 and 27015–27020 requiring `game_port == typed`. Population samples come only from **verified** head-counts (never from INFO), kept 7 days, drawn as hourly maxima over 72 h | test output | verified |
| D-054 | 2026-09-21 | M6 storage: `favourites`, `history`, `population` tables are user data and survive the pre-release drop-and-recreate policy that applies to `servers` only (schema v4). History is written on every launch; favourites are keyed by `ip:queryPort`; imported favourites whose server is offline are stored from the XML fields (name, map, max players, version, tags) and verified later | code, cache tests | accepted |
| D-055 | 2026-09-21 | M7 additions: app icon generated from `tools/make_icon.js` (dependency-free PNG encoder, amber "Z" on a dark tile) via `tauri icon`; first-run Welcome overlay (localStorage flag) with favourites import; diagnostics export (`diagnostics_export` writes a JSON support report to the app data folder) and copy-to-clipboard; updater wired with `tauri-plugin-updater` 2.12.0 + `tauri-plugin-process` 2.3.1 (crates.io max stable; the 3.0 alphas are ignored), minisign key pair generated with `tauri signer generate --ci` into `%USERPROFILE%\.tauri\dayz-launcher.key` (never in the repo), public key in `tauri.conf.json`, endpoint placeholder `https://github.com/OWNER/dayz-launcher/releases/latest/download/latest.json` to be replaced when a release host exists; the in-app check reports failure honestly until then | code, crates.io, CLI output | accepted |
| D-056 | 2026-09-21 | Q17: removing `will-change: transform` from the virtual table's row window lowered the WebView2 GPU process commit from 98.3 to 92–93 MB with no observable scroll cost; `contain: strict` on the scroll body kept | release measurements | verified |
| D-057 | 2026-09-21 | Q16: an idle Steamworks release (`DAYZ_STEAM_IDLE_SECS=20`) did drop the `Client` and unload `steamclient64.dll`, but host private bytes were 61.1 MB before and after (the allocator keeps the freed heaps); the feature stays in the worker as an opt-in experiment and is **off by default** | release measurements | verified |
| D-058 | 2026-09-21 | Installer verified (Q14): 7-Zip listing shows `dayz-launcher.exe` and `steam_api64.dll` at the install root; silent install (`/S /D=<dir>`) exit 0, installed app alive after 10 s with `steam_api64.dll` loaded from the install folder and `steamclient64.dll` from Steam; silent uninstall exit 0, folder and HKCU uninstall entry gone. Installer 3.23 MB, exe 8.54 MB after the updater's rustls stack. Updater signing needs `TAURI_SIGNING_PRIVATE_KEY` (path or content); the `_PATH` variant printed by `signer generate` is not read by the bundler | build + install logs | verified |
| D-059 | 2026-09-21 | No code signing for the first releases. Consequence: SmartScreen "unknown publisher" warning on the installer; documented in README and Q11 | user | accepted |
| D-060 | 2026-09-21 | Q18 investigation, four 7–8 min idle runs of the release build: run 2 renderer 69→187 MB and host CPU 9.7 s; run 3 (after making the sort stable and moving visible-row re-verification to a 60 s timer) renderer 165→196 MB and host CPU 34.8 s; a debug run showed CPU growing in 30 s windows with no IPC, verification or Steam activity logged; thread attribution in run 4 put the busiest host thread outside every loaded module (consistent with the in-process Steam client) with total CPU only 2.7 s in 7 min; run 5 (Steam quiet) host 70 MB flat, renderer 65–72 MB flat, CPU 0.15 %. Steam's content/workshop logs show no downloads during runs 2–3. Outcome: not reproducible; the two bad runs are recorded as unexplained. Kept: ping quantised to 20 ms with id tie-breaks in every sort, re-verification only on viewport change or a 60 s timer (paused when hidden), 120 s freshness. Diagnostics view was invoked 7× in the dev run because Vite reloads remounted it; harmless (≈8 ms each) | run logs | recorded, monitoring |
| D-061 | 2026-09-21 | Release host is GitHub: repo `NobbyBo11ocks/dayz-launcher` created 2026-09-21, made public so the updater can fetch release assets without a token. Endpoint set to `https://github.com/NobbyBo11ocks/dayz-launcher/releases/latest/download/latest.json` (static-manifest form from S-55). Each release must attach the installer, its `.sig` and a `latest.json` whose `platforms.windows-x86_64.url` points at the installer asset; GitHub rewrites spaces in asset names to dots, so the URL uses `DayZ.Launcher_<ver>_x64-setup.exe`. Procedure in README | S-55, gh CLI output | accepted |
| D-062 | 2026-09-21 | **v0.1.0 published** at https://github.com/NobbyBo11ocks/dayz-launcher/releases/tag/v0.1.0 from commit 133672f (rebuilt so the exe embeds the real endpoint; installer 3 229 963 B, SHA-256 `a999b10e…b839`). GitHub renamed the asset to `DayZ.Launcher_0.1.0_x64-setup.exe` (spaces → dots, confirmed via the API), so `latest.json` was generated after the upload from the real asset URL. End-to-end check: the manifest fetched through the `releases/latest/download/latest.json` endpoint parses, the installer URL answers 200 with the same hash, and an independent Ed25519/BLAKE2b-512 check (Node crypto, minisign "ED" prehash) validates both the file signature and the global signature against the pubkey in `tauri.conf.json` (key id b809d4b505f6e54d). Note: the `.sig` file content is already base64 (decodes to the minisign text block) and goes into `signature` verbatim | gh CLI, curl, scratch verify_sig.js | verified |
| D-063 | 2026-09-21 | Release tooling added under `tools/`: `make_latest.js` (reads version/productName from `tauri.conf.json`, the local `.sig`, and the installer asset URL from `gh release view`, writes `latest.json`; refuses a tag that does not match the configured version) and `verify_update_sig.js` (fetches the manifest through the configured endpoint, downloads the installer, verifies minisign ED-prehash file + global signatures with node:crypto, exit 0 only on success). Both tested against the live v0.1.0 release: the generated manifest matches the published one byte-for-byte except `pub_date`; the checker passed. README release procedure now uses them | live run | verified |
| D-064 | 2026-09-21 | **Install from GitHub verified**: the release installer downloaded from the v0.1.0 asset URL (same SHA-256 as the build) installed silently, the app started (Steam connected, list refreshed) and both the automatic startup check and the manual *Check for updates* button reported *Up to date* against the live `latest.json`. Two installer findings (S-56): (1) the first silent install landed in the M7 test's temp folder because NSIS restores the previous location from `HKCU\Software\dayzlauncher\DayZ Launcher` and the uninstaller leaves that key unless "delete app data" is ticked; re-installed with `/D=%LOCALAPPDATA%\Programs\DayZ Launcher`, which also rewrote the key. (2) The per-user default `%LOCALAPPDATA%\DayZ Launcher` is the **official Bohemia launcher's data folder** (bisignCache.nson, News.json, FavouriteServers.xml live there on this machine); our files would be dropped into it. Not fixed yet → Q19 | live install, S-56 | verified, Q19 open |
| D-065 | 2026-09-21 | **Details pane loop fixed** (the description and mod list never appeared, and "no population samples yet" flickered). Cause: `server_details` emits `servers:verified` for the queried server (commands.rs), the store replaces the row object, `servers.selected` yields a new object, and both effects in `DetailsPane.svelte` read `row?.id` / `row?.verifiedAt` through the `row` prop, so they re-ran on every replacement: details reset to null and the A2S query restarted, endlessly (one INFO+RULES+PLAYER burst per round trip against the selected server). Fix: `const id = $derived(row?.id ?? null)` and `verifiedAt` likewise; Svelte 5.57 deriveds compare with `===` (`equals` in `reactivity/equality.js`), so unchanged primitives do not retrigger. Details now load once per selection; samples refetch on a new verification without blanking | code, svelte source | fixed |
| D-066 | 2026-09-21 | **v0.1.1 published** at https://github.com/NobbyBo11ocks/dayz-launcher/releases/tag/v0.1.1 from commit f4d867f (version bumped in package.json, package-lock.json, Cargo.toml, Cargo.lock, tauri.conf.json; installer 3 229 947 B, SHA-256 `1ba15514…49b9`). Assets: `DayZ.Launcher_0.1.1_x64-setup.exe`, `.sig`, `latest.json` (from `tools/make_latest.js`). `tools/verify_update_sig.js` against the live endpoint: version 0.1.1, key id matches, file and global signatures valid. The `releases/latest` redirect now points at v0.1.1, so 0.1.0 installs will offer the update on their next check. Release notes carry the install-folder caveat from Q19 | gh CLI, tools output | verified |
| D-067 | 2026-09-21 | **Custom NSIS template** (Q19 resolved, option (a) chosen): `src-tauri/nsis/installer.nsi` is a verbatim copy of Tauri's template at tag `tauri-cli-v2.11.5` (S-56) with one line changed, the currentUser default `$INSTDIR` from `$LOCALAPPDATA\${PRODUCTNAME}` to `$LOCALAPPDATA\Programs\${PRODUCTNAME}`; wired via `bundle.windows.nsis.template`. `tools/nsis_template_check.js` re-fetches upstream for the installed CLI version and asserts the copy equals upstream + that change (`--write` refreshes it after a CLI upgrade); the dev notes and README point to it. Test: cleared the remembered location (`reg delete … /ve`), ran the rebuilt installer silently with no `/D`: it installed 0.1.1 into `%LOCALAPPDATA%\Programs\DayZ Launcher`, rewrote the uninstall key and the remembered location to that folder, the Start Menu shortcut targets it, and the official launcher's `%LOCALAPPDATA%\DayZ Launcher` was untouched (same timestamp, 17 entries, none of ours). Existing installs keep their folder on update because NSIS restores the remembered location; no migration attempted. Shipped in v0.1.2 (D-068) | live install, S-56 | verified |
| D-068 | 2026-09-21 | **v0.1.2 published** at https://github.com/NobbyBo11ocks/dayz-launcher/releases/tag/v0.1.2 from commit 62feeac, the first build with the custom NSIS template (installer 3 230 891 B, SHA-256 `e7e0ebda…3b8c`). Assets: `DayZ.Launcher_0.1.2_x64-setup.exe`, `.sig`, `latest.json`. `tools/verify_update_sig.js` against the live endpoint: version 0.1.2, key id matches, file and global signatures valid; `releases/latest` redirects to v0.1.2. Release notes explain the new default folder and how to move an install that landed in the official launcher's data folder (uninstall without deleting app data, then reinstall) | gh CLI, tools output | verified |
| D-069 | 2026-09-21 | **Collapsible details pane**: the pane is rendered only while a row is selected (`{#if servers.selected}` in Servers and Favourites; the grid gets `with-pane` for the 360 px column), a click on the selected row clears the selection (`onSelect(null)`), Escape clears it too, and the second click of a double-click (`MouseEvent.detail > 1`) is ignored so double-click still selects and joins. The accent-coloured fill bar under the player count was removed (it read as a strikethrough). Verified on the installed build with screenshots: full-width table with nothing selected, pane on click, collapse on second click | screenshots | verified |
| D-029 | 2026-09-21 | M1 acceptance: `cargo test` 9 passed (fixtures = this machine's `libraryfolders.vdf`, `appmanifest_221100.acf` with LastOwner redacted, `appworkshop_221100.acf`, CF `meta.cpp`/`mod.cpp`); `cargo test -- --ignored live_diagnostics` reproduces docs/02 exactly: Steam `C:\Program Files (x86)\Steam` (HKCU), running pid 15176, 2 libraries, DayZ in `G:\SteamLibrary`, exe `1.29.0.163709` / game `1.29.163709`, build 24689949, StateFlags 4, 4 Workshop items with meta/mod names, 20 junctions (16 dangling), 7 ms; clippy 0 warnings; svelte-check 0 errors | test output | verified |
| D-011 | 2026-09-21 | Game port for `-port=` comes from A2S_INFO EDF 0x80, not the query port | S-42 (2402 vs 27017), S-18 `gamePort` | verified |
| D-012 | 2026-09-21 | Version check = A2S `version` vs `DayZ_x64.exe` ProductVersion (`1.29.0.163709` ↔ `1.29.163709`) | S-41, S-42 | verified |
| D-013 | 2026-09-21 | Docs are written with an editor; a multi-file Bash heredoc failed to parse in this shell (Git Bash on Windows). Not a product decision, recorded to avoid repeating it | session | noted |
