# 09 · Decisions and verification log

Append-only. Format: `D-nnn · date · decision · evidence (S-ids) · status`. Every patch that changes protocol, launch, Steam, versions or performance behaviour gets an entry **before** the code change, with the source re-checked that day.

| ID | Date | Decision / check | Evidence | Status |
|---|---|---|---|---|
| D-001 | 2026-09-21 | Windows-only target; Linux dropped | user instruction | accepted |
| D-002 | 2026-09-21 | Stack: Tauri 2.11.6 + Rust 1.98 + Svelte 5.57 + Vite 8.3 + TS 7.0; NSIS per-user installer; WebView2 downloadBootstrapper **Correction**: TS 7.0 here is wrong — the project is on TypeScript 6 (D-016, docs/04 §19). | S-20…S-34, S-41 | accepted (ADR-001) |
| D-003 | 2026-09-21 | Primary server list from Steamworks `ISteamMatchmakingServers`; A2S direct for ping/details/mods; DZSA/BattleMetrics optional | S-25, S-15, S-18 | accepted (ADR-002) |
| D-004 | 2026-09-21 | DayZ RULES escape table is `01 01→01`, `01 02→00`, `01 03→FF`; the velvetcache summary's `→02/→03` is rejected | S-12 `sequences.go`, S-14, S-42 live (447→443 bytes, 0 trailing) | verified |
| D-005 | 2026-09-21 | Fragment keys are 2 bytes `[index 1-based, total]`; sort by index and concatenate | S-12, S-14, S-42 (`1/4…4/4`) | verified |
| D-006 | 2026-09-21 | DayZ payload order: version u8, overflow u8, dlcFlags u16, dlcHash u32×popcount, modCount u8, mods{hash u32, idLen u8 & 0x0F, id LE, nameLen u8, name}, sigCount u8, sigs{len,str}, desc{len,str} | S-12, S-14, S-42 | verified |
| D-007 | 2026-09-21 | Match mods by Workshop ID only; names differ across meta.cpp / mod.cpp / DZSA | S-41 (CF vs Community Framework vs @CF), S-42 | verified |
| D-008 | 2026-09-21 | `-mod=` uses absolute junction paths, `;`-separated, one quoted argument, in server-reported order | S-41 RPT logs | verified |
| D-009 | 2026-09-21 | Reuse the official `!Workshop\@<meta.cpp name>` junction convention; create only missing junctions | S-41 (20 live junctions) | accepted |
| D-010 | 2026-09-21 | Spawn `DayZ_BE.exe` (not `steam://run`) with working dir = game folder, arguments `0 1 1 -exe DayZ_x64.exe "-mod=…" …`; wait on that process for exit code | S-41 `Launcher.log` `GameExecutor: Starting the game, process path: …\DayZ_BE.exe` / `parameters: 0 1 1 -exe DayZ_x64.exe "-mod=…"` (2026-07-23 23:56:31), S-13 agrees | verified (Q1 closed) |
| D-014 | 2026-09-21 | Official launcher initialises Steamworks (`Steam init result: Online`), uses the UGC download manager (`Item was installed: <id>`) and caches PBO/bisign signatures; our Steamworks-based design mirrors it | S-41 `Launcher.log` | verified |
| D-015 | 2026-09-21 | Frontend is plain Svelte 5 + Vite (index.html, `src/main.ts`, `App.svelte`), not SvelteKit; kept the template's `src-tauri/` (icons, build.rs) and rewrote the rest | S-46; docs/05 §7 "no runtime deps beyond @tauri-apps/api" | accepted |
| D-016 | 2026-09-21 | TypeScript pinned to 6.0.3 instead of 7.0.2 because `svelte-check` 4.7.6 peers on `^5 \|\| ^6` | S-47 | accepted, revisit |
| D-017 | 2026-09-21 | Rust installed with rustup-init (`-y --default-toolchain stable --profile default --no-modify-path`), SHA-256 verified against rust-lang; `%USERPROFILE%\.cargo\bin` prepended to the user PATH manually | S-49, S-23 | done |
| D-018 | 2026-09-21 | `steam_api64.dll` will be taken from the vendored `steamworks-sys` redistributable folder and bundled as a Tauri resource (Q6 closed) | S-48 | accepted |
| D-019 | 2026-09-21 | Tauri config: NSIS only, per-user, WebView2 `downloadBootstrapper` silent, strict CSP (`'unsafe-inline'` only for styles), `withGlobalTauri` off, capability `core:default` only, window 1280×800 min 960×600, lib crate-type `rlib` only, release profile fat LTO + abort + strip | S-33, S-34, docs/05 §7 | accepted |
| D-020 | 2026-09-21 | Vite 8 is Rolldown/Oxc based: `build.minify` is `"oxc"` (esbuild is no longer bundled; `minify: "esbuild"` fails with "Cannot find package 'esbuild'") and the top-level `esbuild` option is replaced by `oxc`. docs/05 §7 corrected | `node_modules/vite/dist/node/index.d.ts` lines 2884, 3540–3545; build error output | verified |
| D-021 | 2026-09-21 | M0 build results: `svelte-check` 0 errors; Vite bundle 36.49 kB JS (14.44 kB gzip) + 2.11 kB CSS (0.97 kB gzip); `cargo build` debug 52.8 s cold; `npm run tauri build` release 2 m 09 s cold; `dayz-launcher.exe` 4 064 768 B; NSIS installer `DayZ Launcher_0.1.0_x64-setup.exe` 1 315 556 B (budget < 15 MB). Tauri CLI fetched NSIS 3.11 and nsis_tauri_utils 0.5.3 with hash validation | build output | verified |
| D-022 | 2026-09-21 | Bundle identifier changed from `com.dayzlauncher.app` to `com.dayzlauncher.desktop` (Tauri warns `.app` suffix conflicts with macOS bundles; harmless on Windows but cheaper to fix before first install) | `tauri build` warning | accepted |
| D-023 | 2026-09-21 | M0 idle memory baseline, release exe, 7 s after launch, 1280×800: host WS 25.4 MB / commit 5.9 MB; WebView2 browser 114.5 / 39.1, gpu 63.7 / 75.1, renderer 65.5 / 30.5, utility 35.3 / 11.4 and 17.0 / 7.5, crashpad 9.5 / 2.2 (WS / commit, MB). Totals: WS 331 MB, commit 171.7 MB, 7 processes. Budget metric redefined to private bytes (docs/05 §6) | `Get-Process` + `Win32_Process` measurement script | verified |
| D-024 | 2026-09-21 | IPC smoke test: `npm run tauri dev` → Vite ready in 734 ms, debug rebuild 3.63 s, stderr shows `[ipc] app_info called from the webview` within ~8 s; process tree killed cleanly (0 leftover app or Vite processes). The `eprintln!` is `#[cfg(debug_assertions)]` only | dev log | verified |
| D-025 | 2026-09-21 | Exe version must be read from the `StringFileInfo\<lang><cp>\ProductVersion` string (translations from `VarFileInfo\Translation`, then 0409/04B0, 04E4, 0000); `VS_FIXEDFILEINFO` has 16-bit fields and cannot hold DayZ's build 163709 (the compiler rejected the literal in a test). Live result: `1.29.0.163709` → game form `1.29.163709` | `windows-sys` `VS_FIXEDFILEINFO` definition; live diagnostics | verified |
| D-026 | 2026-09-21 | `HKCU\Software\Valve\Steam\ActiveProcess\pid` is stale (16884 vs live steam.exe 15176); Steam liveness = ToolHelp enumeration for `steam.exe` with image path under `SteamPath`. The registry pid is kept as `registryPid` for display only | `Get-Process steam` vs registry read | verified |
| D-027 | 2026-09-21 | Junction inventory uses `symlink_metadata` + `FILE_ATTRIBUTE_REPARSE_POINT` + `junction::get_target` (reads reparse data, works on dangling targets). `junction::exists` follows the link and dropped the 16 dangling `!Workshop` entries on this machine; the inventory now lists them and warns | junction 2.0.0 `internals.rs`; PowerShell listing (20 junctions, 4 targets exist) | verified |
| D-028 | 2026-09-21 | M1 crates pinned: winreg 0.56.0, keyvalues-parser 0.2.4 (pest 2.9.2), junction 2.0.0, thiserror 2.0.20, windows-sys 0.61.2 (already in Tauri's tree; features Foundation, Storage_FileSystem, System_Threading, System_Diagnostics_ToolHelp) | Cargo.lock, S-26–S-28 | accepted |
| D-030 | 2026-09-21 | M1 in-app smoke test: `npm run tauri dev` → Diagnostics view invoked the command inside the WebView; debug trace `[ipc] diagnostics: steam_running=true libraries=2 dayz=true workshop_items=4 junctions=20 warnings=1 in 8 ms` within ~8 s; process tree killed with 0 leftovers | dev stderr | verified |
| D-031 | 2026-09-21 | Legacy Steam Master Server Query Protocol is retired: `hl2master.steampowered.com` and `hl1master.steampowered.com` are NXDOMAIN on the system resolver, 1.1.1.1 and 8.8.8.8; `Steam\config\MasterServer2.vdf` is not shipped any more; community dates the shutdown to Oct 2025. `a2s/master.rs` and `tools/master_query.js` (written and unit-tested against the wiki samples) were deleted rather than kept as dead code. List fallbacks are now: user-supplied Steam Web API key, then DZSA | S-50 | verified |
| D-032 | 2026-09-21 | M2 fixture set captured with `tools/a2s_capture.js` from 51.81.8.81:27017: `wildlandz.info.bin` 207 B, `wildlandz.rules.0.bin` 609 B (single datagram, 13 rules, 4 fragments, 12 mods, 12 signatures, 0 trailing bytes), `wildlandz.player.bin` 6 B, plus `wildlandz.expected.json` from the reference JS parser. Node tools converted to ESM (`tools/lib/a2s.js`) because the root package.json is `type: module` | S-42, capture output | verified |
| D-033 | 2026-09-21 | DayZ sends A2S_RULES as one datagram regardless of size: 121-mod server 4 587 B (43 rules, 34 fragments, 95 signatures), 105-mod server 5 309 B (49 rules, 40 fragments, 197 signatures); no split header seen. Client receive buffer is 64 KiB; split reassembly kept per spec. New keyword `allowedFilePatching` added to `DayzTags`. Fixtures `kingofgames.*`, `volatile.*` | `tools/a2s_capture.js` 188.24.17.159:27016 and 51.89.173.29:2303 | verified |
| D-034 | 2026-09-21 | A2S_PLAYER on DayZ: one entry per connected player, empty name, score 0, real duration (1963.8 s, 946.1 s …). Use entry count as an independent head-count vs INFO `players`; never expect names (Q3 closed) | same captures | verified |
| D-035 | 2026-09-21 | The per-mod u32 hash in the DayZ RULES payload is a content hash: CF 1559212036 = `0x68AFC6C1` on all three captured servers. Keep it in `Mod` for future build-mismatch warnings; matching stays by Workshop ID | fixtures | verified |
| D-036 | 2026-09-21 | DZSA's full list is fetchable outside a browser when a browser User-Agent is sent (HTTP 200, 24.5 MB, 0.6 s; plain fetcher UA gets 403). 18 678 servers, 18 672 unique addresses, 2 030 with players > 0, 18 211 on 1.29.163709. Used only as the sweep-benchmark address source; product fallback per D-031 | `curl -A "Mozilla/5.0 … Chrome/153"` | verified |
| D-037 | 2026-09-21 | Live INFO sweep over the 18 678 DZSA addresses (per-query sockets, no pacing): 256-wide/2 s → 12 561 ok, 6 116 timeouts, 53.7 s, RTT p50 30 / p90 99 / p99 251 / max 399 ms; 256-wide/1 s → 9 326 ok, 41.0 s; 512-wide/1 s → 3 803 ok, 30.4 s; 1024-wide/1.5 s → 2 014 ok. Loss grows with burst size (consumer NAT/ISP), not with server latency, so the client gained send pacing (default 400 datagrams/s) and defaults of 128 concurrency, 1 s timeout, 1 retry. The "20 000 in 15 s" acceptance is withdrawn: Steamworks supplies the list with pings (ADR-002); our fan-out serves visible rows, favourites and on-demand RULES. One address in the list is `0.0.0.0:2303` (io error 10049), correctly classified | `tests/live_sweep.rs` runs | verified |
| D-038 | 2026-09-21 | **Fake population is widespread.** Of 9 326 responding servers, 5 405 report 61–127 players in A2S_INFO (e.g. `XOMA PvP 120/127`, `Divide Horizon 120/120`), yet DZSA counts only 2 030 servers with any players and A2S_PLAYER returns real per-player entries (D-034). The UI must show the PLAYER head-count as the player number for any server it displays and flag INFO/PLAYER mismatches; INFO `players` is only a hint for sorting before verification | sweep histogram, D-034 | accepted |
| D-039 | 2026-09-21 | Paced re-run after a 5-minute cool-down (256-wide, 2 s, 400 pps): 11 219 ok, 7 458 timeouts, 94.9 s, RTT p50 27 / p99 280 ms; the same settings after only 90 s cool-down gave 6 491. Confirms the NAT flow-table explanation (recovery with rest) and shows pacing at 400 pps neither hurts nor helps success within run-to-run variance (11.2k vs 12.6k unpaced). Defaults stay conservative (400 pps, 128 concurrency, 1 s, 1 retry); the product never sweeps the full list | `tests/live_sweep.rs` run 6 | verified |
| D-040 | 2026-09-21 | `steam_api64.dll` vendored at `src-tauri/resources/steam_api64.dll` (317 080 B, SHA-256 `eb17909a76668cf9ae0b92a618a34a50f6c73d3a6787cb4dd8ce36a8b10bfb75`, copied from `steamworks-sys` 0.13.0 `lib/steam/redistributable_bin/win64`, Steamworks SDK 1.64 redistributable). `build.rs` copies it next to dev and test binaries; `bundle.resources` maps it beside the installed exe (verify placement in M7, Q14) | S-48, crate source | accepted |
| D-041 | 2026-09-21 | Steam caps `RequestInternetServerList` at **10 000** servers per request (observed `total=10000` exactly on an unfiltered query and again on `noplayers`). Refresh now runs partitions: `hasplayers` (2 988 total / 2 684 ok / 37.9 s) then `noplayers` (10 000 capped / 9 171 ok / 131.9 s); 12 988 total in 169.8 s. Steamworks init 454–529 ms after launch; warm start renders 13 464 cached rows 677–705 ms after launch (budget < 1 s). Empties need a further split (Q15) | dev smoke logs | verified |
| D-042 | 2026-09-21 | Auto-refresh re-triggered on every page load/launch because the 60 s throttle only knew the in-process time; `SteamWorker` now seeds `last_done` from the cache's `last_refresh` meta and compares unix seconds | dev smoke log run 2 | fixed |
| D-043 | 2026-09-21 | Fake population: the memory patch inflates A2S_INFO only; PLAYER stays truthful (55/126 sampled servers: INFO 52–127 vs PLAYER 0) or is suppressed entirely (25/126 never answer PLAYER). Detection rules R1–R7 in docs/11; verified head-count becomes the displayed number, inflated/unverifiable hidden by default | S-51–S-54 | accepted |
| D-044 | 2026-09-21 | Cache statistics after the first partitioned refresh (`tools/cache_stats.js`, 13 491 rows): INFO-populated 11 563 (61+: 8 747); genuinely empty 1 928 of which no3rd 325 / 3PP 1 603, privHive 1 928, password 784; empties by map chernarusplus 1 117, deerisle 198, enoch 142, namalsk 95, sakhal 85, banov 27, Bitterroot 27, Pripyat 24; eight IPs host 176–395 "populated" servers each; 11 174 rows ping < 60 ms | script output | verified |
| D-045 | 2026-09-21 | Steam's `hasplayers`/`noplayers` filters use the master's authenticated player count, which the A2S memory patch does not affect: `hasplayers` = 2 988 servers, while ~8 900 INFO-"populated" servers arrived via `noplayers`. `ServerRow.steam_empty` records the partition; `inflated() = steam_empty && players > 0` (rule R0) ships in M3 with a default "Hide inflated" filter. Empty servers are now fetched per map (10 maps from D-044) plus a capped catch-all (Q15 closed). Cache schema bumped to v2 (drop-and-recreate policy pre-release) | D-041 partition totals, D-044 | accepted |
| D-046 | 2026-09-21 | Full 12-partition refresh on a fresh cache: 36 679 total / 33 605 responded / **27 037 inflated** in 473 s. `hasplayers` 3 018 (0 inflated, 38 s); `noplayers`+chernarusplus 10 000 capped (7 677 inflated), +enoch 10 000 capped (8 524), +namalsk 10 000 capped (8 990), +sakhal 2 287 (1 610), +deerisle 1 078 (236), +banov 157, +Bitterroot 139 (0 inflated); the last four requests got `NoServersListedOnMasterServer` (Steam throttling back-to-back list requests). Warm start with 33 575 rows: 734 ms; relaunch throttle confirmed. Decisions: automatic refresh = `hasplayers` only; "Full refresh" is manual and runs the map partitions with a 3 s gap and early stop after 2 empty master answers | dev smoke log run m3f/m3g | accepted |
| D-047 | 2026-09-21 | M4 verification pass, first attempt (2 821 servers, INFO+PLAYER back to back, 400 pps): verified 1 732, inflated 7, synthetic 3, unverifiable 676, offline 403 in 60 s. **Corrected by D-050**: the 676 and 403 were burst loss, not host firewalls. Fixes: the automatic pass sends PLAYER only (Steam's INFO is a minute old at most), offline targets are retried with INFO at a 2.5 s timeout, and only the automatic pass emits `servers:verify-done` (an on-demand check of 25 visible rows had masqueraded as the full pass). R4 keeps a safety clause: an "unverifiable" verdict never hides a row Steam vouched for (`steamEmpty == false`) | dev smoke logs m4a/m4b | fixed |
| D-050 | 2026-09-21 | M4 verification pass after the fixes (2 858 servers): **verified 2 840, inflated 5, synthetic 3, unverifiable 6, offline 4 in 33 s**; on-demand checks of 25 visible rows 0.1–2.1 s. The 8 fake-among-populated cases are the "N ghost + real players" variant of the memory patch (S-51) that rule R0 cannot see; R3/R5 catch them | dev smoke log m4c | verified |
| D-048 | 2026-09-21 | M4 measurements (release): exe 5.99 MB, installer 2.26 MB; cached list of 2 886 rows rendered 748 ms after launch; host private 65–79 MB with `steamclient64.dll` + `gameoverlayrenderer64.dll` loaded in-process by Steamworks; all-process commit 294–315 MB (gpu 99, renderer 77, browser 41); host CPU 0.1 % idle with the list loaded. Budgets in docs/05 §6 revised to < 90 MB host / < 330 MB all with the attribution recorded; Q16/Q17 opened for the M7 perf pass | `Get-Process` + module listing | accepted |
| D-049 | 2026-09-21 | M4 UI: frameless window with custom title bar (permissions `core:window:allow-{minimize,toggle-maximize,is-maximized,close,start-dragging}`), virtualised grid (36 px rows, viewport + 8 overscan, roving keyboard selection, `aria-sort`), filter bar persisted in localStorage, details pane with live INFO/RULES/PLAYER and installed-mod ticks from the Workshop inventory, Slate/Light themes with four accents. Verification rules R0–R5 wired end to end | code, svelte-check 0 warnings | accepted |
| D-051 | 2026-09-21 | M5 end-to-end on this machine (`tests/live_join.rs`): Steamworks ready in 100 ms; `ISteamUGC` details for 1819514788 ("Ear-Plugs", 109 548 B); subscribe + high-priority download + install in 1 265 ms (states pending → downloading → installed); `@Ear-Plugs` junction created in `!Workshop` (20 → 21, official ones untouched); `DayZ_BE.exe 0 1 1 -exe DayZ_x64.exe -connect=193.25.252.87 -port=2302 -name=LauncherTest -skipintro -nosplash` started against a verified vanilla server and both BE and `DayZ_x64.exe` were alive after 30 s, then killed by the test. Side effect: the test Steam account is now subscribed to Ear-Plugs | test output | verified |
| D-052 | 2026-09-21 | M5 design: sync jobs run on the Steam thread (subscribe callback → `DownloadItem(high_priority)` → poll `GetItemState`/download info every 250 ms, re-kick idle items every 5 s, 45 min ceiling); Workshop details paged by 50 (`kNumUGCResultsPerPage`); junction names `@<meta.cpp name>` with a `(id)` sibling on clashes, never deleted; `-name` defaults to the Steamworks persona (Q13 closed, no `loginusers.vdf` parsing needed); launch settings in `%APPDATA%\com.dayzlauncher.desktop\settings.json` | docs/02 §4–6, crate source | accepted |
| D-053 | 2026-09-21 | M6 live checks (`tests/live_m6.rs`): the official `FavouriteServers.xml` on this machine parsed (1 entry, its server live at 138 ms); direct connect by **game** port must match the INFO reply's EDF game port because one host runs several servers — `51.81.8.81` answers on 27016 (game 2302, "WILDLANDZ Livonia") and 27017 (game 2402, "Green County"); a first version accepted the first answer and picked the wrong server. Rule now: typed port as query port first, else scan game+1..+3 and 27015–27020 requiring `game_port == typed`. Population samples come only from **verified** head-counts (never from INFO), kept 7 days, drawn as hourly maxima over 72 h | test output | verified |
| D-054 | 2026-09-21 | M6 storage: `favourites`, `history`, `population` tables are user data and survive the pre-release drop-and-recreate policy that applies to `servers` only (schema v4). History is written on every launch; favourites are keyed by `ip:queryPort`; imported favourites whose server is offline are stored from the XML fields (name, map, max players, version, tags) and verified later | code, cache tests | accepted |
| D-055 | 2026-09-21 | M7 additions: app icon generated from `tools/make_icon.js` (dependency-free PNG encoder, amber "Z" on a dark tile) via `tauri icon`; first-run Welcome overlay (localStorage flag) with favourites import; diagnostics export (`diagnostics_export` writes a JSON support report to the app data folder) and copy-to-clipboard; updater wired with `tauri-plugin-updater` 2.12.0 + `tauri-plugin-process` 2.3.1 (crates.io max stable; the 3.0 alphas are ignored), minisign key pair generated with `tauri signer generate --ci` into `%USERPROFILE%\.tauri\dayz-launcher.key` (never in the repo), public key in `tauri.conf.json`, endpoint placeholder `https://github.com/OWNER/dayz-launcher/releases/latest/download/latest.json` to be replaced when a release host exists; the in-app check reports failure honestly until then **Superseded by D-156**: the generator, the artwork and the toolchain all changed. | code, crates.io, CLI output | accepted |
| D-056 | 2026-09-21 | Q17: removing `will-change: transform` from the virtual table's row window lowered the WebView2 GPU process commit from 98.3 to 92–93 MB with no observable scroll cost; `contain: strict` on the scroll body kept | release measurements | verified |
| D-057 | 2026-09-21 | Q16: an idle Steamworks release (`DAYZ_STEAM_IDLE_SECS=20`) did drop the `Client` and unload `steamclient64.dll`, but host private bytes were 61.1 MB before and after (the allocator keeps the freed heaps); the feature stays in the worker as an opt-in experiment and is **off by default** | release measurements | verified |
| D-058 | 2026-09-21 | Installer verified (Q14): 7-Zip listing shows `dayz-launcher.exe` and `steam_api64.dll` at the install root; silent install (`/S /D=<dir>`) exit 0, installed app alive after 10 s with `steam_api64.dll` loaded from the install folder and `steamclient64.dll` from Steam; silent uninstall exit 0, folder and HKCU uninstall entry gone. Installer 3.23 MB, exe 8.54 MB after the updater's rustls stack. Updater signing needs `TAURI_SIGNING_PRIVATE_KEY` (path or content); the `_PATH` variant printed by `signer generate` is not read by the bundler | build + install logs | verified |
| D-059 | 2026-09-21 | No code signing for the first releases. Consequence: SmartScreen "unknown publisher" warning on the installer; documented in README and Q11 | user | accepted |
| D-060 | 2026-09-21 | Q18 investigation, four 7–8 min idle runs of the release build: run 2 renderer 69→187 MB and host CPU 9.7 s; run 3 (after making the sort stable and moving visible-row re-verification to a 60 s timer) renderer 165→196 MB and host CPU 34.8 s; a debug run showed CPU growing in 30 s windows with no IPC, verification or Steam activity logged; thread attribution in run 4 put the busiest host thread outside every loaded module (consistent with the in-process Steam client) with total CPU only 2.7 s in 7 min; run 5 (Steam quiet) host 70 MB flat, renderer 65–72 MB flat, CPU 0.15 %. Steam's content/workshop logs show no downloads during runs 2–3. Outcome: not reproducible; the two bad runs are recorded as unexplained. Kept: ping quantised to 20 ms with id tie-breaks in every sort, re-verification only on viewport change or a 60 s timer (paused when hidden), 120 s freshness. Diagnostics view was invoked 7× in the dev run because Vite reloads remounted it; harmless (≈8 ms each) | run logs | recorded, monitoring |
| D-061 | 2026-09-21 | Release host is GitHub: repo `NobbyBo11ocks/dayz-launcher` created 2026-09-21, made public so the updater can fetch release assets without a token. Endpoint set to `https://github.com/NobbyBo11ocks/dayz-launcher/releases/latest/download/latest.json` (static-manifest form from S-55). Each release must attach the installer, its `.sig` and a `latest.json` whose `platforms.windows-x86_64.url` points at the installer asset; GitHub rewrites spaces in asset names to dots, so the URL uses `DayZ.Launcher_<ver>_x64-setup.exe`. Procedure in README | S-55, gh CLI output | accepted |
| D-062 | 2026-09-21 | **v0.1.0 published** at https://github.com/NobbyBo11ocks/dayz-launcher/releases/tag/v0.1.0 from commit 133672f (rebuilt so the exe embeds the real endpoint; installer 3 229 963 B, SHA-256 `a999b10e…b839`). GitHub renamed the asset to `DayZ.Launcher_0.1.0_x64-setup.exe` (spaces → dots, confirmed via the API), so `latest.json` was generated after the upload from the real asset URL. End-to-end check: the manifest fetched through the `releases/latest/download/latest.json` endpoint parses, the installer URL answers 200 with the same hash, and an independent Ed25519/BLAKE2b-512 check (Node crypto, minisign "ED" prehash) validates both the file signature and the global signature against the pubkey in `tauri.conf.json` (key id b809d4b505f6e54d). Note: the `.sig` file content is already base64 (decodes to the minisign text block) and goes into `signature` verbatim | gh CLI, curl, scratch verify_sig.js | verified |
| D-063 | 2026-09-21 | Release tooling added under `tools/`: `make_latest.js` (reads version/productName from `tauri.conf.json`, the local `.sig`, and the installer asset URL from `gh release view`, writes `latest.json`; refuses a tag that does not match the configured version) and `verify_update_sig.js` (fetches the manifest through the configured endpoint, downloads the installer, verifies minisign ED-prehash file + global signatures with node:crypto, exit 0 only on success). Both tested against the live v0.1.0 release: the generated manifest matches the published one byte-for-byte except `pub_date`; the checker passed. README release procedure now uses them | live run | verified |
| D-064 | 2026-09-21 | **Install from GitHub verified**: the release installer downloaded from the v0.1.0 asset URL (same SHA-256 as the build) installed silently, the app started (Steam connected, list refreshed) and both the automatic startup check and the manual *Check for updates* button reported *Up to date* against the live `latest.json`. Two installer findings (S-56): (1) the first silent install landed in the M7 test's temp folder because NSIS restores the previous location from `HKCU\Software\dayzlauncher\DayZ Launcher` and the uninstaller leaves that key unless "delete app data" is ticked; re-installed with `/D=%LOCALAPPDATA%\Programs\DayZ Launcher`, which also rewrote the key. (2) The per-user default `%LOCALAPPDATA%\DayZ Launcher` is the **official Bohemia launcher's data folder** (bisignCache.nson, News.json, FavouriteServers.xml live there on this machine); our files would be dropped into it. Not fixed yet → Q19 | live install, S-56 | verified, Q19 open |
| D-065 | 2026-09-21 | **Details pane loop fixed** (the description and mod list never appeared, and "no population samples yet" flickered). Cause: `server_details` emits `servers:verified` for the queried server (commands.rs), the store replaces the row object, `servers.selected` yields a new object, and both effects in `DetailsPane.svelte` read `row?.id` / `row?.verifiedAt` through the `row` prop, so they re-ran on every replacement: details reset to null and the A2S query restarted, endlessly (one INFO+RULES+PLAYER burst per round trip against the selected server). Fix: `const id = $derived(row?.id ?? null)` and `verifiedAt` likewise; Svelte 5.57 deriveds compare with `===` (`equals` in `reactivity/equality.js`), so unchanged primitives do not retrigger. Details now load once per selection; samples refetch on a new verification without blanking | code, svelte source | fixed |
| D-066 | 2026-09-21 | **v0.1.1 published** at https://github.com/NobbyBo11ocks/dayz-launcher/releases/tag/v0.1.1 from commit f4d867f (version bumped in package.json, package-lock.json, Cargo.toml, Cargo.lock, tauri.conf.json; installer 3 229 947 B, SHA-256 `1ba15514…49b9`). Assets: `DayZ.Launcher_0.1.1_x64-setup.exe`, `.sig`, `latest.json` (from `tools/make_latest.js`). `tools/verify_update_sig.js` against the live endpoint: version 0.1.1, key id matches, file and global signatures valid. The `releases/latest` redirect now points at v0.1.1, so 0.1.0 installs will offer the update on their next check. Release notes carry the install-folder caveat from Q19 | gh CLI, tools output | verified |
| D-067 | 2026-09-21 | **Custom NSIS template** (Q19 resolved, option (a) chosen): `src-tauri/nsis/installer.nsi` is a verbatim copy of Tauri's template at tag `tauri-cli-v2.11.5` (S-56) with one line changed, the currentUser default `$INSTDIR` from `$LOCALAPPDATA\${PRODUCTNAME}` to `$LOCALAPPDATA\Programs\${PRODUCTNAME}`; wired via `bundle.windows.nsis.template`. `tools/nsis_template_check.js` re-fetches upstream for the installed CLI version and asserts the copy equals upstream + that change (`--write` refreshes it after a CLI upgrade); the dev notes and README point to it. Test: cleared the remembered location (`reg delete … /ve`), ran the rebuilt installer silently with no `/D`: it installed 0.1.1 into `%LOCALAPPDATA%\Programs\DayZ Launcher`, rewrote the uninstall key and the remembered location to that folder, the Start Menu shortcut targets it, and the official launcher's `%LOCALAPPDATA%\DayZ Launcher` was untouched (same timestamp, 17 entries, none of ours). Existing installs keep their folder on update because NSIS restores the remembered location; no migration attempted. Shipped in v0.1.2 (D-068) | live install, S-56 | verified |
| D-068 | 2026-09-21 | **v0.1.2 published** at https://github.com/NobbyBo11ocks/dayz-launcher/releases/tag/v0.1.2 from commit 62feeac, the first build with the custom NSIS template (installer 3 230 891 B, SHA-256 `e7e0ebda…3b8c`). Assets: `DayZ.Launcher_0.1.2_x64-setup.exe`, `.sig`, `latest.json`. `tools/verify_update_sig.js` against the live endpoint: version 0.1.2, key id matches, file and global signatures valid; `releases/latest` redirects to v0.1.2. Release notes explain the new default folder and how to move an install that landed in the official launcher's data folder (uninstall without deleting app data, then reinstall) | gh CLI, tools output | verified |
| D-069 | 2026-09-21 | **Collapsible details pane**: the pane is rendered only while a row is selected (`{#if servers.selected}` in Servers and Favourites; the grid gets `with-pane` for the 360 px column), a click on the selected row clears the selection (`onSelect(null)`), Escape clears it too, and the second click of a double-click (`MouseEvent.detail > 1`) is ignored so double-click still selects and joins. The accent-coloured fill bar under the player count was removed (it read as a strikethrough). Verified on the installed build with screenshots: full-width table with nothing selected, pane on click, collapse on second click | screenshots | verified |
| D-070 | 2026-09-21 | **UI preferences move to settings.json.** Trigger: after an uninstall/reinstall and an installer-launched relaunch, an instance started at 19:21 showed the welcome overlay and the default accent although the WebView's localStorage leveldb still held `onboarded` and `accent: green` and its LOG showed no open at 19:21, i.e. that instance never attached to the persistent profile (cause not identified; the uninstaller also ran at 18:45, which explains the missing install folder but not this). Decision: `Settings` gains `ui: UiPrefs { theme, accent, onboarded, filters (opaque object), lastUpdateCheckMs }`; `settings_set` now replaces launch options only and keeps the stored `ui`; new `ui_prefs_set(patch)` merges a partial object, validates theme/accent against the known names, persists and returns the result (4 unit tests). Frontend: `state/uiprefs.svelte.ts` is the single writer (patches coalesced within 150 ms, flushed on unload, retried on failure); theme/accent, filters and the last update check keep localStorage as an instant-start cache but the file wins once read; the welcome overlay shows only when the file says not onboarded, and a legacy localStorage flag is migrated silently. The settings file is `%APPDATA%\com.dayzlauncher.desktop\settings.json`; the uninstaller's "delete app data" removes it together with the cache, which is the user's intent | code, leveldb log inspection | verified |
| D-071 | 2026-09-21 | **GitHub Actions**: `ci.yml` (push to main, PRs, manual) runs svelte-check, the Vite build, `cargo fmt --check`, `cargo clippy --all-targets -D warnings`, `cargo test` and `tools/nsis_template_check.js` on `windows-latest`; the Rust sources were formatted once with rustfmt so the check can be enforced. `release.yml` (tag `v*`, or manual with a `publish` switch; manual default is a build-only dry run that uploads the bundle as a workflow artifact) checks the tag against `tauri.conf.json`, requires the `TAURI_SIGNING_PRIVATE_KEY` secret, builds and publishes through `tauri-apps/tauri-action@v1` (`updaterJsonPreferNsis`, `uploadUpdaterJson`, `generateReleaseNotes`, `args: --ci`; the action writes `latest.json` with GitHub's rewritten asset names), then smoke-installs the installer on the runner (silent install into the Programs folder, app must still run after 15 s, silent uninstall must leave no folder) and finally runs `tools/verify_update_sig.js` against the live endpoint with retries. Secrets are set by the maintainer (`gh secret set`, README); the manual tools remain for local releases. Source: tauri-action README at `dev` (inputs `tagName`, `releaseName`, `releaseBody`, `generateReleaseNotes`, `uploadUpdaterJson`, `updaterJsonPreferNsis`, `uploadWorkflowArtifacts`, `args`). First CI run (commit ef59cae) green in 7.5 min on a cold cache, all eleven steps; the runner warned that `checkout@v4`/`setup-node@v4` target Node 20, so both were bumped to v5. The release workflow's first run waits for the secrets | tauri-action README, GitHub Actions docs, run 35640713698 | CI verified, release pending secrets |
| D-072 | 2026-09-21 | **v0.1.3 published** by hand at https://github.com/NobbyBo11ocks/dayz-launcher/releases/tag/v0.1.3 from tag `v0.1.3` = commit d099d4c (tag pushed before the workflow files existed on main, so no Actions run was triggered). Installer 3 244 702 B, SHA-256 `29fd87b6…34b0`; `verify_update_sig.js` against the live endpoint: version 0.1.3, key id matches, both signatures valid; `releases/latest` redirects to v0.1.3. Ships D-069 (collapsible pane, no fill bars) and D-070 (settings-file preferences) | gh CLI, tools output | verified |
| D-073 | 2026-09-21 | **Country flags, offline** (Q5 resolved). Data: DB-IP IP-to-Country Lite (S-57) compacted by `tools/geoip_build.js` into `src-tauri/resources/geoip-v4.bin` (magic `DZGEO1`, code table, sorted u32 range starts + u8 country index; 357 325 IPv4 ranges, 246 codes, 1 787 129 B, ~770 KB after LZMA) embedded with `include_bytes!` and parsed once on first use (`geoip.rs`, binary search via `partition_point`; private/loopback/`ZZ` → none; 3 unit tests incl. 1.0.0.1 → AU, 8.8.8.8 → US). `ServerRow.country` is derived from the IP wherever a row is built (Steam list, A2S probe, import placeholder, cache load), not stored, so no schema bump. UI: `Flag.svelte` draws a 16×12 cell from one 8224×24 PNG sprite (`tools/flags_build.js`: 257 flag-icons 4x3 SVGs composed as nested `<svg>` with per-flag id prefixes and rasterised by resvg at 2×; 145 KB, emitted as a hashed asset), names from `Intl.DisplayNames`, flag before the server name, flag + country in the details header, a Country select in the filter bar (`filters.country`, persisted), and a Credits paragraph in Settings with the CC BY attribution. Data refresh: re-run `node tools/geoip_build.js` (downloads the current month) and commit the table; not automated because the file is 1.8 MB per refresh. Dev deps added: `flag-icons`, `@resvg/resvg-js` | S-57, S-58, S-59 | verified |
| D-074 | 2026-09-21 | **Wait for a free slot** in the join dialog. DayZ already queues players in-game (`lqs` keyword = login queue length, docs/03), so joining a full server straight away is legitimate and stays the default; the option covers the case where the user prefers not to sit in the game's queue. New `server_slots(id)` command does an INFO-only query (players, max, `lqs` queue, RTT); the dialog fetches it on open, shows the live count next to the address, and when the server is full offers a checkbox. "Wait and join" polls every 10 s (one datagram each way), shows checks and elapsed time, and when INFO reports `players < max` it flashes the taskbar entry (`requestUserAttention`, capability `core:window:allow-request-user-attention`) and runs the normal launch; "Stop waiting" and "Join now anyway" are always available. The server's own count is used deliberately: it is what the server admits by, inflated or not. Live run 20:12: on a full vanilla server (100/100, 7 queued) the dialog showed the live count and the option; "Wait and join" started polling, the very first check already saw a free slot, and DayZ was launched through BattlEye (pids 11744/20564, killed for the test). The launch happening while seven players were queued in-game shows INFO's count lags the queue: the launcher can only race for the slot, which the notes in the dialog say | docs/03 keyword table, Tauri window API, live run | verified |
| D-075 | 2026-09-21 | **Mod management** in the Mods view: sortable by name/size/updated (largest first by default), "Update N mods" runs the existing `mods_sync` job for items whose Workshop timestamp is newer than the installed one (progress inline via `mods:progress`/`mods:done`), and a per-row Unsubscribe with an inline confirmation. New `mods_unsubscribe(ids)` command → `SteamWorker::unsubscribe` → `Cmd::Unsubscribe` on the Steam thread, which calls `ISteamUGC::UnsubscribeItem` per id and collects the callbacks across ticks (`ActiveUnsubscribe`, 15 s timeout, missing answers reported as failed). Steam deletes the files on its own, normally once nothing runs as app 221100 (the launcher itself counts, so removal may wait until it exits); `!Workshop` junctions are never deleted (a dev-notes rule: they may belong to the official launcher) and show as stale until the official launcher or the user removes them. Live check `tests/live_m8.rs` (ignored): subscribe + download Ear Plugs (68 848 B) in 2.3 s, `unsubscribe` answered `Ok` in 237 ms, content folder still present 3 s later (Steam's own schedule) | steamworks-rs `UGC::unsubscribe_item`, existing sync flow, live run | verified |
| D-076 | 2026-09-21 | **v0.1.4 published** by hand at https://github.com/NobbyBo11ocks/dayz-launcher/releases/tag/v0.1.4 from tag `v0.1.4` = commit 9270ee4 (installer 3 837 013 B, SHA-256 `6f77737e…cd37`; `verify_update_sig.js` against the live endpoint: version 0.1.4, key id matches, both signatures valid; `releases/latest` → v0.1.4). Ships D-073 flags, D-074 wait-for-slot, D-075 mod management. The tag push also triggered `release.yml` for the first time: every step up to and including the tag/version check passed and it stopped at "Signing key is configured" because the repository has no secrets yet, exactly as designed; once they are set, the same tag push would have published the release itself | gh CLI, tools output, run 35643746747 | verified |
| D-077 | 2026-09-21 | **Steam idle release is a setting, on by default (15 min).** D-057 kept the idle shutdown opt-in because it saved no memory, but it has a user-visible effect that matters more: a Steamworks session for app 221100 makes Steam show the user as playing DayZ and count the time as playtime, like the official launcher does. `Settings.steam_idle_minutes` (0 = never) feeds `SteamWorker::spawn(.., idle_after)` and `set_idle_timeout` (pushed from `settings_set`); the worker reads the shared value each tick, `DAYZ_STEAM_IDLE_SECS` still overrides for experiments. Settings → Steam shows "Idle release after N min" with an explanation; the status line already says "released while idle (reconnects on use)". Reconnect is transparent on the next refresh, details query with Steam data, sync or join. Live check: launcher started 21:38:32, refresh and verification done by ~21:40, `steamclient64.dll` and `steam_api64.dll` gone from the process at 21:54:35 (polled every 30 s), i.e. the session was released after the configured 15 idle minutes | Steamworks init semantics (docs/02, ADR-002), D-057 measurements, live poll | verified |
| D-078 | 2026-09-21 | **Performance section in Diagnostics.** `perf.rs`: `mark_first_paint()` records `uptime_ms()` when the Servers view first renders rows (the view calls `perf_first_paint` from a `requestAnimationFrame` after its first non-empty list; only the first mark counts), `sample()` returns uptime, host private bytes and CPU time (`K32GetProcessMemoryInfo`, `GetProcessTimes`), and the private bytes of every `msedgewebview2.exe` descended from the host (ToolHelp parent links, breadth-first). Diagnostics shows them against the docs/05 budgets (start ≤ 1 000 ms, host ≤ 90 MB, total ≤ 330 MB) with a Sample button, and `diagnostics_export` embeds a sample so user reports carry the numbers Q18 needs. windows-sys feature `Win32_System_ProcessStatus` added; one unit test. First live sample (warm second instance, 20:28): 314 ms to the first list, host 87.9 MB, 3.8 % CPU during the start-up refresh; the WebView line read 0 because WebView2 shares one browser process per user data folder and that instance had attached to another instance's processes, so the walk now starts from every `dayz-launcher.exe` pid rather than only the caller. Cross-check after the fix (21:19, one instance): in-app host 81.4 MB / WebView 215.1 MB over 6 processes / total 296.5 MB versus an external `PrivateMemorySize64` reading nine seconds later of host 81.4 / WebView 208.6 MB; cold starts measured 795 ms and 726 ms. The 342 MB WebView reading from an earlier run, taken 34 s after start during the refresh, was therefore a real transient, not a counting error | docs/05 §6 budgets, Win32 psapi/ToolHelp docs, live samples | verified |
| D-079 | 2026-09-21 | **Single instance.** Two launchers were observed running side by side (one started by hand, one by a focus tool); each would refresh, verify and hold its own Steamworks session, and both shared one WebView2 browser process. `tauri-plugin-single-instance` 2.4.5 (crates.io max stable; README says it must be the first plugin) now hands a second launch to the running one, which un-minimises, shows and focuses its main window. Rust-only, no capability needed. Live check: a second launch exited within a second and the first window came to the front | plugin README, crates.io, live run | verified |
| D-080 | 2026-09-21 | **Find servers by mod.** After each verification pass, `run_mod_scan` fetches the A2S_RULES DayZ payload of populated, modded servers whose stored list is missing or older than 24 h (`server_mods` + `server_mods_at` tables, created by `CREATE TABLE IF NOT EXISTS` without a schema bump; pruned with the servers). Pacing is gentler than the player pass, 100 datagrams/s with one retry, because RULES replies run to ~5 KB (docs/03); about 1 700 modded populated servers ≈ 20 s and ≈ 8 MB. Results stream as `servers:mods` batches (server → mod ids plus the names seen) and end with `servers:mods-done`; `mods_index` returns the stored catalogue (mod id, `mod.cpp` name, server count) and the per-server ids at start-up; `mods_scan(force)` starts a scan by hand. UI: `filters.mod` (persisted, 0 = any) with a "Find mod…" box narrowing a select of up to 300 mods by server count in the filter bar, a status-line note while scanning and when the filtered view still has unscanned servers, and a Servers column in the Mods view whose count jumps to the filtered list (`servers.navigate`). Names are `mod.cpp` names, not Workshop titles, and are not unique, so the id is the key. First live run (21:41) targeted 23 618 servers because the selection used the INFO player count for unverified rows, which the ~20 000 R0 fakes inflate; the selection now requires a verified head-count or Steam's `hasplayers` partition and skips `inflated()` rows, bringing it back to the populated modded set | docs/03 §RULES payload, existing verify pass, live run | fixed; live scan re-checked with the next install |
| D-081 | 2026-09-21 | **Details pane tidy-up**. The 360 px column now forbids horizontal overflow (`overflow-x: hidden`, `overflow-wrap: anywhere`, `minmax(0, 1fr)` grid columns); the name clamps to two lines instead of one ellipsised line; country, map, version and password sit in a wrapping chip row; the address is a monospace line with copy and the query port; Join is a full-width primary button with the favourite star beside it; the facts grid has a fixed label column; the description clamps to three lines with more/less; the mod list shows ten rows with "Show all N"; and the session list, which printed one duration per player (121 values for a full server), is replaced by median/longest/recent-arrivals plus a five-bucket length histogram | screenshots of the previous pane | verified on screen (two-line name, chip row, aligned facts, ten-row mod list, nothing clipped) |
| D-082 | 2026-09-21 | **GeoIP table read in place.** `geoip.rs` no longer copies the embedded table into `Vec`s (1.4 MB of starts + 0.35 MB of indices on first use); it keeps `&'static [u8]` slices into the executable image and binary-searches with unaligned little-endian reads, so the table stays file-backed and shareable rather than private memory. Same three tests plus a range-boundary test; lookups are still O(log n) over 357 325 ranges | D-073, host memory sitting at the 90 MB budget (D-078) | gates green; effect measured with the next build |
| D-083 | 2026-09-21 | **Favourite alerts.** The `favourites` table gains an `alert` flag by an additive `ALTER TABLE` (checked through `PRAGMA table_info`, user data is never dropped). A background task polls each watched favourite with one A2S_INFO per minute (1.5 s timeout, one retry) and emits `favourite:alert` when a server that was full reports room (`slot`) or one that missed three polls answers again (`online`); the first observation only sets the baseline, so enabling a watch never fires immediately. The UI keeps up to five toasts in the top-right corner with Join and dismiss, flashes the taskbar entry (`requestUserAttention`), and the Favourites view shows a bell per row plus a "N watched" count. No Windows toast plugin yet: taskbar flash and in-app toast are enough while the launcher is open, and adding `tauri-plugin-notification` would need an AppUserModelId setup worth its own decision. UI check on 0.1.5: the bell toggles per favourite row, the header shows "1 watched", the flag persists through `favourite_alert_set`; a state change of a watched server has not been observed yet | docs/03 INFO cost, existing wait-for-slot polling (D-074), UI run | UI verified; transition alert pending a real event |
| D-084 | 2026-09-21 | **Server store outlives the Servers view.** Found while checking 0.1.5: the status line kept saying "verifying player counts…" although the cache showed the pass finished at ~21:56 (2 964 verified, 16 inflated, 5 offline, 5 unverifiable) and the mod scan had run (820 lists at 21:56:16, the rest fresh from the earlier run). `servers.start()` was called from an `$effect` in `Servers.svelte` whose cleanup called `stop()`, so switching to Favourites during the pass unregistered every `servers:*` listener and the `verify-done`/`mods-done` events were lost; the Favourites view also depended on the Servers view having been opened first. `start()` now runs once from `App.svelte` and nothing stops it. Same class of bug as D-065 (component lifetime tied to app-wide state) | cache.db query, code | fixed; gates green |
| D-085 | 2026-09-21 | **v0.1.5 published** by hand at https://github.com/NobbyBo11ocks/dayz-launcher/releases/tag/v0.1.5 from tag `v0.1.5` = commit 9345917 (installer 3 882 195 B, SHA-256 `a1db1a2e…8c43`; `verify_update_sig.js` against the live endpoint: version 0.1.5, key id matches, both signatures valid; `releases/latest` → v0.1.5). Ships D-077 idle release, D-078 Performance section, D-079 single instance, D-080 mod search, D-081 details pane, D-082 GeoIP in place, D-083 favourite alerts, D-084 store lifecycle. The tag push triggered `release.yml` again; it stops at the signing-secret check until the secrets exist | gh CLI, tools output | verified |
| D-086 | 2026-09-21 | **Windows toasts for favourite alerts** when the launcher is not the focused window (`tauri-plugin-notification` 2.4.0, capability `notification:default`; the installer's shortcuts already carry the AppUserModelId, S-61). The in-app toast and taskbar flash stay; the system toast is added only when `isFocused()` is false, and permission is requested on the first such alert | S-61 | gates green; toast appearance to be seen on a real alert |
| D-087 | 2026-09-21 | **LAN browser**: a partition with the pseudo key `lan` makes the Steam thread call `lan_server_list` instead of the master server (S-62); rows carry no Steam count, so they are never flagged by R0 and the usual PLAYER verification applies. A "LAN" button next to "Full refresh" sends `servers_refresh({partitions: [{lan: "1"}], force: true})`; results merge into the list like any partition | S-62 | gates green; no LAN server available here to observe rows |
| D-088 | 2026-09-21 | **Launch profiles**: `Settings.launch_profiles: Vec<LaunchProfile{name, profile_name, extra_args, skip_intro, no_splash, no_pause}>` saved with the launch options; the flat fields stay the current options so nothing changes for existing users. Settings → Launch has Load / Delete / "Save as profile"; the join dialog lists the profiles as "launch with" and passes the chosen name to `launch_game(id, password, profile)`, which substitutes that profile's five values for this launch only | settings.rs, JoinDialog | gates green |
| D-089 | 2026-09-21 | **DZSA list fallback** when Steam is unavailable: `browser/dzsa.rs` fetches the public list (S-60) with a browser User-Agent through reqwest 0.13.5 (rustls, gzip; the version the updater already pulls), converts every server into a `ServerRow` (keywords rebuilt as `battleye,no3rd,privHive,etm…,mod,HH:MM` so `DayzTags` and the filters behave as for Steam rows; `steam_empty = None`) and keeps its mod list. `servers_dzsa` upserts in batches of 500, emits `servers:batch`, stores the mod lists in one transaction per batch (`replace_server_mods_many`), closes with `servers:done {source: "dzsa"}` (new `source` field, `steam` otherwise) and a `servers:mods-done`, then runs the verification pass on populated rows. Triggered once automatically when Steam fails to initialise and no list was fetched in the last 10 minutes, and by a "Load list from DZSA" button in the Steam-unavailable banner. Never scheduled: 24 MB per fetch | S-60, D-031 (dead legacy master) | unit test for the converter; live fetch (`tests/live_dzsa.rs`): 18 315 servers converted in 1.9 s, 2 133 populated, 17 361 with mod lists, 18 314 with a country, keywords rebuilt as expected |
| D-090 | 2026-09-21 | **Code-signing guide** written as docs/12 (options with rough 2026 prices, Tauri's `bundle.windows` keys or `signCommand` for Azure Trusted Signing, workflow wiring with a PFX secret or OIDC login, `Get-AuthenticodeSignature` verification, a checklist). Nothing is wired: there is no certificate yet (Q11). Recommendation recorded: Azure Trusted Signing if identity validation is possible, otherwise an OV certificate | S-63, Tauri Windows signing guide | written |
| D-091 | 2026-09-21 | **Slim, unlabelled title bar**: the "DayZ Launcher" text is gone, the bar is 30 px instead of 40 (`grid-template-rows` in app.css follows), the window buttons are 40 px wide, and the only content is an update notice ("Update x.y.z available in Settings") when one is pending | user screenshot, TitleBar.svelte | built; checked on screen |
| D-092 | 2026-09-21 | **Friends tab**: `Cmd::Friends` on the Steam thread reads `friends().get_friends(FriendFlags::IMMEDIATE)` and, per friend, `name()`, `state()` and `game_played()`; a friend is "in DayZ" when `GameId::app_id()` (low 24 bits) is 221100, and the server is reported when the game address is set (S-64). `friends_list` returns `{steamId (string), name, state, inDayz, server?{ip, gamePort, queryPort}}` sorted in-DayZ, then online, then name. The tab polls every 30 s while open (Steam answers from its local cache), hides offline friends by default, resolves the server against the row cache by `ip:queryPort` and otherwise joins through direct connect on `ip:gamePort`. A friends lookup counts as activity for the idle release (D-077) and re-opens a released session | S-64, `tests/live_friends.rs` | live: 33 friends answered in 3 ms, worker and raw API agree; the only friend in DayZ at the time (state Snooze) had `0.0.0.0:0` from Steam, so the tab showed "in the menu, or on a server Steam does not report" and no Join; whether DayZ ever announces the server to Steam is unconfirmed (Q21) |
| D-093 | 2026-09-21 | **Dangling-junction cleanup**: `workshop::remove_dangling` re-reads the `!Workshop` inventory and, for entries whose target folder is gone, deletes the reparse point (`junction::delete`) and then the empty directory; live junctions are never touched whoever created them. Exposed as `junctions_remove_dangling` and a "Remove N dangling" button in Diagnostics with an inline Yes/No confirmation and a rescan afterwards. Chosen over an automatic "clean on scan" switch because a junction whose target sits on a temporarily missing drive would look dangling too; the dev notes' junction rule now names this user-confirmed action as its one exception. Unit test creates two real NTFS junctions in a temp folder, deletes one target, and checks that only that junction is removed and the live target's files are intact | Windows `RemoveDirectory` semantics on junctions (test), workshop.rs | 59 tests green |
| D-094 | 2026-09-21 | **LAN tab and no-scroll layouts**. LAN: its own section (rows whose address is private, loopback or link-local, `isLanIp`), a "Scan LAN" button that runs the D-087 partition, `RefreshDone.source = "lan"` when every partition of a refresh was the LAN one; the LAN button left the Servers bar; a LAN scan no longer flags "verifying…" (LAN rows carry no Steam count, so no pass follows; visible rows are checked on demand). Layouts: `.content.padded` is now `overflow: hidden` and a flex column, every padded view is a column that fits the viewport, and only a table (Recent, Mods, Friends: sticky header) or a column (Diagnostics: two columns 2:3; Settings: two equal columns) scrolls when the window is smaller than its content. Favourites and LAN show the details pane only when the selected row is in their list | app.css, views | svelte-check 0 errors; every view checked on screen, maximised on a 2560×1440 display |
| D-095 | 2026-09-22 | **v0.1.6 published** by hand at https://github.com/NobbyBo11ocks/dayz-launcher/releases/tag/v0.1.6 from tag `v0.1.6` = commit b1d63e5 (installer 4 433 490 B, SHA-256 `63b64163…ca20f`; `verify_update_sig.js` against the live endpoint: version 0.1.6, key id matches, both signatures valid; `releases/latest` → v0.1.6). Ships D-086…D-094. Installed here from the built installer; first start 814 ms to the list, host 68.7 MB, total 334 MB (docs/05 §6). The tag push triggered `release.yml`, which stops at the signing-secret check until the secrets are added (`gh secret list` is empty) | gh CLI, tools output | verified |
| D-096 | 2026-09-22 | **Joining a friend's session**. Steam offers two sources for where a friend plays: the game-server address it records when the client authenticates with a server (`FriendGameInfo_t`, S-64: what Steam's own "Join Game" for DayZ uses, and what the DayZ in-game browser's friends filter builds on) and the rich-presence `connect` string (S-65). `list_friends` now takes the game-server address first and falls back to `connect` parsed by `parse_connect` (`+connect ip:port`, `-connect=ip -port=n`, bare ip, default port 2302; unit-tested), and re-requests presence for every friend in DayZ so a late value shows on the next 30 s poll; the `steamworks` dependency gains the `raw-bindings` feature for `RequestFriendRichPresence` and the key enumeration in the live test. A Join then goes through the row cache by `ip:queryPort` or direct connect on `ip:gamePort`, i.e. the normal mod check and launch. Steam's friends server list (`RequestFriendsServerList`) is not wrapped by the crate and would need the crate's private callback plumbing, so it was not added. Unverified end to end: no friend was on a server during the session (Q21 keeps the live test to run). Also: a LAN scan no longer writes `last_refresh` or prunes the cache (only a `steam` refresh does) | S-64, S-65, `tests/live_friends.rs` | gates green; live dump shows the read path; join with a real friend pending (Q21) |
| D-097 | 2026-09-22 | **v0.1.7 published** by hand at https://github.com/NobbyBo11ocks/dayz-launcher/releases/tag/v0.1.7 from tag `v0.1.7` = commit d694b0b (installer 4 433 260 B, SHA-256 `5e8b0cf8…4fed`; `verify_update_sig.js` against the live endpoint: version 0.1.7, key id matches, both signatures valid; `releases/latest` → v0.1.7). Ships D-096. Installed here; Friends tab checked on screen with the installed build. Gates: clippy clean, 60 unit tests, svelte-check 0 errors | gh CLI, tools output | verified |
| D-098 | 2026-09-22 | **Window size, position and maximised state are remembered** (roadmap candidate, and docs/06 had promised it since M4): `tauri-plugin-window-state` 2.4.1 with `StateFlags::SIZE | POSITION | MAXIMIZED` only. VISIBLE, DECORATIONS and FULLSCREEN stay out: the window is frameless by config, never fullscreen, and a state saved while hidden must not restore hidden. The plugin writes `.window-state.json` into the app config folder next to settings.json (`DEFAULT_FILENAME` and `app_config_dir()` in the crate source) | S-66 | gates green; restart check on screen |
| D-099 | 2026-09-22 | **News tab** above Servers. Source: Steam's keyless news feed for app 221100 (S-67), one request of a few kilobytes: `news.rs` fetches 60 posts with bodies truncated server-side, keeps Bohemia's `steam_community_announcements` as "official" and the press feeds Steam attaches (PC Gamer, PCGamesN, GamingOnLinux, PlayGround.ru) as press, reduces bodies to plain text (`plain_text`: BBCode, HTML tags and entities removed, 320 characters), flags game-update posts by title (`is_update_title`: update, hotfix, patch, experimental, stable, release) and links official posts to `store.steampowered.com/news/app/221100/view/<gid>`. `news_fetch` stores the list in the cache's meta table; `news_cached` paints it at start. Frontend store: cached list, fetch at start and every 30 minutes, "Official only" (default on) and "Updates only" filters, `newsSeen` in settings.json; the rail shows an unread badge (first run marks the newest five as unread), opening the tab clears it, and an official update post arriving after the last look raises an in-app toast plus a Windows notification when unfocused. Links open through `tauri-plugin-opener` (S-68), which the Workshop and credits links now use too | S-67, S-68 | unit tests for the text reducer, update titles and conversion; live feed fetched |
| D-100 | 2026-09-22 | **News is the landing page**. Design: a welcome band (accent radial glow, the Steam avatar in an accent ring, "Good evening, <persona>", chips for the local DayZ version, servers with players from the last refresh, friends in DayZ, favourites and a pending launcher update; "Join again · <last server>" and "Browse servers" buttons), then the newest post featured with its picture beside the text, then the latest 24 posts as cards with 16:9 thumbnails, "Update"/"New" pills, three-line summaries and hover lift. Embeds are thumbnails, not players: a post's first picture from Steam's clan-image CDN, or the YouTube preview's thumbnail with a play badge that opens the video in the browser (an embedded player would add a renderer process against the 330 MB budget). Posts are fetched in full (`maxlength=0`) so pictures and previews are found; images load lazily. The avatar is Steam's medium (64×64) image as raw RGBA over IPC, drawn on a canvas into a PNG data URL: no image codec added, and the CSP already allows `data:`. CSP `img-src` gains `clan.akamai.steamstatic.com`, `clan.fastly.steamstatic.com` and `i.ytimg.com` (S-69). The "New" boundary is frozen per visit (`visitSeen`) so pills do not vanish the moment the tab opens **Superseded in part by D-150** (embeds do play in the app now) and by D-111 (the two Steam image hosts left the CSP when thumbnailing moved to the host). | S-67, S-69, steamworks `Friend::medium_avatar` | unit test for picture/video extraction; on-screen check |
| D-101 | 2026-09-22 | **Home page shows the latest updates by default**: a segmented control Updates · All news · With press replaces the two checkboxes; `updates` (official posts whose title reads like a game update) is the default, the other two widen the list. First on-screen run of D-100 found the featured card collapsed to zero height (a flex child with `overflow: hidden` shrinks; fixed with `flex: none` on the list's children) and a missing space in the status line. Because the home page is now the first frame, `perf_first_paint` is marked from it (the Servers view still marks too; the backend keeps the first), and the Diagnostics label reads "to the first frame"; the Servers-only mark had shown 28 s when Servers was opened late. Favourites' empty-state text gets the same 520 px measure as LAN's | user screenshots 2026-09-22 00:41 | svelte-check 0 errors; rebuilt |
| D-102 | 2026-09-22 | **v0.1.8 published** by hand at https://github.com/NobbyBo11ocks/dayz-launcher/releases/tag/v0.1.8 from tag `v0.1.8` = commit f7d42cc (installer 4 524 390 B, SHA-256 `3bfc366f…9d91`; `verify_update_sig.js` against the live endpoint: version 0.1.8, key id matches, both signatures valid; `releases/latest` → v0.1.8). Ships D-098…D-101. Installed here; home page checked on screen (avatar, persona, chips, featured card, picture cards). Bundle: 178.2 kB JS / 38.4 kB CSS raw (57.7 / 7.0 kB gzip). Gates: clippy clean, 64 unit tests, svelte-check 0 errors | gh CLI, tools output | verified |
| D-103 | 2026-09-22 | **Counts move to the title bar** (the DayZ version chip dropped, "servers with players" and "friends in DayZ" moved to the title bar, the text next to Refresh dropped and Refresh put at the right). The title bar gets an accent-coloured `status` prop (in the theme colour) (App.svelte builds it from the last refresh and `servers.friendsInDayz`); the band keeps avatar, greeting and buttons, with chips only for favourites and a pending launcher update; Refresh sits at the right of the news bar with the "checked at / every 30 min" text in its tooltip. The friend count is polled every minute by the servers store, skipped while the Steam session is released, and on the Steam thread the friends and avatar reads no longer reset the idle timer (a session re-opened for any command still does), so the D-077 idle release keeps working with the poll in place. Also from the on-screen check: a card rendered blank because the post's first picture is Steam's transparent spacer GIF, so `first_image` now skips GIFs and takes the next picture, and a picture that fails to load drops the card back to text | user screenshot 2026-09-22 00:5x, sdk.rs, S-69 | gates green; on-screen check |
| D-104 | 2026-09-22 | **v0.1.9 published** by hand at https://github.com/NobbyBo11ocks/dayz-launcher/releases/tag/v0.1.9 from tag `v0.1.9` = commit 91b003d (installer 4 522 358 B, SHA-256 `e7be723e…a001`; `verify_update_sig.js` against the live endpoint: version 0.1.9, key id matches, both signatures valid). Ships D-103. Installed here and checked on screen | gh CLI, tools output | verified |
| D-105 | 2026-09-22 | **Title-bar server count is instant**. It came from the completed refresh, so it appeared after ~40 s. It is now `servers.populatedCount`, a derived count over the row cache (trusted head-count above zero, untrusted rows excluded): present within the first second from the cached list and climbing live as refresh and verification batches land. One pass over ~30 000 rows per batch, about a millisecond | review 2026-09-22 01:05 | svelte-check 0 errors; on-screen check |
| D-106 | 2026-09-22 | **Icons in the title bar**: the title bar takes `servers` and `friends` numbers instead of a text and draws a 13 px inline globe and person (stroke `currentColor`, so they follow the accent) before each count; tooltips explain what is counted. No icon font or sprite, two tiny SVG paths; the two counts sit 32 px apart | review 2026-09-22 01:08 | svelte-check 0 errors; on-screen check |
| D-107 | 2026-09-22 | **v0.1.10 published** by hand at https://github.com/NobbyBo11ocks/dayz-launcher/releases/tag/v0.1.10 from tag `v0.1.10` = commit 7ec3fe1 (installer 4 526 029 B, SHA-256 `c93c9e0d…e4b2`; `verify_update_sig.js` against the live endpoint: version 0.1.10, key id matches, both signatures valid). Ships D-105, D-106. Installed here; the title bar showed "4,478 servers with players · 1 friend in DayZ" with icons three seconds after launch, from the cache | gh CLI, tools output | verified |
| D-108 | 2026-09-22 | **Servers page header reorganised** (four rows were wrapping). Three rows with one visual language: a primary row (search with a magnifier and the "/" hint inside it, perspective and mods as segmented controls, map and country selects, a "Filters" fold toggle with a count of the quick filters in effect, and Refresh / Full at the right), a fold-away row (specific mod, the seven quick toggles with a dot when engaged, Ping ≤ as one bordered control, "Hide inflated" with its count, Reset, and Direct connect at the right), and a thin 11.5 px status line with thousands separators. Every control is 28 px high with the same radius and border; the accent is reserved for engaged states and the primary button. `FilterBar` renders either part through a `part` prop so the store stays the single source; the fold state is a per-machine localStorage convenience, open by default. First on-screen run wrapped Refresh and Direct connect onto their own lines at the remembered 970 px window: the filter block now wraps inside itself and the buttons stay pinned to the first line. Second pass, after a screenshot of the result: the fold is gone, everything sits in two rows plus the status line; the second row uses 26 px chips (11.5 px text) under the 28 px primary controls; Direct connect is a small target-icon button beside Refresh that opens a popover with the address field (Escape closes it); the specific-mod controls moved to the end of the chip row; the header block sits on the elevated background | user screenshots 2026-09-22 01:15 and 01:2x | svelte-check 0 errors; on-screen check |
| D-109 | 2026-09-22 | **Discord via the Friends tab**. What Discord allows a third-party app (S-70): no friends list, but Rich Presence through the running Discord client, with a party and a join secret that put "Join" / "Ask to Join" on the user's profile, and an `ACTIVITY_JOIN` event carrying the secret to the joiner's launcher. Design: a `discord` module on a thread with `discord-presence` 3.2.0 (S-71); "Connect Discord" in the Friends tab opens the pipe, the Ready user (name, avatar) is shown as the login; while DayZ runs the presence is "Playing on <server>" with party size = players/max and `secrets.join` = `ip:gamePort` (only for servers the join dialog can reach; cleared on exit); an incoming join request is accepted automatically (the server is public anyway); an incoming `ACTIVITY_JOIN` runs the normal join flow (direct connect on the secret, then the mod check and launch). A `discord-<appid>://` protocol handler is registered in HKCU so Discord can start the launcher when it is not running, and the URL is handed to the running instance by the single-instance plugin. Needs a Discord Application ID from the developer portal, stored in Settings; empty means off. Presence updates go through the crate's rate limiter. **Withdrawn on 2026-09-22 01:3x before any Rust code existed; the research (S-70, S-71) stays for a later decision** | S-70, S-71 | withdrawn |
| D-110 | 2026-09-22 | **Stability pass**. Checked: CI green on every push to main (the Release workflow's failure is the expected signing-secret stop); `SCHEMA_VERSION` has been "4" since the first commit, so no schema drop explains Q22; no code path deletes history rows and `prune` is age-based only. Insurance added: `history_add` and `favourite_set` now run `PRAGMA wal_checkpoint(PASSIVE)` so a join or favourite reaches the main database file at once instead of living in the write-ahead log until the next automatic checkpoint. Live checks on the installed 0.1.11: every tab on screen, memory sampled for seven minutes (results below) | git log, gh run list, cache.rs, memwatch | see D-111 |
| D-111 | 2026-09-22 | **News pictures become 640 px thumbnails made by the host** after the stability pass measured the 0.1.11 home page at 348–372 MB total (WebView 254–279 MB, first frame 424 ms, host 82–119 MB) against the 330 MB budget: every card was decoding a 3840×2160 original (S-72). `news::thumbnail` fetches a post's picture once (Steam's two clan-image hosts only, ≤ 16 MB), shrinks it in a blocking task with the `image` crate (jpeg+png only) to ≤ 640 px, writes `news-thumbs/<gid>.jpg` under the app's local data and returns the bytes as a raw `tauri::ipc::Response`; the store turns them into blob URLs (CSP `img-src` now `'self' data: blob:` plus `i.ytimg.com`, the clan hosts are gone). Cards with a video use YouTube's 320×180 preview and never fetch the original; the featured card and video-less posts use the thumbnail. `news_fetch` prunes thumbnails of posts that left the list. Unit test: a 1920×1080 PNG comes back as a 640×360 JPEG; host allowlist checked. Also published as v0.1.11 first: compact header + checkpoints (installer 4 524 917 B, SHA-256 `be7232fc…590e`, `verify_update_sig.js` valid, commit 7d4e11b) | memwatch 01:35–01:37, S-72 | gates green (65 tests); re-measured in D-113 |
| D-112 | 2026-09-22 | **Start-up race fixed.** First start of 0.1.12 after a cold boot (07:25, Steam not yet running) showed the Welcome overlay although settings.json says onboarded; the second start was normal. Cause (S-73): Tauri creates the config windows before the setup hook, and `uiPrefs` issues `settings_get` at script load, so on a slow start the call reached a state that was not managed yet, failed, and the store fell back to defaults; `SettingsStore::load` and the frontend both treated "could not read" as "nothing there". This also explains the settings.json recreated at 01:19:54 with fresh defaults for everything but what the app wrote next. Fix: the main window is `create: false` in tauri.conf.json and built at the end of setup once the state is managed; `uiPrefs` retries the read up to 8 × 250 ms and records `readOk`; the overlay opens only when the file was read and says not onboarded. Also: install from PowerShell (`Start-Process … -ArgumentList '/S'`), the Git Bash form hung once with an idle hidden installer window | S-73, settings.json timestamps | gates green; cold-start check on screen |
| D-113 | 2026-09-22 | **Memory after the thumbnails (0.1.12, external samples, 970 px window)**: 305 MB total at 48 s (host 82.2, WebView 222.9), 297 MB at 108 s (78.9 / 217.9), 235 MB at 168 s (69.8 / 165.0, the renderer had dropped its image cache); the first instance of the morning, started before Steam and showing the overlay, read 337 → 326 MB. Against 0.1.11's 348–372 MB (WebView 254–279 MB) the pictures cost about 60 MB less at their peak and nothing once idle; the 330 MB budget holds again. Six thumbnails on disk total 330 KB. No crash record in the event log or WER for the session (the launcher was closed by hand at 07:30) | memwatch 07:26–07:30 | measured |
| D-114 | 2026-09-22 | **v0.1.12 published** by hand at https://github.com/NobbyBo11ocks/dayz-launcher/releases/tag/v0.1.12 from tag `v0.1.12` = commit 63b6a64 (installer 4 675 803 B, SHA-256 `4c0c4ca5…2a51`; `verify_update_sig.js` against the live endpoint: version 0.1.12, key id matches, both signatures valid). Ships D-110…D-113. Installed here from PowerShell; cold start on screen: no overlay, persona and counts present, window restored by the plugin although the window is now built in setup, thumbnails on every card. Installer +150 KB for the `image` crate | gh CLI, tools output | verified |
| D-115 | 2026-09-22 | **Small polish round** from a review of screenshots (01:4x) and Q22: (1) the Friends tab shows each friend's 32 px Steam avatar (`Cmd::FriendAvatar` → `Friend::small_avatar`, raw RGBA over IPC, drawn once per friend; a miss is retried once after 4 s; presence reads stay outside the idle timer); (2) Diagnostics gets a "Cache" section with row counts (servers, mod lists, favourites, history, population samples) and the database and write-ahead-log sizes, also written into the exported report, so a loss like Q22 is visible without tools; (3) the empty Mods view explains that joining a modded server installs mods and offers "Browse servers". The canvas-to-PNG helper moved to `src/lib/avatar.ts` and serves both avatars | user screenshots, Q22 | gates green; on-screen check |
| D-116 | 2026-09-22 | **v0.1.13 published** by hand at https://github.com/NobbyBo11ocks/dayz-launcher/releases/tag/v0.1.13 from tag `v0.1.13` = commit 7c83432 (installer 4 683 950 B, SHA-256 `2b7dc658…11a0`; `verify_update_sig.js` against the live endpoint: version 0.1.13, key id matches, both signatures valid). Ships D-115. Installed here; Diagnostics, opened during the check, read: Cache reads 11 204 servers, 2 089 mod lists, 0 favourites, 0 joins, 11 170 population samples, 11.2 MB + 4.2 MB WAL; first frame 507 ms, host 81.5 MB, total 321 MB. Friend avatars not yet seen on screen (the app was in use) | gh CLI, tools output, screen | verified |
| D-117 | 2026-09-22 | **Q21 resolved: DayZ does announce the server to Steam.** At 07:48 the Friends tab (opened by hand) listed a friend "In DayZ" with the server resolved from the row cache by `ip:queryPort` ("DayZERO Namalsk Hardcore 1PP \| Vanilla+ \| Adventure · namalsk · 12/45") and a Join button, so `FriendGameInfo_t` carries the game address once a player is on a server; the earlier `0.0.0.0:0` readings were friends idling in the menu. The rich-presence `connect` fallback (D-096) stays as a second route. Avatars (D-115) confirmed on screen in the same view. What remains untested is pressing Join, which runs the ordinary direct-connect and mod-check flow (D-053, D-051) | screen 2026-09-22 07:48 | verified |
| D-118 | 2026-09-22 | **Join failed with "no Steam instance running": stale `ActiveProcess\pid`.** Join was pressed on the friend's server at 07:50; DayZ started with the right 12 mods and `-connect`, and died after one second with steam_api's "Unable to locate a running instance of Steam" (S-74). `HKCU\Software\Valve\Steam\ActiveProcess\pid` read 16884 while Steam ran as 2000; the same staleness was on record since D-026. The game's older `steam_api64.dll` follows that pid; the launcher's newer one does not, which is why the launcher itself was connected. A first build wrote the live pid into that value before spawning the game; on the retry at 08:02 the game started, loaded the map and stayed up, but the value still read 16884 afterwards, so the write had not taken effect and cannot be what made the difference. The write was removed again: the launcher only warns in the join plan when the value disagrees with the live process, with the advice to quit and restart Steam if the game reports it cannot find Steam. Why the 07:50 start failed and the 08:02 start succeeded is not established (Q23) | S-74, RPT and minidump of 07:50, RPT of 08:02 | join verified on the retry; cause open |
| D-119 | 2026-09-22 | **Priority and elevation policy**. Priority: `SetPriorityClass(HIGH_PRIORITY_CLASS)` at start; `BELOW_NORMAL` from the moment DayZ is spawned until it exits, then high again, so the launcher never competes with the game for cores. Elevation: not forced. DayZ inherits the launcher's integrity level and its steam_api must match the Steam client's, so an always-elevated launcher on a PC where Steam runs normally would reproduce exactly the D-118 error; instead the launcher matches Steam at start (`proc::elevation_state`: token elevation of both processes via `OpenProcessToken`/`GetTokenInformation(TokenElevation)`): Steam elevated and launcher not → restart elevated through `ShellExecuteW("runas")` (UAC prompt) and exit; launcher elevated and Steam not → a join-plan warning to start the launcher normally. On this machine everything runs elevated (built-in Administrator), so the state is "matched" and nothing changes **See D-151**: the ordering was inverted in the implementation until then, so DayZ actually ran *below* normal. | S-74, windows-sys `Win32_Security`/`Win32_UI_Shell` | gates green; behaviour on a UAC-enabled PC untested (no such machine here) |
| D-120 | 2026-09-22 | **v0.1.14 published** by hand at https://github.com/NobbyBo11ocks/dayz-launcher/releases/tag/v0.1.14 from tag `v0.1.14` = commit 5101525 (installer 4 683 972 B, SHA-256 `60827c25…7bdb`; `verify_update_sig.js` against the live endpoint: version 0.1.14, key id matches, both signatures valid). Ships D-118 (warning only) and D-119. Installed here while no game ran; the Mods tab was open (12 items, 5.91 GB, every junction present with server counts) right after | gh CLI, tools output, screen | verified |
| D-121 | 2026-09-22 | **"Join again" is a short accent button** (the secondary button carried the whole server name and clipped at "Vanill…"). It now reads "Join again" in the same accent style as "Browse servers", with the server name and address in its tooltip. The button's presence also shows a join reached the history table after the D-110 checkpoint. Also: the news bar's Refresh button is gone; the feed loads at start and every 30 minutes (D-099) | user screenshot 2026-09-22 08:2x | svelte-check 0 errors |
| D-122 | 2026-09-22 | **v0.1.15 published** by hand at https://github.com/NobbyBo11ocks/dayz-launcher/releases/tag/v0.1.15 from tag `v0.1.15` = commit 109a7ef (installer 4 685 697 B, SHA-256 `921269f8…a24a`; `verify_update_sig.js` against the live endpoint: version 0.1.15, key id matches, both signatures valid). Ships D-121. Installed here; the band shows "Join again" and "Browse servers" as two accent buttons and the news bar has no Refresh | gh CLI, tools output, screen | verified |
| D-123 | 2026-09-22 | **Stale-registry warning removed from the join plan** (seen on the dialog of a join that works). On this machine the value never matches the live process yet joins succeed (D-118, Q23), so the line only worries the user and predicts nothing. Diagnostics keeps showing "registry says pid … (stale)" for the record; the elevation-mismatch warnings (D-119) stay because they describe a real blocker | user screenshot 2026-09-22 08:2x | gates green |
| D-124 | 2026-09-22 | **v0.1.16 published** by hand at https://github.com/NobbyBo11ocks/dayz-launcher/releases/tag/v0.1.16 from tag `v0.1.16` = commit 1e694ef (installer 4 683 309 B, SHA-256 `ffb77636…7a90`; `verify_update_sig.js` against the live endpoint: version 0.1.16, key id matches, both signatures valid). Ships D-123. Installed here; the Recent tab showed both of today's joins (07:50 and 08:26), so history survives restarts and installs with the D-110 checkpoint in place | gh CLI, tools output, screen | verified |
| D-125 | 2026-09-22 | **The Steam thread keeps trying.** The first launcher instance of the morning started 30 s before Steam and stayed "Steam unavailable" until it was restarted, because `run()` gave up after one failed `SteamAPI_Init`. Now it retries every 10 s (`STEAM_RETRY`), answers commands with "Steam is not running" meanwhile (`reject`, also reused by the idle re-open path), and publishes the normal initialised status the moment a session opens, which triggers the automatic refresh; the DZSA fallback (D-089) still fills the list in the meantime. The three views' wording says Steam is not running and that the launcher connects by itself | this morning's 07:25 start, sdk.rs | gates green; live retry not exercised (Steam was running) |
| D-126 | 2026-09-22 | **v0.1.17 published** by hand at https://github.com/NobbyBo11ocks/dayz-launcher/releases/tag/v0.1.17 from tag `v0.1.17` = commit ca8ed8d (installer 4 687 756 B, SHA-256 `4d414757…f530`; `verify_update_sig.js` against the live endpoint: version 0.1.17, key id matches, both signatures valid). Ships D-125. Installed here; normal start with Steam up: persona, counts and pictures present | gh CLI, tools output, screen | verified |
| D-127 | 2026-09-22 | **"Unable to locate a running instance of Steam" explained (Q23 closed).** It happened again on "Join again" at 08:4x. Every failing game start came from a launcher instance that had been started from a developer shell (07:46 and 08:44), which belongs to a different logon session than the desktop; the instance opened the normal way (08:02) joined without trouble. Steam's client IPC is not reachable from another logon session, whatever the elevation. Rule for testing: start the installed launcher only through the desktop (Start menu, taskbar), never from a developer shell. No launcher change. Log check at 08:48: error dumps at 08:26:18 and 08:45:17 (both from shell-started instances), then `DayZ_x64_2026-09-22_08-46-35.RPT` from the desktop-started instance runs 52 KB to "Termination successfully completed" at 08:47:43 with a script log, i.e. the game loaded and was quit normally | RPT/minidump of 07:50, 08:26, 08:45; RPT of 08:46 | resolved |
| D-128 | 2026-09-22 | **Friends in the server browser** (roadmap candidate, unblocked by D-117). The minute poll that feeds the title bar now also maps each friend's server to a row id (`ip:queryPort` when Steam gives the query port, else ip + game port against the cached rows) into `servers.friendsOn`; the Servers, Favourites and LAN tables show an accent person pill with the count and the names in its tooltip, and the quick-filter row gains a "Friends" chip (`filters.friendsOnly`, counted as a non-default filter, saved with the others) that keeps only those servers | D-117, Friends poll | svelte-check 0 errors; on-screen check |
| D-129 | 2026-09-22 | **v0.1.18 published** by hand at https://github.com/NobbyBo11ocks/dayz-launcher/releases/tag/v0.1.18 from tag `v0.1.18` = commit 7b5b068 (installer 4 687 741 B, SHA-256 `51ef9f7f…0d3c`; `verify_update_sig.js` against the live endpoint: version 0.1.18, key id matches, both signatures valid). Ships D-128. Installed here and opened through the desktop; the chip read "Friends 0" because no friend was on a server at that moment (kyle had gone Away, Catman was in the menu), so the row badge awaits a friend in a session | gh CLI, tools output, screen | verified |
| D-029 | 2026-09-21 | M1 acceptance: `cargo test` 9 passed (fixtures = this machine's `libraryfolders.vdf`, `appmanifest_221100.acf` with LastOwner redacted, `appworkshop_221100.acf`, CF `meta.cpp`/`mod.cpp`); `cargo test -- --ignored live_diagnostics` reproduces docs/02 exactly: Steam `C:\Program Files (x86)\Steam` (HKCU), running pid 15176, 2 libraries, DayZ in `G:\SteamLibrary`, exe `1.29.0.163709` / game `1.29.163709`, build 24689949, StateFlags 4, 4 Workshop items with meta/mod names, 20 junctions (16 dangling), 7 ms; clippy 0 warnings; svelte-check 0 errors | test output | verified |
| D-011 | 2026-09-21 | Game port for `-port=` comes from A2S_INFO EDF 0x80, not the query port | S-42 (2402 vs 27017), S-18 `gamePort` | verified |
| D-012 | 2026-09-21 | Version check = A2S `version` vs `DayZ_x64.exe` ProductVersion (`1.29.0.163709` ↔ `1.29.163709`) | S-41, S-42 | verified |
| D-013 | 2026-09-21 | Docs are written with an editor; a multi-file Bash heredoc failed to parse in this shell (Git Bash on Windows). Not a product decision, recorded to avoid repeating it | session | noted |
| D-130 | 2026-09-22 | **"Clear list" on the Recent view**. `Cache::history_clear` (`DELETE FROM history`, then a checkpoint so the deletion is as durable as the inserts) behind a new `history_clear` command; the view shows a ghost "Clear list" button at the right of its header that turns into an inline "Remove all N joins from the list? Clear / Keep" confirmation, the same shape as the junction cleanup (D-093). It is now the only code path that deletes history rows, which matters for Q22: a future loss of rows without this action is still unexplained. The home page's "Join again" follows `history[0]`, so it disappears with the list | unit test in cache.rs | verified (cargo test) |
| D-131 | 2026-09-22 | **Twelve accents and an `--accent-fg` token**. Palette in colour-wheel order: amber, orange, red, rose, pink, violet, indigo, blue, sky, teal, green, lime; vivid tones on the dark theme, deeper tones on the light theme. Buttons used to paint `#111` on the accent, which was unreadable on the deeper light-theme teal and green; every accent surface now uses `var(--accent-fg)` (`#111` on dark, `#fff` on light except amber and lime). The accent rules moved from `:root[data-accent]` to plain `[data-accent]` so the Settings picker's dots carry their own colour through the same rules, and the picker is a row of 18 px dots with a ring on the chosen one and the name beside it, keeping the Appearance row at one line (D-094). The list is kept in three places by design (app.css tokens, `ACCENTS` in prefs.svelte.ts, `ACCENTS` in settings.rs, each cross-referenced) | docs/06 §5 | verified (svelte-check, cargo test) |
| D-132 | 2026-09-22 | **Lime is the default accent**. `UiPrefs::default` and `DEFAULT_ACCENT` in prefs.svelte.ts both read `lime`. A new default alone would never reach an existing install, whose settings.json already holds `amber`, and a saved value cannot be told apart from a deliberate choice; so `UiPrefs` gains `accentDefaultV2` (false by default) and `normalise()` runs a one-time move: when the marker is unset, `amber` becomes `lime` and the marker is set, every other accent is left alone. Amber picked deliberately afterwards survives, because the marker is then already set | unit test `old_amber_default_moves_to_lime_once` | verified (cargo test) |
| D-133 | 2026-09-22 | **News summaries no longer start with a picture URL** (seen on the 1.27 and 1.28 cards). Steam's older announcements embed pictures as `[img]{STEAM_CLAN_IMAGE}/…jpg[/img]`, with the address as the tag's *content*; `plain_text` stripped the tags but kept that content, so the card read "{STEAM_CLAN_IMAGE}/4458811/1ff7…png Greetings, Survivors!". The stripper now recognises `[img]` and drops everything up to `[/img]`; an unclosed one keeps its text as before. The newer posts use `[img src="…"][/img]`, whose content is empty, which is why only the older ones showed it. `first_image` already handled both forms, so the pictures themselves were never affected | Steam feed pulled live (posts 1789039014336026, 1796631172407063, 1800991756473565, 1792116353358040); unit test | verified (cargo test) |
| D-134 | 2026-09-22 | **README rewritten**. Centred hero with the app icon, five shields.io badges (release in the new lime, CI, downloads, platform, licence), a download link and a section index, then "Why this launcher" with a measurements table, a two-by-two highlights table, both screenshots, a mermaid flow of list → verify → join, install with the SmartScreen note, privacy, build from source with the CI gates, the release procedure (Actions and by hand, unchanged), a repository map, a tools table and credits with the Bohemia/Valve disclaimer. An MIT `LICENSE` file was added to match `package.json`, which claimed MIT with no licence text. Screenshots retaken from the running 0.1.19 build with `PrintWindow(PW_RENDERFULLCONTENT)`, because a screen grab caught another window on top; scaled to 1450 px (home as JPEG 214 KB, servers as PNG 473 KB) **Superseded by D-154**: the licence is all-rights-reserved. | GitHub renders the README | verified (published) |
| D-135 | 2026-09-22 | **v0.1.19 published** by hand at https://github.com/NobbyBo11ocks/dayz-launcher/releases/tag/v0.1.19 from tag `v0.1.19` = commit 803326f (installer 4 687 251 B, SHA-256 `d787e747…0eff0`; `verify_update_sig.js` against the live endpoint: version 0.1.19, key id `b809d4b505f6e54d` matches, both signatures valid). Ships D-130–D-134. Installed here and opened from the desktop: the accent moved from amber to lime by itself, the twelve dots show in Settings, Recent shows "Clear list" and its confirmation (cancelled, the two recorded joins were kept), and the 1.27/1.28 cards now open with the post's first line instead of a picture address | gh CLI, tools output, screen | verified |
| D-136 | 2026-09-22 | **v0.1.19 budgets measured** (the release gate in [05 §6](05-architecture-and-optimisation.md), run after the release rather than before it). Installer 4.69 MB, exe 13.2 MB. Cold start to first frame **427 ms** (budget 1000). Host memory **73 MB flat from 80 s to 12 min** (94 MB at 40 s during the start-up refresh, budget 90 at idle). Whole app **294–316 MB** over a 12-minute idle run at 20 s samples, one transient sample at 347 MB (budget 330). Idle CPU with the window focused **0.38 % of one core** (browser 0.16, host 0.14, renderer 0.05, gpu 0.02), 0.02 % unfocused, 0.2 % minimised (budget 0.5). Bundles 58.5 KB JS + 7.7 KB CSS gzip. Refresh: 2 379 servers from Steam in 36 s, 2 365 verified, 10 fake, 4 offline. **Two findings.** (1) Measuring CPU while taking screenshots reads 1–2 % instead of 0.4 %, because capturing the window forces redraws; the first two readings today were of the measurement, not the app, and the budget row now says so. (2) Raw CSS is 43.8 kB against the old 40 kB line; gzip is 7.7 kB against its 8 kB budget. Deduplicating every identical rule block across components would recover a measured 4.7 kB, so the gzip figure becomes the binding budget and the dedup is recorded as the lever to pull if it is ever threatened, rather than refactoring eleven components for bytes the wire never sees ([05 §7](05-architecture-and-optimisation.md)) | measurement scripts in a scratch folder; Diagnostics → Performance | verified |
| D-138 | 2026-09-22 | **Accent migration runs in memory, not on disk** (nuance of D-132, found while checking `settings.json`). `SettingsStore::load` normalises the prefs it read but does not write them back, and the frontend only patches the file when a value actually changes, so an install whose file still says `amber` gets lime applied on every start while the file keeps the old value until something else writes. The outcome the user sees is identical, and a deliberate pick of amber afterwards is written together with the `accentDefaultV2` marker and then sticks. Left as is: persisting at load would mean a write on every start for a cosmetic difference, and the behaviour is correct either way. If it is ever worth making the file self-describing, save in `load()` only when `normalise()` changed something | `%APPDATA%\com.dayzlauncher.desktop\settings.json` read live | noted |
| D-137 | 2026-09-22 | **Q10 closed: DayZ servers answer RULES.** New tool `tools/rules_failure_rate.js` samples populated servers from the launcher's own cache (read-only through `node:sqlite`), queries INFO then RULES at the app's pacing and reports the failure rate by reason. Two runs, 150 and 300 addresses: 447 of 450 answered INFO (3 offline or filtered) and every one of those 447 answered RULES, with no timeouts and no errors; median RTT 28 ms for both, median 10 mods per server and a maximum of 99. So the official launcher's "Server failed to respond to rules request" is a dead or filtered host, not a server that refuses RULES, and the mod list needs no fallback. The DZSA list stays as the fallback for the *server list*, which is a different failure (Steam unavailable, D-089) | S-75 | verified |
| D-139 | 2026-09-22 | **Live paths re-verified on v0.1.19** after the day's changes, with the app running alongside (a second Steamworks client for 221100 coexists with the launcher's session, as Q4 found). `live_friends`: 33 friends, 3 in a game, the worker answered in 2.9 ms; the friend in DayZ read `0.0.0.0:0` because he was in the menu, so `server: None` and no Join, which is the intended behaviour. `live_m6`: both official favourites read from `FavouriteServers.xml` (one live in 146 ms, one offline), direct connect resolved 27016 → game port 2302 and 27017 → 2402 on one host, population samples round-tripped. Live Steam diagnostics: full inventory in 9 ms, no warnings, and all Workshop junctions now report `targetExists` (mods were re-subscribed after the 2026-09-21 clear-out). `live_dzsa`: 18 274 servers in 2.3 s, 1 813 populated. Not run: `live_join` and `live_m8` (they start DayZ and change the user's Workshop subscriptions) and `live_sweep` (thousands of addresses, and two 150–300 address samples had just run for D-137) | cargo test output | verified |
| D-140 | 2026-09-22 | **Four stale questions closed with evidence.** Q9 resolved and not merely guarded: the mod id-length byte was 1 in four of 3 213 live entries (4 in the rest), so `width = idLen & 0x0F` is load-bearing and the rules parser stays as it is. Q7 closed: the heaviest cached mod list, 121 mods, spells out to ≈ 8 100 characters of `-mod=`, a quarter of the 32 767 limit, so relative `!Workshop\@…` paths are unnecessary. Q8 closed as won't-fix: mods are matched by Workshop ID only, so the per-mod hash and flag bytes are never read. Q2 closed as not applicable: the launcher always starts `DayZ_BE.exe` with the official argument form (D-010), which is what starts BattlEye; the behaviour of `DayZ_x64.exe` on its own only matters for a path we deliberately do not take. Open questions are now only Q11 (code signing, still to decide), Q18 (idle-growth monitoring, flat on v0.1.19), Q20 (WebView storage, no recurrence) and Q22 (one loss of rows, mitigations shipped) | S-76, S-45, D-010 | verified |
| D-141 | 2026-09-22 | **One Refresh button on the Servers page**. The "Full" button is gone and Refresh now sends the whole partition list. That costs nothing in perceived speed, because `full_partitions()` already starts with the `hasplayers` partition: the populated servers still land in about 40 s and the empty ones keep arriving behind them for a few minutes, with the header counting rows as they come. The automatic refresh on start is unchanged and still asks only for populated servers, so a launch never triggers the long pass | `full_partitions()` order, D-046 | verified on screen |
| D-142 | 2026-09-22 | **Mods view becomes a mod manager**. On top of the per-item update and unsubscribe from D-075 it now has a search box (name, `meta.cpp` name or Workshop ID), four filters with live counts (All, Updates, On no server, No junction), tick boxes with a select-all for the current filter, and bulk **Update N** and **Unsubscribe** over the selection with one inline confirmation instead of one per mod. Rows gained a Folder button that reveals the mod directory through the opener plugin (`revealItemInDir`, already covered by `opener:default`), a per-row Update when that mod is stale, a sortable Servers column, and a junction cell that distinguishes a healthy junction from one whose target is gone. Bulk actions reuse the existing commands, which already took lists: `mods_sync(job, ids)` and `mods_unsubscribe(ids)`. No Rust change | existing command signatures | verified on screen |
| D-143 | 2026-09-22 | **Visual pass over every page**, with the window maximised at 1936×1056 and captured through `PrintWindow`. Three things were worth fixing. (1) **Mod names were raw file values**: `mod.cpp` lines like `name = "Uncuepa's Civilian Clothing"; // name` kept their quotes and trailing comment, because the scanner only stripped a `;` at the very end of the line. A quoted value now ends at its closing quote and a bare one at a line comment, so the value is the string the author wrote; unit-tested with the real lines from this machine. Items whose `mod.cpp` name is a localisation key (`$STR_nam_mod_terrain_name`) now display the Workshop's own name instead. (2) **Recent's empty state** was a single grey line at the top left while LAN and Favourites centre theirs; it now matches, with a Browse servers button. (3) **The News hero could fill the window** on a large screen: it is capped at 320 px with the summary clamped to five lines, so more cards stay in view. Left alone deliberately: the wide gap in the server table at very large widths (the details pane fills it when a row is selected) and the dash in the friends' Server column for friends who are in another game | on-screen review of all nine views | verified |
| D-144 | 2026-09-22 | **v0.1.20 published** by hand at https://github.com/NobbyBo11ocks/dayz-launcher/releases/tag/v0.1.20 from tag `v0.1.20` = commit c676a87 (installer 4 692 177 B, SHA-256 `e4f397f9…88e0`; `verify_update_sig.js` against the live endpoint: version 0.1.20, key id matches, both signatures valid). Ships D-141–D-143. Installed here and opened from the desktop: the Servers header carries one Refresh, the Mods page lists all 12 items with readable names (`Namalsk Island`, `WindstrideClothing`, `Uncuepa's Civilian Clothing` where the raw file values used to show), filters and bulk actions, Recent shows its centred empty state and the news hero is capped with a second row of cards in view | gh CLI, tools output, screen | verified |
| D-145 | 2026-09-22 | **Release workflow needs one secret, not two** (and the command carries `--repo`, because `gh` otherwise reads the repository from the current folder and fails outside a checkout, which is how the second attempt failed from `C:Windowssystem32`), and the README's commands were Bash-only. Run in PowerShell, both failed: `<` is not redirection there ("The '<' operator is reserved for future use") and an empty `""` argument is dropped before the process sees it, so `--body` had no value. Verified by printing `process.argv` from PowerShell: `--body ""` arrives as a bare `--body`, and `--%` swallows the rest of the line instead of producing an empty argument. The password secret turns out to be unnecessary anyway: the key has no password, the build passes `--ci` so the CLI never prompts, and a missing secret resolves to an empty string in the `env:` block. README now gives a PowerShell form (`(Get-Content … -Raw).Trim() \| gh secret set …`) beside the Bash one and says the password secret is not needed; the workflow header says the same. The private key itself is never read or uploaded by any tool | user's terminal output; `process.argv` test; release.yml | verified |
| D-146 | 2026-09-22 | **Mods column in the server browser** (the gap named as the last one worth closing). The count comes from the mod-scan index the store already loads (`modsByServer`, D-080), so no backend or protocol change: the table takes it as a prop the way it takes the friends marker (D-128), and Servers, Favourites and LAN all pass it. The column sits between Map and Players, is sortable (descending first, like Players) and puts unscanned servers below every scanned one in both directions. A dash means "not scanned yet", which is deliberately different from `0` ("vanilla"), and the tooltip says which of the two it is; the grid gained a 52 px column for it | D-080, D-128 | verified on screen |
| D-147 | 2026-09-22 | **First automated release exposed two workflow defects** (v0.1.21, the first published by the tag-push workflow after the signing secret was set). (1) **`tauri-action` writes an `api.github.com` asset URL into `latest.json`**, which serves JSON metadata rather than the installer unless the caller sends an `Accept: application/octet-stream` header; `verify_update_sig.js` fetched 1 716 bytes and reported the file signature INVALID, i.e. auto-update would have been broken for every user. Our own `make_latest.js` takes the browser download URL from the release listing, so the workflow now regenerates and re-uploads the manifest after publishing, before the verification step. The published v0.1.21 manifest was corrected by hand the same way (the runner's `.sig` was downloaded first, because the runner's installer differs byte-for-byte from a local build). GitHub's `releases/latest/download/…` redirect serves a cached copy for a few minutes, which is why the verify step already retries six times. (2) **The smoke test's uninstall check was a race**: NSIS copies itself to `%TEMP%` and keeps deleting after the parent process returns, so a 2 s sleep then "install folder still exists" failed the run even though the release had published correctly. It now polls for up to 45 s and only fails if an app binary is still there | workflow logs, `gh release view --json assets`, tools output | verified |
| D-148 | 2026-09-22 | **v0.1.21 published by the workflow**, the first release built and uploaded from a tag push rather than by hand, at https://github.com/NobbyBo11ocks/dayz-launcher/releases/tag/v0.1.21 (tag = commit 496eb16, installer 4 693 279 B built on the runner, SHA-256 `63d340ea…e1e0`). Ships D-146. The manifest needed the hand correction in D-147 before `verify_update_sig.js` passed; after it, the live endpoint reports version 0.1.21, key id matches and both signatures valid. Note for future hand-fixes: the runner's installer differs byte-for-byte from a local build of the same commit, so the `.sig` must be downloaded from the release rather than taken from the local bundle directory, and GitHub's `releases/latest/download/…` alias served the old asset for about two minutes after the replacement | gh CLI, tools output | verified |
| D-149 | 2026-09-22 | ~~**The UI scales with the window instead of reflowing**~~ **(reverted the same day, D-153)**. `src/lib/state/zoom.svelte.ts` sets Chromium's `zoom` on `<html>` to `min(w/1280, h/800)` clamped to 0.5–1.75, against the default window size as the reference, on a rAF-throttled resize listener. `zoom` scales layout rather than painting a transform, so text stays crisp and hit areas follow. Media queries read the *real* viewport, so they would collapse the rail and drop the details pane while everything was already scaled down; the two breakpoints the app uses are therefore published as `data-compact-rail` and `data-no-pane` on `<html>` against the effective width (`innerWidth / zoom`) and the CSS matches on those attributes | Chromium zoom behaviour | verified on screen |
| D-150 | 2026-09-22 | **News videos play in the app**. Clicking a play badge opens a centred player over the page instead of handing the link to the browser; Escape or the backdrop closes it, and "On YouTube" still opens it externally. The iframe is created only while a video is open and destroyed on close, so an idle home page carries no embedded player and the memory budget is unchanged (the D-100 reason for avoiding embeds). Source host is `www.youtube-nocookie.com`, added to the CSP as the only `frame-src`; `img-src` already allowed `i.ytimg.com` for the preview thumbnails | D-100 | verified on screen |
| D-151 | 2026-09-22 | **Audit fixes, round one** (four parallel code audits over the Rust protocol/cache, the Rust Steam/launch/IPC layer, the frontend stores and the Svelte views, plus a visual pass at the 960×600 minimum). Fixed here, worst first. **The game inherited below-normal priority**: `launch_game` dropped the launcher to `BELOW_NORMAL_PRIORITY_CLASS` *before* spawning, and Windows gives a child the parent's class, so DayZ itself ran below normal for the whole session — the opposite of D-119's intent; the launcher now spawns first and steps aside after. **Importing official favourites could kill the process**: `parse_attrs` tested bytes with `(b as char).is_whitespace()`, which is true for 0xA0, a UTF-8 continuation byte, so a name containing "ą" or a non-breaking space could stop the scan mid-character and panic while slicing — with `panic = "abort"` that takes the whole app down; the scan is ASCII-only now and a regression test covers non-ASCII favourites. **Two ways to lock the window**: modal backdrops are `position: fixed` and painted over the frameless title bar, leaving minimise/maximise/close and the drag region dead (the title bar now sits above overlays), and the join dialog refused Escape, the backdrop and Cancel while a Workshop download ran, whose only backstop is a 45-minute timeout (only the launch itself is uninterruptible now; closing during a download just stops watching and Steam carries on). **Keyboard in the server table was doubled**: `keydown` was bound on both the grid and every row, and Svelte delegates that event, so after any click arrows skipped two rows, Enter fired twice and F toggled a favourite twice, i.e. not at all; rows no longer bind it, which is also the correct ARIA shape for `role="grid"`. **A refresh with no populated server left "verifying player counts…" on screen for ever** — the pass was skipped without emitting `servers:verify-done`, which is the only thing that clears the flag. Also: the details pane and the news hero used raw-viewport media queries that fought the new scaling (now on the effective-width attributes), the Diagnostics error replaced the whole report instead of sitting beside it, the table header sat 10 px off its columns whenever the body scrolled (`scrollbar-gutter: stable`), light-theme accent swatches in Settings rendered dark-theme colours because the overrides only matched the root, the Mods toolbar stayed enabled until the first progress event, `.shell` used `100vh` which root zoom does not scale, and two joins to one server in the same second were duplicate `{#each}` keys | audit findings, each re-read in the code before fixing | gates green (68 Rust tests, svelte-check clean) |
| D-152 | 2026-09-22 | **Browser responsiveness** (audit finding: the `list` derived filters and sorts every row on each invalidation, with no debounce and no cached collator, and `SvelteMap.values()` subscribes it to every key so each streamed batch re-ran the whole pass). Three changes, cheapest first: the search box keeps the typed value locally and writes the store 180 ms later (clearing still applies at once), so a keystroke no longer costs a full filter and sort of ~20 000 rows; one module-level `Intl.Collator` replaces `localeCompare`, which constructs a collator per call, on the name and map sorts; and sorting by mods builds the count map once per sort instead of reading the reactive map inside the comparator, turning ~n log n signal reads into n. Not done here and recorded for later: `modsByServer` still subscribes the derived to thousands of keys while the mod filter is on, the row map is never pruned of servers Steam has dropped, and the 100 ms refresh batches still invalidate the whole derived — the real fix for those is a version counter plus a plain lookup map | audit; docs/05 §6 budgets | gates green |
| D-153 | 2026-09-22 | **Proportional scaling reverted**, after a short round trip: asked for, extended to scale up as well, reverted, reinstated, then dropped. `zoom.svelte.ts` is deleted, the `<html>` attributes are gone and the three breakpoints are media queries again exactly as before (rail at 1100 px, details pane at 1000 px, news hero at 1100 px). Two improvements found while the scaling was in place are kept, because they stand on their own: the stacked news hero releases its 320 px cap, which is what stopped the title and buttons being clipped below 1100 px, and `.shell` uses `height: 100%` rather than `100vh`. The consequence of the revert is worth stating plainly: below 1000 px the details pane is hidden again, so selecting a server shows nothing and its Join button is out of reach at the 960 px minimum width — the open recommendation is to make the pane an overlay at small widths rather than hiding it | product decision | verified (gates green, scaling absent from the build) |
| D-154 | 2026-09-22 | **Licence changed from MIT to all rights reserved**. The source stays public to read, audit and build for personal use; selling, redistributing in any form, publishing a modified version, and rebranding (a different name, logo or identity, or removing the credits) all need written permission. Third-party components keep their own licences and the text says so, naming DB-IP (CC BY 4.0) and flag-icons (MIT), and the Bohemia/Valve disclaimer stays. `package.json` moves to `SEE LICENSE IN LICENSE`, and the README badge and licence section match. Consequence accepted: this is not an open-source licence, so GitHub stops recognising it and contributors have no redistribution right | product decision | applied |
| D-155 | 2026-09-22 | **Installer and uninstaller wear the app's theme**. The NSIS template already honoured `MUI_ICON`, `MUI_HEADERIMAGE_BITMAP`, `MUI_HEADERIMAGE_UNBITMAP`, `MUI_WELCOMEFINISHPAGE_BITMAP` and `MUI_UNICON`, so no template change was needed: `tauri.conf.json` now supplies `headerImage`, `sidebarImage` and `installerIcon`. The two bitmaps are generated from the app icon in the launcher's own colours (slate `#161b22`, lime `#a3e635`): a 150×57 header with the icon and wordmark over an accent rule, and a 164×314 sidebar with an accent edge, the icon, the name and a one-line description. NSIS needs plain 24-bit BMP — a 32-bit file with alpha renders as a black box — so the art is composed as PNG and drawn onto a 24bpp surface with System.Drawing before saving | Tauri NSIS config keys; our template (D-067) | verified on screen |
| D-156 | 2026-09-22 | **New app icon**. `tools/make_icon.js` was rewritten: it draws the mark as SVG and rasterises with `sharp` instead of pushing pixels by hand, producing a dark tile with a lime chevron "Z" over a faint ring, plus an accent rule. It writes a multi-size `icon.ico` (16–256, PNG-compressed entries, container written by hand — no new dependency) and the sixteen PNG sizes the bundle and Store assets use. Checked at 16, 24, 32 and 48 px, where the mark still reads | rendered previews | verified |
| D-157 | 2026-09-22 | **"Join again" removed from the home page**. The button, its handler and the history load that fed it are gone from News, so the page no longer touches the history table at all; Recent remains the one place past joins live | product decision | applied |
| D-158 | 2026-09-22 | **Diagnostic log**. `src-tauri/src/log.rs` keeps the last 400 entries in memory and appends to `<app local data>/logs/launcher.log`, rotating at 2 MB; UTC timestamps, four levels, an area tag. Instrumented: start-up and elevation, cache open with row count, Steam status changes and every refresh with per-partition counts and timings, verification summaries, launches with pid and argument length, game exit codes, cache prunes and checkpoint failures (the Q22 evidence), news fetches with post and picture counts. The frontend half (`src/lib/log.ts`) forwards uncaught errors, rejected promises, console errors, failed IPC calls and each view the user opens, tagged `ui:…`. Diagnostics gained a Log panel with a problems-only filter, copy, and a button that reveals the file. Nothing leaves the machine, and nothing on a per-row or per-frame path logs | product decision | verified on screen |
| D-159 | 2026-09-22 | **Second audit round, including three regressions from D-151.** Worst first. **The join dialog could launch the wrong server**: favourite-alert toasts sat above the modal (`z-index` 60 against the backdrop's 50) and stayed clickable, so a toast's Join changed `joiningId` while the dialog kept showing the first server's plan, password and mod list — the launch then used the new server with the old plan, and its mods were never checked. Toasts moved below the dialog and the dialog is now `{#key}`-ed on the server id, so a new target rebuilds it. **Regressions**: `scrollbar-gutter` on the table header did nothing (it only applies to scroll containers), leaving the header 10 px out in *both* states rather than one — the header takes a 10 px padding instead; the Mods toolbar's optimistic `updating` was never cleared when the backend rejected synchronously (Steam disconnected), locking the page; and the empty verify summary added in D-151 replaced a good status line with "0 verified" after a LAN scan, so it is now sent only for a Steam list refresh. **Data safety**: `settings.json` is written to a temp file and renamed, because a truncating write plus a silent default-on-parse-failure could lose every launch profile and preference; and the 30-day prune now excludes favourites, which it had been deleting, silently stopping their alerts (regression test added). **Search**: a reset within the 180 ms debounce wrote the old term back into the fresh filters, leaving the list filtered by an invisible term; the pending write is cancelled now | two parallel audits, each finding re-read in the code | gates green (70 Rust tests) |
| D-160 | 2026-09-22 | **The Steam worker now tells the truth about its own state.** `initialized` was written once and never cleared, and the refresh state machine had no deadline anywhere, so Steam quitting mid-refresh left "Refreshing…" and "verifying player counts…" on screen until the app was restarted: the completion callback never came, `refreshing` was never cleared, every later command went to a dead client, the idle release could never fire (so Steam kept counting DayZ playtime), and the DZSA fallback — gated on `!initialized` — became unreachable exactly when it was needed. A 5 s liveness check (`ISteamUser::BLoggedOn`) with a 15 s grace now drops a dead session, answers everything in flight (a rejected `RefreshDone`, a failed `SyncDone`, an error to a pending unsubscribe), clears `initialized`, and re-opens by itself every 10 s. Each partition also gets a 180 s deadline (the slowest healthy one measured 39 s, D-136) and counts as an empty answer, so two dead partitions end the refresh through the existing throttle path | live worker code; steamworks 0.13.1 `user.rs:24` | gates green |
| D-161 | 2026-09-22 | **A rejected refresh is no longer reported as a successful one.** D-159 made `reject()` answer `Cmd::Refresh` with an empty `RefreshDone` so the UI would stop waiting; `lib.rs` then treated it as a real list refresh, wrote `last_refresh`, pruned rows the refresh had not renewed and emitted a default verify summary, so the user saw "0 of 0 shown · 0 from Steam in 0 s · 0 verified · 0 fake · 0 offline" with no error. `RefreshDone` carries an explicit `rejected` flag now; the host skips the meta write, the prune and the summary, and the browser shows "Steam did not answer the refresh, so the list was not updated" while keeping the previous numbers | own regression from D-159 | gates green |
| D-162 | 2026-09-22 | **Data loss paths closed, and the app stops dying silently.** A corrupt or locked `cache.db` propagated out of `setup`, and with `panic = "abort"` and `windows_subsystem = "windows"` that is no window, no dialog and no message: the icon bounces and nothing happens. The file is now moved aside (kept for diagnosis), its stale WAL/shm removed, and the open retried once. `settings.json` that cannot be parsed is copied to `settings.json.unreadable` and logged instead of being silently replaced by defaults on the next preference change, which used to destroy every launch profile. Three cache write paths reported only through `eprintln!`, which a release build has nowhere to print: upsert, `apply_verifications` and `population_add` now log, as does `history_add` — the exact symptom Q22 describes. `import_official_favourites` swallowed both its writes while returning `imported: N`, i.e. reported a success that saved nothing; it fails loudly now. The DZSA fallback with an empty or all-empty list never emitted `servers:verify-done`, so the spinner ran for ever, and a frontend watchdog clears `verifying` after 5 min and `modScanning` after 20 min with a visible message when the matching event never arrives | resilience audit, each path re-read | gates green |
| D-163 | 2026-09-22 | **Security hardening.** `reqwest` is built with `gzip`, so a few hundred KB on the wire can decompress to gigabytes; `Response::bytes()` buffered all of it and an allocation failure aborts the process. Every body now goes through `src-tauri/src/http.rs` with a cap checked against `Content-Length` first and against a running total while it downloads (DZSA 64 MB, news 8 MB, pictures the existing 16 MB — which previously was only checked *after* the whole body was in memory). The image decoder gets explicit `image::Limits` (8192 px, 256 MB) so a small file declaring huge dimensions cannot allocate the full raster, and picture requests no longer follow redirects, which would have taken them off the host `image_allowed` vetted. `-password=` is masked in the command line shown in the join dialog and copied into support reports. The video frame gets `sandbox` without `allow-top-navigation` — `frame-src` says what may be framed, not what the frame may do. The CSP gains `base-uri`, `form-action` and `frame-ancestors`, which do not inherit from `default-src`. The updater sets `requireSignedVersion`. `junction_name` strips `;`, which Windows allows but `-mod=` uses as its separator. `log_ui` flattens control characters so a UI message cannot forge log lines | security audit; vendored plugin sources | gates green |
| D-164 | 2026-09-22 | **Performance, all measured before and after.** The browser applied every 100 ms Steam batch on arrival, and each write invalidates the whole derived chain (filter, sort, maps, countries, counts) at ~13 ms for 19 000 rows — ~130 ms of every second on the main thread for the length of a refresh. Rows are pooled and merged once every 350 ms instead (~35 ms/s), flushed early on `servers:done` and before any verification is applied. The mod scan opened one transaction per server (43–51 ms per 200-server chunk) and now uses the `replace_server_mods_many` that already existed (8–10 ms), and the `servers:mods` event deduplicates its names, which were sent once per occurrence. Seven fields nothing renders left the wire row (`bots`, `secure`, `keywords`, `steamId`, `lastSeen`, `tags.external`, `tags.unknown`) and absent options are skipped, taking the cold-start payload from 15.05 MB to about 10.4 MB. A2S binds one socket per query rather than one per attempt, so a retry no longer opens a second NAT flow for the same target (the thing D-037 measured as the cause of answer loss), and INFO/PLAYER use a 16 KiB receive buffer instead of 64 KiB. News thumbnails are cached at two sizes — 360 px for cards, 640 px for the featured picture — instead of decoding ~920 KB per card, and their blob URLs are revoked on replacement. Also: `load_all` drops an `ORDER BY` the browser re-sorts anyway, `Cache::get` uses a cached statement, the start-up `COUNT(*)` moved off the first-frame path, the WAL is checkpointed after a completed refresh, `TICK_IDLE` is 100 ms, the friends poll builds one index instead of scanning the rows per friend, and two `[...rows.values()].filter()` copies became counting loops | performance audit with its own benchmarks | measured, see D-166 |
| D-165 | 2026-09-22 | **Feature gaps the flow audit found.** The Join button was greyed out with no reason when DayZ was missing or `DayZ_BE.exe` was gone, and the machine warnings `diagnostics::collect` produces were only visible in Diagnostics — both now appear in the dialog. "DayZ is not installed" is no longer claimed when the library is listed but its drive is not mounted; the message names the folder. Launching while DayZ is already running is refused, because the second process died instantly and its exit restored the launcher's High priority while the real game was still playing (undoing D-119). The three server tables say why they are empty instead of showing a blank grid, and the Favourites list distinguishes "no favourites" from "favourites whose rows have not arrived yet". A friend on an unreachable server produced a button that did nothing; the reason is shown. The details pane re-reads the installed Workshop set after any download, instead of once per session. `scanMods` existed with no way to call it, so a mod filter with unscanned servers was a dead end; the status line offers it. Opening Friends no longer re-opens an idle-released Steam session, which restarts the playtime clock D-077 exists to stop. `invokeLogged` was written for D-158 and used nowhere, so failed commands never reached the log — every call site goes through it now. `cache_stats` failing left the panel that exists to show missing data showing nothing, silently. A `<svelte:boundary>` keeps one failing page from blanking the window | feature and flow audit | verified on screen |
| D-166 | 2026-09-22 | **Documentation reconciled with the code, and the v0.1.22 release repaired.** The docs-versus-code audit found the rename had touched `tauri.conf.json` and the README and nothing else: `LICENSE` named a product that no longer exists, `index.html` still titled the window "DayZ Launcher", and the release workflow's smoke test looked for the old install folder and uninstall key — it threw, which skipped the step that rewrites `latest.json`, so **v0.1.22 shipped a manifest pointing at an api.github.com asset URL the updater cannot fetch**. The manifest was corrected by hand (the `.sig` downloaded from the release, because the runner's installer differs byte-for-byte from a local build, D-148), the workflow now reads `productName` from `tauri.conf.json` instead of hard-coding it, and the rewrite and verify steps run `always()` so a failing smoke test can never leave a broken manifest published. Corrected in the docs: the README's install path, its outbound-traffic list (which omitted Google entirely although the home page fetches YouTube thumbnails and frames a player), its "Join again" feature claim removed in D-157, the missing diagnostic log, the repo map and the `sharp` requirement of `make_icon.js`; docs/05's module table, event names, row payload, and the four §7 claims that never shipped (`content-visibility`, code splitting, an SVG sprite, `tauri icon`); docs/06's column table, filter list and an unreachable breakpoint; docs/00's "no remote code" rule, which the YouTube player is an exception to; docs/07's v0.1.22 row; and five docs/09 rows that read as current while later ones had superseded them. **The CSS gzip budget moves from 8 KB to 10 KB**: the dedup lever docs/05 had been holding in reserve was pulled and recovered 1.3 kB raw but 0.07 kB gzip, because gzip already folds repeated rule blocks, so the 8 kB line cannot be met by refactoring — only by shipping fewer views | docs audit; gh CLI; measured bundle | manifest re-verified |
| D-167 | 2026-09-22 | **Diagnostics redesigned as a compact dashboard, then cut back to what only it could show**. The wall of key/value pairs became a status strip — Steam, DayZ, Mods, Cache, Memory, Start-up, each with a state dot — over cards with tight headers and their own scroll areas. Then everything the strip already said, and everything the Mods page or the server browser already showed, came out: the Workshop item table (the Mods page does it better), the Steam process/account rows, the DayZ state flags and official-launcher row, the duplicate performance and cache figures, and the decorative meter bars. Two implementation notes worth keeping: cards in a flex column shrink by default and the column's `overflow` then clips what a shrunk card cannot show, which silently swallowed two rows of every four-row list until `flex: none`; and a newline before a `{#if}` collapses, so a separator has to carry its own `&nbsp;` | on-screen check | superseded by D-168 |
| D-168 | 2026-09-22 | **Diagnostics becomes Logs**. The page is now the log and nothing else: entries newest first, a problems-only filter, Copy, and a button that reveals the file. Removed with it: `perf.rs` and the `perf_sample`/`perf_first_paint` commands, `cache_stats` and `Cache::stats`, `diagnostics_export`, and `junctions_remove_dangling` — which means D-093's dangling-junction clean-up no longer has a UI; the launcher still never deletes a junction on its own, and the action can come back on the Mods page in a few lines if it is wanted. The `diagnostics` command itself stays: the details pane and the Mods page read the Workshop inventory through it, and `join_plan` uses `diagnostics::collect` internally. **Logging was trimmed to what is needed**: gone are the line per view opened, the DEBUG line per refresh partition, the Steam status line on every 10 s retry, the news-fetch line, the start-up `COUNT(*)` that existed only for a log line, and the verification line on every on-demand check (it fires whenever the visible rows change). What stays is failures, plus one line each for start, a completed refresh, a full verification pass, a launch and its exit, a prune, and the Steam session going away or coming back. The DEBUG level went with its last call site | product decision | gates green |
| D-169 | 2026-09-22 | **Logging can be switched off**. `Settings.logging` (default on) drives an `AtomicBool` in `log.rs`; `write` returns immediately when it is off, and switching it off clears the in-memory ring too, so "no logging" means no file and nothing held. The switch sits on the Logs page as a Recording/Paused chip rather than in Settings, because that is where anyone who cares about it is already looking. Start-up lines before the settings file is read are kept deliberately: those are the ones that explain a failure to start | product decision | verified on screen |
| D-170 | 2026-09-22 | **The dangling-junction clean-up moves to the Mods page** (brought back after D-168 removed the page it lived on). Same rule as D-093: confirmed by the user, and only junctions whose target folder is gone — the launcher still never deletes a junction on its own initiative, and never one it did not create. It sits in the Mods toolbar beside "Update all", which is where the count of stale junctions was already shown, so the number and the action that fixes it are finally in the same place | D-093 | gates green |
| D-171 | 2026-09-22 | **The log covers what a launcher actually does**. D-168 trimmed the noise and took some substance with it, so the coverage was rebuilt around the user's work rather than the app's internals: a join plan records the server, its version against the local one, how many mods it needs, how many are missing or stale, the download size and every warning it raised; a mod download records what was asked for, then whether it finished and how long it took; unsubscribes record what went and what failed; junctions record the ones actually created for a launch, by name; direct connect records the address; favourite alerts record which server and why; and launcher updates record an available version and an install. Together with the lines kept in D-168 — start, refresh, verification pass, launch, exit, prune, Steam session lost and back — a session now reads as a story of what the person did | product decision | verified on screen |
| D-172 | 2026-09-22 | **Each kind of log can be switched off on its own**. Ten areas — Joining, Launching, Mods, Steam, Verification, Cache, Alerts, Updates, Settings, App — as chips on the Logs page, each showing how many entries it has. An entry's area is its target up to the first `:`, so App covers the host's start-up lines and every `ui:…` target together. Muting is enforced in the host (`log::set_muted`, checked in `write`), so a muted area is not written to the file either and costs nothing; the choice is stored in `settings.json` as `logMuted` | product decision | verified on screen |
| D-173 | 2026-09-22 | **The welcome line moves into the title bar**. The News page opened with a banner carrying the Steam avatar, a greeting, two status chips and a "Browse servers" button — a band of chrome that belonged to the window, not to one page, and that repeated what the title bar already showed. The greeting now sits at the right of the title bar in the accent colour, so it is on screen everywhere; the banner, its chips and its button are gone, and the news list starts at the top of the page. `news.loadAvatar()` moved to `App.svelte` with the picture | product decision | verified on screen |
| D-174 | 2026-09-22 | **The News page can be switched off**. `UiPrefs.news` (default on) rather than a launch setting, so it is read at start-up with the localStorage cache and the sidebar paints the right shape on the first frame (D-070). Off removes the section from the rail, makes Servers the landing page, and skips `news.start()` entirely — with it off the launcher never contacts Steam's news feed, its picture CDN or YouTube, which is also the only way to opt out of the third-party traffic D-150 introduced | product decision | verified on screen |
| D-175 | 2026-09-22 | **Backend optimisations, all measured before and after** (from a file-by-file audit against docs/05 §7, benchmarked against a copy of the live 20 MB cache with 12 298 servers and 127 174 mod rows). `server_mods_mod` did not cover `name`, so the mod-catalogue query — which runs during start-up while holding the cache mutex — fell back to a table lookup for all 127 k index entries: **247 ms → 17 ms** with the index widened to `(mod_id, name)`. `servers_last_seen` cost **+20.6 ms on every full upsert** to save 0.8 ms on the one query that used it, so it is dropped. `import_official_favourites` ran a transaction *and* a WAL checkpoint per favourite: **50 favourites 111.8 ms → 6.7 ms** through a new `favourites_set_many`. The mod scan built a full `ServerRow` for every cached server to keep the 13 % that qualify (**19 ms and ~7 MB of strings**); `Cache::scan_targets` reads the eight columns the rules use and joins the last scan time in one pass. `prune`'s orphan sweep scanned 127 k rows on every completed refresh even when nothing was deleted (**23.6 ms**), and now runs only when it did. Also: `Cache::get` builds its SQL once instead of per call, the dead `log_debug!` macro is gone (it referenced a level that no longer exists), and the unused `bytes` dependency and `Win32_System_ProcessStatus` feature are out of the manifest | optimisation audit with its own benchmarks | gates green |
| D-176 | 2026-09-22 | **The row map is a plain `Map` with one version source.** As a `SvelteMap`, `.values()` created and read a source per key, so each of six deriveds registered 19 000 dependencies per recompute (~114 000 edges), and `set()` allocated a `Set` of reactions per row — all to buy per-key granularity that nothing used, because every consumer reads the whole map. Measured on the project's own Svelte 5.57.1 with 19 000 rows and a 2 000-row batch: **flush 6.28 → 2.85 ms, cold start 12.06 → 2.33 ms, reactivity heap 7.7 → 0.9 MB**. Anything reactive reads `rowsTick` first and `rowsChanged()` is the only writer. In the same pass: `#pending` is a plain `Set` (nothing reactive read it), `isLanIp` stops allocating two arrays per row (**6.97 → 1.07 ms over 19 000**), the Logs list is unkeyed so one new entry no longer destroys and rebuilds all 400 rows, and a dead `app_info` call left the start-up path | optimisation audit with its own benchmarks | gates green |
| D-177 | 2026-09-22 | **Accent where the eye goes first**. The welcome line in the title bar and the server table's column headers are accent-coloured, matching the live counts beside them; the headers brighten on hover instead of going grey | product decision | verified on screen |
| D-178 | 2026-09-22 | **The signed-in user's avatar is gone** (first made bigger, then the profile avatar alone removed; friends' avatars stay). It briefly sat at the foot of the sidebar, but the title bar already says who you are, so the picture was decoration. Removed end to end: the rail block and its CSS, `news.avatar` and `loadAvatar`, the `steam_avatar` command, `Cmd::Avatar` and the worker's `avatar()` method. Friends keep theirs — `friend_avatar`, `Cmd::FriendAvatar` and `avatarDataUrl` are untouched | product decision | gates green |
| D-179 | 2026-09-22 | **v0.1.23 published and measured**, built and uploaded from the tag by the workflow at https://github.com/NobbyBo11ocks/dayz-launcher/releases/tag/v0.1.23 (tag = commit 2c2f732, installer 4 734 452 B on the runner, SHA-256 `ec3aa758…0cbd`). The workflow passed end to end for the first time since the rename — smoke install, run, uninstall, manifest rewrite and verification — and `verify_update_sig.js` reports version 0.1.23, the key id matching and both signatures valid through the live endpoint. **Budgets, all inside**: installer 4.73 MB (< 15 MB); host 78.7 MB (< 90 MB); all processes 305.6 MB (< 330 MB), host 78.7 + WebView 226.9 across six processes; idle CPU **0.20 %** of one core over 120 s (< 0.5 %), better than v0.1.19's 0.38 %; JS 62.2 KB gzip (< 120 KB) and CSS 8.2 KB (< 10 KB, the budget raised in D-166). Refresh 3 085 listed / 2 773 answered in 41.7 s, verification 2 773 checked in 30.1 s, mod scan 3.4 s. **Method note**: the idle window must start at least ~3 minutes after a cold start — the refresh, the verification pass and the mod scan run for about 80 s, and sampling across them reads 1.5–5 %, which is work, not idle. Six cold starts logged zero warnings and zero errors; one verification pass took 70 s with 524 unverifiable, which is the D-037 flow-table degradation from restarting the app six times in an hour, not a defect | gh CLI, tools output, external process sampling | verified |
| D-180 | 2026-09-22 | **Settings rebuilt as cards**. It was two ragged columns of headings and rows with a half-empty right side, the News switch buried inside the Steam group and the credits as a wall of text at the bottom left. Now it is eight cards with header bars, the same shape as the Logs page, in two balanced columns: Appearance, Server browser, News and Steam on the left; Launch options, Launch profiles, Updates and About on the right. One 92 px label column means every control lines up, and each note sits under the control it explains instead of floating between groups. "Check for updates" and "Install and restart" wear the accent as the primary action of their card | product decision | verified on screen |
| D-181 | 2026-09-22 | **The last two optimisation-audit findings.** Sorting the list by server name cost **38.6 ms per recompute** over 19 000 rows, because `Intl.Collator.compare` is expensive and runs n log n times — and it runs on every 350 ms flush while that column is selected. The names only change when a row is added, removed or renames itself, which verification never does, so one collator sort now builds a rank per id and every later comparison is integer subtraction (~3.6 ms). The id tie-break is baked into the ranks, so the order is identical; checked on screen against the live list. Measured on this machine first: `localeCompare` with the same options costs **922 ms**, so D-152's shared collator stays. Second: a friend's 32×32 avatar is 4 096 bytes of RGBA but crossed IPC as **14 657 bytes** of JSON decimals (3.6×) and arrived as a 4 096-element JS array; `friend_avatar` returns `tauri::ipc::Response` now, like `news_thumb`, with an empty body meaning "Steam has not cached it yet" — the case the caller already retries. Verified live with 33 friends, every avatar rendering | optimisation audit; on-screen check | gates green |
| D-182 | 2026-09-22 | **Favourite alerts removed**. The bell on a favourite, the per-minute INFO watcher behind it (D-083), its toast and its Windows notification are gone, along with `watch_favourites`, `favourite_alert_set`, `Cache::favourites_watched`, the `alert` column's migration and read, the `FavouriteAlert` type on both sides, and the Alerts chip on the Logs page. It also retires the finding that the watcher could overrun its own 60 s poll: twenty silent favourites took 60 s of sequential 1.5 s-timeout probes, so "came back online" arrived a full cycle late — there is nothing left to be late. **Kept**: the news toast and its Windows notification for DayZ update posts (D-099) are a separate feature, so `tauri-plugin-notification` stays; it was briefly removed here and put back when the frontend stopped compiling, which is the check working | product decision | gates green, verified on screen |
| D-183 | 2026-09-22 | **A landing page for the launcher** (hosting still undecided, so it is deliberately host-agnostic). `site/` is one static `index.html` plus the two screenshots and the icon — 720 KB in total, no framework, no web fonts, no trackers, nothing to build. It uses the app's own tokens (slate background, lime accent) so the two read as one product, and the download buttons point at `releases/latest`, which resolves correctly from any host. Content is the honest version: the trust rules as the headline, the measured v0.1.23 numbers rather than adjectives, every outbound connection listed in full, and the SmartScreen warning stated plainly rather than buried. Drops onto GitHub Pages, Netlify, Cloudflare Pages or any static host by copying the folder; nothing host-specific (no CNAME, no deploy workflow) until the choice is made | verified over a local HTTP server at desktop and narrow widths | verified on screen |
| D-184 | 2026-09-22 | **UX audit round: eight things that were actually broken.** **The join dialog never took focus**, so the grid behind it kept answering keys: ↓ then Enter re-pointed `joiningId` at a different server and the `{#key}` from D-159 rebuilt the dialog, silently discarding the plan, a typed password and the wait-for-slot choice — the launch was never wrong (D-159 saw to that) but the swap was invisible. The dialog now focuses its first control and traps Tab, and the page's own key handlers stand down while it is open. **"Close (keeps downloading)" pointed at a page that showed nothing**: both Mods listeners dropped any event whose job was not their own, and the page's job is 0 until the user presses Update there, so a download started from a join had no progress, no completion and no reload — the Steam worker runs one sync at a time, so Mods now adopts whichever job is in flight and says when it came from a join. **A failed update install was reported as a failed check** and removed the Install button, leaving only "Check for updates"; it stays installable now and the message names the step that failed. **"Join again" probed up to ten ports at 1.5 s each** with no pending state, so it looked dead for ~15 s and invited repeat clicks, each starting another probe. **The Servers empty state contradicted the status line** — it claimed Steam had not answered on the first frame (`steam` is simply null until the first status event) and then told the user to press a Refresh button that is disabled because the refresh it describes is already running. **Favourites blamed Steam for the search box it shares with Servers.** Also: the Mods page sent users to Diagnostics, which no longer exists, for a button 300 px above the sentence | UX audit, each finding re-read in the code | gates green |
| D-185 | 2026-09-22 | **Two promises the code was not keeping.** **News kept fetching after being switched off**: `start()` armed a 30-minute interval, nothing could cancel it, and `refresh()` had no gate — so with the page hidden the launcher still hit Steam's feed every half hour and an update post still raised a Windows notification and a toast linking into a page that was no longer in the sidebar. Settings and the website both say "the launcher never contacts Steam's news feed, its picture CDN or YouTube"; now it does not. There is a real `stop()`, and the check sits in `refresh()` because that is the one place every fetch passes through. **The index migration ran on every start**: D-175's `DROP INDEX`/`CREATE INDEX` batch was unconditional, measured at 87–112 ms against the live 147 000-row cache, on the path before the window exists — several times per launch what the index saves per query. It checks `sqlite_master` first and is now what it always claimed to be, a migration | correctness audit, measured against a copy of the live cache | gates green |
| D-186 | 2026-09-22 | **Accent as ink is a different problem from accent as fill.** All 24 theme × accent combinations pass for `--accent-fg` on `--accent` — that half was tuned properly. Using the accent as *text*, which D-177 did today for the title bar and the table headers, was never checked: measured against the three light surfaces, **lime 2.73:1 and amber 3.21:1**, with red 4.26 and green 4.43 also short, and lime is the default accent (D-132). `--accent-ink` separates the roles: it equals `--accent` everywhere except light-theme lime, amber, red and green, whose ink values clear 4.5:1 on all three surfaces (5.22, 5.75, 5.71, 6.29). Two tinted-accent surfaces went with it — the news "update" pill measured as low as 2.22:1 and the friends marker 2.40:1 — both now sit on the plain row background. Also fixed: **~40 buttons had no focus ring**, because every component's `.btn` block opens with `all: unset` (which resets `outline`) and beats `app.css` on source order at equal specificity; a more specific global selector recovers all of them, including every control in the join dialog. And `var(--muted)` — a token that does not exist — was rendering today's table empty state at full `--fg` | visual audit with computed ratios; values re-measured here | gates green |
| D-187 | 2026-09-22 | **The cache-recovery path could destroy the data it was meant to protect.** D-162 moved an unopenable `cache.db` aside so a corrupt file could not stop the app starting with no window, and then deleted `-wal` and `-shm` unconditionally. When the file is *locked* rather than corrupt — an antivirus scan, a backup, a sync client — the rename fails while deleting the unlocked write-ahead log succeeds, so the retry reopens the original database with every uncheckpointed commit gone. Favourites, join history and population live nowhere else. The audit reproduced it end to end: 3 joins and a favourite in the log, 0 of each afterwards, `integrity_check` still `ok`, and the only trace a line reading "started on a new cache; old file kept: false" — wrong twice over, since it was neither a new cache nor kept. That is the Q22 symptom exactly. It shipped in v0.1.23, *after* the original 2026-09-22 00:13 incident, so it cannot be the first cause, but it is a live one. Now the three files move together or nothing moves, and a rename that fails ends the attempt with a message naming the likely cause instead of touching a byte. **`checkpoint()` could not see its own failure**: SQLite reports a blocked PASSIVE checkpoint in the pragma's result row, not as an error, so `execute_batch` returned `Ok` while nothing was copied — measured at `(busy 0, log 2, checkpointed 0)` with a reader holding an older snapshot. It reads the row now, which is the instrumentation Q22 was supposed to have. **A corrupt `servers` page emptied the Favourites view**: one `try` covered the whole of `start()`, so a failed row read skipped `loadFavourites()` and every star vanished for the session although the table was fine on disk — apparent loss that reads exactly like the real thing. **A settings file of `[]` parsed as valid**: serde deserialises a struct from a sequence, so the corrupt-file safety net was bypassed by the shape most likely to appear. The shape is checked now, a regression test covers it, and the kept copy is timestamped so a second occurrence cannot overwrite the first. Confirmed safe by the same audit and not changed: the v0.1.22 → v0.1.24 upgrade keeps every row (the destructive branch of `migrate` is unreachable for all shipped versions), the retired `alert` column is harmless because every statement names its columns, launch profiles survive in both directions, and an auto-update can never delete app data | data-integrity audit, every case reproduced against copies of the live cache | 72 tests, gates green |
| D-188 | 2026-09-22 | **Second optimisation round, all measured.** **The filters were read through a `$state` proxy once per row**: seventeen `f.<field>` reads inside the `list` loop, and a proxy get measures 38.9 ns against 1.6 ns for a plain read. Hoisting them takes one recompute from **10.64 ms to 0.24 ms** at 13 106 rows, and the whole chain a flush drives — `list`, `maps`, `countries`, `untrustedCount`, `populatedCount`, `unscannedModded`, `status` — from **14.37 ms to 5.72 ms**. It ran every 350 ms flush with the Servers page open, so it was the most expensive thing left in the app. **The favourites import probed one server at a time** at 1.5 s each: 50 favourites measured **21.9 s**, and **75.9 s** when none answered, against **1.63 s** either way once they run together. The client's own 128-permit semaphore and 400 pps pacer already bound the burst, so D-037's constraint is untouched. **`Verification.keywords` crossed IPC 2 773 times a pass and nothing rendered it** — the browser reads the parsed `tags`. One 500-server `servers:verified` event was **202 KB, already at the ~200 KB cap docs/04 §4 sets**, and is 157 KB without it; the host still writes it to the cache. Confirmed by the same audit and left alone, each with the number that settled it: `unscannedModded`'s `SvelteMap.has()` per row (1.072 vs 1.113 ms — `has()` never creates a source for an absent key), `modsByServer.get()` in the filter (0.27 ms), `applyMods` into a `SvelteMap` (≈2 ms across a whole scan), hoisting the sort's `switch` (0.091 ms), and a suspected A2S deadline bug that survived 1 000 servers across five client configurations with 0 lost | optimisation audit with its own benchmarks | gates green |
| D-189 | 2026-09-22 | **The two structural findings from the visual audit.** **The details pane was clipped between 1001 px and 1239 px.** The table cannot go below 700 px of min-content (490 px of fixed columns plus a 200 px name floor and the gutter), so a row with the 360 px pane needs 1060 px of content — but the rail collapses at 1100 px while the pane only hid at 1000 px, leaving a whole band where the pane was cut off the right edge: at 1101 px roughly 40 % of it, including the Join button. Below 1240 px it is an overlay on the right now rather than clipped or hidden, which is D-153's open recommendation — at the 960 px minimum a row click used to select the row and visibly do nothing. **The server grid announced as empty.** ARIA requires rows to be owned by their rowgroup and the two virtualisation wrappers sat in between, so the whole list was invisible to a screen reader; the cells had no `gridcell` role, so a row would have read as one run of text anyway; the empty-state paragraph was an invalid owned child of the rowgroup and was dropped; and `aria-rowcount` said 19 000 with no `aria-rowindex` on rows, which is meaningless with virtualisation. Wrappers are `presentation`, cells have their role, rows carry their real index, and the empty state moved outside | visual audit; checked on screen at the 960 px minimum | gates green |
| D-190 | 2026-09-22 | **The exit crash, and two silent failures around it.** **The launcher fast-failed with 0xC0000409 on exit.** D-160's liveness check used `ISteamUser::BLoggedOn`, which is false whenever the client is merely offline or reconnecting — routine — so the worker dropped the session and rebuilt it in a loop, logged live as "session is gone" / "session re-opened" ten seconds apart with a refresh landing "0 listed, 0 answered, early=true" in between. Dropping the `Client` calls `SteamAPI_Shutdown` while Steam's own socket thread is mid-flight, and `steamclient` answers with *"Illegal termination of worker thread 'SocketThread'"*. `SteamAPI_IsSteamRunning` answers the question actually being asked, and the session is no longer dropped for it — only D-077's deliberate idle release still drops one. Tauri exits through `process::exit`, so `Drop` never ran either: there is a real `shutdown()` on `RunEvent::Exit` now that joins the worker thread and then checkpoints the WAL, which also stops an exit abandoning uncommitted history. **A failed RULES query launched the game vanilla into a modded server**: `launch_game` mapped the error to an empty mod list, which does not mean "no mods", it means "we could not ask" — the server rejects the connection and the only trace was a log line reading "0 mod link(s)". It falls back to the cached list now (`mods_for`), and refuses to start at all when nothing is cached and the row is modded. **The `steam:status` snapshot was read before the listeners existed**, several awaits early; the worker emits "session open" exactly once, so an event landing in that window left `initialized: false` for the whole session — Refresh disabled, no automatic refresh, and the DZSA fallback pulling ~24 MB instead | bug audit; crash reproduced from the live log, each path re-read in the code | 72 tests, gates green |
| D-191 | 2026-09-22 | **Workshop updates are asked of Steam, not read from its file.** Mods were only ever compared through `appworkshop_221100.acf` (`latest_time_updated > time_updated`), which records what Steam knew the last time *it* checked — a mod its author updated can sit stale in that file indefinitely, and the user finds out when the server rejects the join. `ISteamUGC::GetItemState` answers live from the running client, the same source the download progress already reads, and it is filtered to `SUBSCRIBED | NEEDS_UPDATE`: an item can sit installed on disk without a subscription — left over from an unsubscribe Steam has not collected, or pulled in by something else — and Steam does not maintain those, so an update offered for one is an update that will never arrive. The live answer *replaces* the file's rather than joining it, which is the only way the subscription rule can hold; the file is the fallback for when Steam cannot be asked, and "could not ask" is a distinct answer (`None`) from "nothing is stale" so the two are never confused. One store feeds all three readers — the Mods page, the join plan and a badge on the Mods rail entry — checked five seconds after start (the collect walks folders and junctions, so it stays off the first-frame path), on every mod job finishing, when the page loads, and every fifteen minutes | `ISteamUGC` docs re-read (docs/08); D-075 sync path unchanged | 72 tests, gates green |
| D-192 | 2026-09-22 | **`SteamAPI_IsSteamRunning` reads the one registry value this repository already knew was unreliable.** D-190 replaced `BLoggedOn` with it; twenty seconds after every start the launcher then logged *"Steam is no longer running"*, emptied the Friends page ("Steam is not running — the friends list comes from Steam"), and turned the first refresh into "0 listed, 0 answered, early=true", with Steam plainly up and the title bar still showing its cached counts. The function answers from `HKCU\Software\Valve\Steam\ActiveProcess\pid`, which `docs/02` §2 and D-026 both record as stale after an unclean Steam exit — caught at 16884 against a live 15176 on 2026-09-21, and reading 16884 against 14916 today, a year-old stale value still sitting in the hive. Liveness is `registry::detect().running` now, the same probe the rest of the app has used since D-026: it enumerates processes and requires the image to live under `SteamPath`, so no key can make it wrong and Steam need not cooperate. Measured at **2.46 ms**, so the interval went 5 s → 10 s to keep it at 0.025 % of one core against the 0.2 % idle budget; detection lands 15–25 s after Steam actually quits, and the session is still never dropped for it (D-190). The lesson is the one D-160 and D-190 both missed: *ask the operating system, not the client that may already be gone* | D-026 and `docs/02` §2 re-read after reproducing it live; cost measured by `registry::cost` | 73 tests, gates green |
| D-193 | 2026-09-23 | **Third optimisation round: two barriers and a shared queue.** **`with_rate` was never isolation.** It rebuilds the pacer and keeps the shared `Arc<Semaphore>`, so a bulk pass started from `state.a2s` inherits the interactive pool — and concurrency ÷ rate is exactly how long a permit-holder waits for its datagram. At 128 ÷ 100 pps that is **1.28 s, past the 1 s deadline before the challenge leg is even sent**, so the mod scan's retry was doing the work: instrumented over 2 400 targets, **1 782 of 1 800 answers arrived only on attempt 2**, and the scan put **6 582 datagrams on the wire where 4 200 were needed** — 57 % more NAT churn, against the rule D-037 exists to protect. Its own 64-permit pool puts the queue at 0.64 s: **74.8 s → 49.6 s, first rows 59.7 s → 0.64 s**, interactive path untouched. A test now asserts the distinction `with_rate` does not make. **Verification streams.** Chunks of 500 meant the first result appeared when the slowest of 500 had answered — **2.95 s against 0.40 s** — and each barrier idled the pacer through its own tail, **17.99 s against 15.00 s** for the pass, within 8 % of the 13.87 s pacer floor. Streaming holds permits continuously, which in the shared FIFO starved a row click of its three queries by 85×, so it gets its own pool too; events are capped at 128 rows (~71 KB) or 250 ms. The front end pools the version bump the same way it already pools row batches, so 28 events cost one derived-chain recompute rather than 28. **The map column called the collator per comparison** — D-181 fixed the name column and stopped there. There are only 149 distinct map names across 13 380 rows: ranking the distinct values is **13.43 → 4.69 ms with byte-identical order**, on every 350 ms flush while that column is selected. **`prune_thumbnails` kept precisely the files it was written to delete**: `rsplit_once('-')` returns `None` for a pre-D-160 `<gid>.jpg` and left the whole stem in place — a live gid — so the sweep spared them. 18 of 37 files here, **1 031 165 of 1 540 316 bytes** the app can never read again; regression test added. **`servers:batch` had no size cap**: Steam hands over thousands of rows in one 100 ms window and the flush took all of them, against the ~200 KB docs/04 §4 sets. 358 rows at the measured 558 B/row, and a partition that is ending drains completely before it is taken. **The details pane queried on every selection change** — three paced queries and a 6.4 ms derived chain per row — so holding ArrowDown cost ~64 ms of main thread and ~50 datagrams a second; it waits 220 ms for the selection to settle. Considered and left alone: `Verification.reason` (the 9 % it saves was worth having at 500-row events, not at 128), `modCatalog` as a plain Map (1.6 MB of a 306 MB app for a reactivity refactor across three components), and the flag sprite as WebP (−84 KB of a 4.73 MB installer against a 15 MB budget, and the decoded size is identical). Also, unrelated to speed but recorded here: **every start now logs the row counts** of `servers`, `favourites`, `history` and `population` before anything can write to them, because Q22 and Q25 are both "the user's rows are gone and nothing says when" and three counting queries are a cheap way to give the next occurrence a before-and-after | optimisation audit, every number re-measured here; **confirmed live at v0.1.26**: a verification pass of 2 187 servers took **18 887 ms** against 27 261 ms for 2 492 the build before — 8.64 ms per server against 10.94, with the first rows on screen immediately instead of after the slowest of 500 | 74 tests, gates green |
| D-194 | 2026-09-23 | **Audit round: Q25 answered, and the failures that had no way of saying so.** **Q25 was three ordinary events, not a bug.** The whole app-data folder was removed at 2026-09-22 00:19:54 UTC — both roots carry that creation time to the same second and the oldest row in the database is four seconds younger — which the uninstaller offers as an opt-in box; the three joins were cleared, logged at 12:24:09; and **no favourite was ever written to this file**, proved by the `favourites` b-tree root holding two non-zero bytes in 4 096 against `history`'s root, which still holds all three deleted cells verbatim. It also undermines Q22's premise: NTFS tunnelling preserves a creation time across a delete-and-recreate within ~15 s, so "the file kept its creation time" proves nothing, and "everything else survived" is an illusion because `servers` and `population` refill by themselves within minutes while the user's two tables only refill when the user acts. **The launcher could fail to start with no window, no message and no log line**: `setup` returning `Err` is a Tauri `panic!`, and with `panic = "abort"` plus `windows_subsystem = "windows"` that is an icon flash and nothing else — the most likely cause, a locked `cache.db`, being exactly the case where the log cannot be written either. There is a panic hook with a message box now, and a locked or unmovable cache falls back to an in-memory one for the session rather than refusing to start. **Steam restarting left a corpse**: D-190 rightly stopped dropping the session at the moment of loss, but marking it live again on Steam's return without re-opening it meant every later refresh spun for the full 180 s partition timeout and returned "0 listed". The drop happens on the *return* instead, where nothing is in flight. **One failed favourites read cost the whole session**: it sat unguarded before all eight `listen()` calls, the status snapshot, the first refresh and the watchdog, so a single `SQLITE_BUSY` from a backup left the grid reading "Starting up…" until restart. **A valid-but-empty news reply overwrote the cache and then deleted every thumbnail**, with no Refresh button since D-122 to undo it. **A truncated `libraryfolders.vdf` or `appworkshop_221100.acf` killed the join dialog for every server, vanilla ones included**; they are warnings now. **The News off-switch failed open**: the prefs read falls back to defaults after its retries and the default is on, so a backend slow past ~2 s re-enabled a page the user had turned off — and with it the fetches the page promises never happen. Also: `checkpoint()` was measured copying nothing with `busy = 0`, so its warning no longer hides behind that flag and the two writes whose durability the user would notice — a favourite and a join — escalate to a FULL checkpoint; `favourite_set` logs, the one user table that had no logging at all; a failed relaunch after an update no longer reports itself as a failed install; a failed slot probe no longer masquerades as "still full"; the Logs page's chips no longer move before the guard that decides whether they can; the three third-party GitHub actions that run beside the signing key are pinned to commit SHAs; and `opener:default` is spelled out as the three permissions it currently expands to, so a future release cannot widen it silently | failure-mode, security and data-integrity audits; every finding re-read in the code | 74 tests, gates green |
| D-195 | 2026-09-23 | **The browser spoke the game's internal names, not the player's.** A2S reports the map folder, so Livonia is `enoch`, Chernarus is `chernarusplus` and the Frostline map is `sakhal` — and the search box matched that string only. Typing "Livonia" found **nothing**, with **2 942 Livonia servers** in the cache at the time; "Frostline" found nothing against 383. It is the same confusion players hit with DZSA, where the answer on Steam's forum is the single word "Enoch" (S-77). A table of the maps whose id genuinely differs from the name in use gives the label; everything else is still shown exactly as the server reports it, because inventing a name for a community map would be worse than a plain id. Search matches the id, the label and an alias list, so "chernarus", "livonia" and "frostline" all work; the folder stays in the row's tooltip, since owners quote it in their rules. The dropdown also keys on the lower-cased id — servers disagree about capitalisation, so **189 distinct map values collapse to 154** and `ChernarusPlus` stops being a second map. **Official and Community are now a filter**, which the in-game browser makes a top-level tab: it decides whether your character follows you between servers, the `privHive` tag has carried it on every row since D-035 and nothing read it — 172 official against 20 121 community in the cache. The details pane says it in those words instead of "Private · shard ABC123", the shard being a placeholder on 3 143 of the 3 441 rows that carry one. **The BattlEye chip is gone**: it filtered out **4 of 20 293 servers**, and the anti-cheat row now appears only when BattlEye is absent or file patching is allowed, which is the case worth seeing | competitive audit; every number re-measured against the live cache here | 74 tests, gates green |
| D-196 | 2026-09-23 | **The release smoke test was killing one process and waiting on three.** The uninstall assertion went red on v0.1.26 and again on v0.1.27 with `dayz-launcher.exe` and `steam_api64.dll` still in place — and still there after **120 s**, so the "NSIS keeps deleting after its parent returns" race D-194 widened the window for was never the explanation. `Stop-Process -Id $app.Id` ends the host process only; Tauri's WebView2 children are separate processes that outlive it and hold the install directory open, and NSIS gives up on a file in use without failing. Reproduced the other way round locally, which is what pointed at it: with the whole tree gone, both binaries are deletable within five seconds. The step now stops its direct children first — WebView2's own gpu and renderer processes are children of that browser process, and go with it — waits for the process to actually leave the table, and — when files do remain — prints the processes running from the install directory instead of throwing a message with no evidence in it. It also asserts what the uninstaller's contract really is: the Add/Remove Programs entry gone, not just the files. Worth recording separately: **both releases were fine**. `latest.json` carries the browser download URL and both signatures verify through the live endpoint; only the assertion failed | reproduced locally against the shipped binary | fix green on a build-only dry run; v0.1.27 itself was built by the *old* step, and both releases verify live |
| D-197 | 2026-09-23 | **Four audits, and a third of what they found was mine from the last two rounds.** **The map column sorted by the folder and rendered the name** (D-195): "Livonia" appeared at two positions thirty rows apart and each capitalisation made its own block — 28 labels occupied more than one rank. **A map filter saved before D-195 was migrated on the localStorage path and not on the settings path that wins**, so 103 capitalised map values covering 1 005 servers gave an empty grid, "No server matches these filters" and a *blank* dropdown, because no `<option>` carried the old value. **The hive filter was the only filter `activeFilterCount` did not count**, so filtering to Official alone hid the Reset chip. **`class="bad"` on the anti-cheat row matched no rule at all** — `.bad` exists only as `.chip.bad` and `.trust.bad` — so the one row that exists to warn rendered as body text, the same silent miss as D-186's `var(--muted)`. **`with_concurrency` bounded nothing**: every `run_verification` built a fresh 128-permit pool and they all drew on one pacer, so an interactive query's first send waited 0.29 s behind one pass and 2.53 s behind eight; the on-demand check goes back in the interactive pool, the full pass and the mod scan take an in-flight flag each. **`checkpoint_durable` blocked five seconds holding the one cache mutex and still left 402 of 907 pages in the log** — measured — so a star during a backup froze the list for five seconds and did not even get what it blocked for; the FULL wait is a quarter of a second now. **`prune_thumbnails` deleted any file without a hyphen**, not just a thumbnail. Not mine: **every refresh idled exactly 3 000 ms after its last partition**, because the completion branch shares the clock with `PARTITION_GAP`, which exists to space out *requests* (D-046) and there is no next request; **a busy `Cmd::Refresh` was discarded in silence**, so a manual Full refresh was downgraded to whatever was already running — D-159 made that rule for the no-session path and the busy path never got it; **`version_int` overflowed `i32`** on a server advertising "22.0.0", wrapping in release to a fabricated version that then told the user the server would reject them; **`run_mod_scan` abandoned the rest of a 200-server chunk** on the first cancelled task; **the join dialog's focus trap was armed once**, so pressing Join destroyed the focused button, dropped focus to `<body>` and let the grid behind the modal answer keys again — D-184 all over again; **the Friends poll was rebuilt on every `steam:status`**, which the worker emits from eleven places; **`news.start()` raced `stop()`**, announcing a post and arming an orphan interval for a page just switched off; **the settings file overwrote filters the user had already changed** in the two-second window its read retries for; **the visible-rows effect never fired on a sort change**, so newly visible rows waited up to 60 s to be verified; **the Logs chips were silent no-ops** after a failed settings read; **a failed relaunch left "Installed, restarting…"** on screen beside the error. And in CSS: the `.secondary`/`.ghost`/`.danger` modifiers lost to every component's own `.btn` on source order exactly as the focus ring did in D-186, so **Cancel rendered identically to Join and the unsubscribe confirmation carried no red**; `--border` measured **1.17–1.35:1** against the three surfaces, so no control had a boundary anyone could see, and there is a `--border-control` token at 3:1 now; placeholder text was Chromium's default at 3.51:1; and the news hero's 320 px cap was never released when it stacks, hiding the headline, the summary and all three buttons across the whole 960–1100 px band — which D-153 recorded as fixed | Rust, Svelte, accessibility and docs audits; every finding reproduced by the audit that raised it | 74 tests, gates green |
| D-198 | 2026-09-23 | **The accessibility round: keyboard, announcement and the contrast the last round did not reach.** **Sorting the server table was mouse-only** — the headers are deliberately outside the tab order, seven extra stops in front of a 20 000-row grid being worse than none, so the grid carries the keys instead: ←/→ move the sort column, Space flips its direction. **The first-run overlay declared `aria-modal="true"` and behaved like nothing of the sort**: focus stayed behind it, Tab walked the page underneath and Escape did nothing; it has the same trap the join dialog has had since D-184. **The join dialog never gave focus back**, so closing it dropped the keyboard user to `<body>` and lost the row they came from. **The accent picker was twelve tab stops and the theme picker two** — a `radiogroup` is one stop with arrow keys inside it (WAI-ARIA APG), which is what both are now. **The toast rail was the only live region in the app**, so the Servers status line, the join dialog's phase text and the Mods and Friends messages all changed with nothing announcing them; `polite` on the status text because it changes several times a second during a refresh. **`role="tab"` with no tabpanel anywhere** on the Mods filters and the News segments — they are toggles, and `aria-pressed` is what the FilterBar chips already use correctly; the log area chips had no state at all beyond opacity and a strike-through. **Ping quality and the version mismatch were colour alone**; both now say it in words in their tooltip, which is also what let the next fix be simple. **The selected row failed contrast in all 24 theme × accent combinations**: on the 18 % tint `--fg-muted`, `.ok` and `.warn` measured 3.41, 3.58 and 3.43 at worst, and no tint that keeps the row visible fixes that — so the tint went to 12 % and the row drops the dim and quality colours for full `--fg`, which measures 10.02 dark and 12.00 light at worst. Nothing is lost, because the meaning moved into the tooltips. **Every focus ring was drawn in `--accent`**, which is 2.73:1 on a row and 2.88 on the page background for lime — the default accent (D-132) — so all of them use `--accent-ink`, the token D-186 added for exactly this. Also: a global `button:focus-visible`, because D-186's rule only reaches `.btn` and the Mods filter tabs, its sort headers and the toast close button are not `.btn`; and Recent's local `.btn.danger` put white on `--danger` at 3.35:1 | accessibility audit; every ratio recomputed here before and after | 74 tests, gates green |
| D-199 | 2026-09-23 | **Reverted the same day.** With Steam closed the greeting and the friend count both disappear, because both come from a live session — the persona from `ISteamFriends::GetPersonaName` and the count from the friends poll — while the server count stays, since that one is read from the cache. Putting the reason in the greeting slot looked like a fix for a bar that says nothing; but the slot is the welcome line and nothing else — the line is there to greet them, and Settings already says what is wrong in Steam’s own words. The behaviour is back to what it was: greeting when there is a persona, empty when there is not | product decision | gates green |
| D-200 | 2026-09-23 | **Superseded by D-205 the same day.** The `StrCpy $DeleteAppDataCheckboxState 0` guard added here was a no-op: NSIS evaluates `${If} $Var = 1` with `IntCmp`, which reads an unset variable as 0, so the guard it was meant to add already existed. The reasoning behind it was wrong too — see D-205 for what actually happens and what fixes it | – | – |
| D-201 | 2026-09-23 | **The installer was showing last week's icon.** `make_icon.js` redrew the mark on 2026-09-22 at 12:26 — lime, rounded strokes, a faint ring — and `header.bmp` and `sidebar.bmp` were made at 12:19, seven minutes earlier, so the setup wizard greeted people with an amber square-cut Z that the app no longer used anywhere. There is a `tools/nsis_art.js` now that draws both from the same mark and palette as the icon, so they cannot drift again; NSIS wants BMP, which sharp does not write, so the raw pixels get a 24-bit BMP header from thirty lines here rather than a new dependency | compared the two bitmaps against `icons/128x128@2x.png` | regenerated and reviewed |
| D-204 | 2026-09-23 | **The one-pass guard from D-197 could lose a whole verification pass.** `lib.rs` took the populated targets on *every* `servers:done`, including a rejected one — and a rejected `Done` is either "never reached Steam" (D-160) or, since D-197, "a refresh is already running". So a double-click on Refresh, which fits inside the 100 ms the worker takes to set `refreshing`, made the rejection steal the live refresh's partial rows, spend the guard on them, and leave the real completion a minute later to find the guard held and skip. The front end arms "verifying" on every refresh and only `servers:verify-done` disarms it, so the status line sat at "verifying player counts…" for five minutes until the watchdog invented an error — and thousands of rows kept their unverified counts, which is the one thing this launcher exists to fix. Three changes: a rejected `Done` takes no targets, a failed claim emits the empty summary rather than going quiet, and the guard is released after the verification instead of after the mod scan — holding it across both kept the door shut for ~69 s at the measured v0.1.26 timings. **The mod scan claims its own flag inside `run_mod_scan`** rather than at one call site: the button went through the guarded command and the automatic scan after a refresh called the function directly, so two scans could run at 200 pps over the same chunks, against the rule D-037 exists to protect | Rust stability audit; both paths replayed against a transcription of the real guard and event arms | 74 tests, gates green |
| D-205 | 2026-09-23 | **What actually deletes the user's data on an upgrade, established properly this time.** D-200 guessed at a silent uninstall and guessed wrong twice: NSIS skips page callbacks entirely under `/S` (its manual, §4.12), so a silent install never reaches the uninstall branch at all, and `${If} $Var = 1` on an unset variable is already false because LogicLib maps `=` to `IntCmp` — the guard added there did nothing. The NTFS change journal shows what did happen: an **interactive** upgrade. `PageReinstall` pre-checks "Uninstall before installing", `PageLeaveReinstall` launches the previous uninstaller **with no flags at all**, and the user — halfway through installing — is shown the uninstaller's own confirm page, "Delete application data" checkbox included. Tick it and both app-data roots go, along with the Start Menu and Desktop shortcuts, because every one of those is gated on `$UpdateMode <> 1`. The fix belongs on the installer side, where it reaches people already on an older build: their uninstaller is the one that runs and it already honours `/UPDATE`, so an upgrade now passes `/UPDATE /S`. **Auto-update was never affected** — `tauri-plugin-updater-2.12.0/src/updater.rs:893` always passes `/UPDATE`. Also from the same audit: **a silent install over a locked binary exited 0, skipped the file and wrote the new version to Add/Remove Programs**, because NSIS defaults `AllowSkipFiles on` and the silent default of its Abort/Retry/Ignore box is "ignore" — and WebView2's children outlive the host long enough to hold the exe (D-196), so an auto-update could report success, change nothing, and offer the same update for ever. `AllowSkipFiles off`. The template's deviations are marked in the file now, each carrying the upstream line it replaces, so `nsis_template_check.js` puts them back and compares instead of keeping a second copy of what we wrote | installer audit; NTFS change journal, NSIS manual §4.12, LogicLib.nsh, and a neutered replica | template check green |
| D-206 | 2026-09-23 | **The setup wizard asks one question before it writes anything.** A page of its own, before the install: "Do not show the DayZ news page", with `/NONEWS` for silent installs. It is the one setting worth asking about at install time rather than after, because with it off the launcher never contacts Steam's news feed, its picture CDN or YouTube — someone who wants that has it from the first frame instead of after a page has already loaded. The page exists so the next such question has somewhere to go. **Three things went wrong on the way and each is worth keeping.** A checkbox bolted onto the *finish* page sat behind the 164 px sidebar bitmap, losing its box and the first five characters of its label — that page carries the artwork inside the same inner dialog, so nothing left of ~176 px is visible; the answer was not to move it but to stop using that page. Writing the answer from `.onInit` looked right and was not: an upgrade runs the previous version's uninstaller *partway through the install*, and that deleted the key underneath it — the write belongs in `NSIS_HOOK_POSTINSTALL`, which is also Tauri's supported extension point, so it is a new file (`nsis/hooks.nsh`) rather than another deviation from the stock template. And the Rust side read `r"Software\dayzlauncher\..."`: a raw string, so the doubled backslashes were literal and the key never matched — the value sat there and the news page kept appearing. Verified end to end: `/NONEWS`, then a start that logs "news page switched off by the installer", `settings.json` reading `"news": false`, the registry value gone, and the News entry absent from the rail. The launcher consumes the value and deletes it, so it stays an instruction rather than becoming a second source of truth the user cannot see | built, broken three ways, and verified end to end on this machine | 74 tests, template check green |
