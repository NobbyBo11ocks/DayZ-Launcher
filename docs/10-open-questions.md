# 10 · Open questions

Resolve each with a primary source or a local experiment, then move the answer to [09-decisions-log.md](09-decisions-log.md) and delete it here.

| ID | Question | Blocks | How to resolve |
|---|---|---|---|
| ~~Q1~~ | Resolved 2026-09-21 → D-010: official launcher log shows `DayZ_BE.exe` with `0 1 1 -exe DayZ_x64.exe "-mod=…"` | – | – |
| ~~Q2~~ | Closed 2026-09-22 → D-140 as not applicable: the launcher always starts `DayZ_BE.exe` with the official argument form (D-010), which is what starts BattlEye, and that path has been verified live since M5. Whether `DayZ_x64.exe` alone would start BattlEye only matters for a path we deliberately do not take | – | – |
| ~~Q3~~ | Resolved 2026-09-21 → D-034: one anonymous entry per player (empty name, score 0, real duration) | – | – |
| ~~Q4~~ | Resolved 2026-09-21: the launcher's 221100 session coexists with the running game (M5 and D-074 launches happened with the session open, no errors); the visible side effect is Steam's "In-Game" status and playtime while connected, which D-077's idle release now limits | – | – |
| ~~Q5~~ | Resolved 2026-09-21 → D-073: DB-IP IP-to-Country Lite (CC BY 4.0) compacted to a 1.8 MB table embedded in the exe; flags from flag-icons (MIT) as one sprite; no online lookups | – | – |
| ~~Q6~~ | Resolved 2026-09-21 → D-018: `steamworks-sys` vendors `steam_api64.dll`/`.lib` under `lib/steam/redistributable_bin/win64`; bundle the DLL as a resource | – | – |
| ~~Q7~~ | Closed 2026-09-22 → D-140: the heaviest mod list in the cache is 121 mods, which spells out to about 8 100 characters of `-mod=`, a quarter of the 32 767 limit. Relative paths are not needed | – | – |
| ~~Q8~~ | Closed 2026-09-22 → D-140 as won't-fix: mods are matched by Workshop ID only (project rule), so the per-mod hash and flag bytes are never read. Revisit only if Bohemia documents them (S-45) | – | – |
| ~~Q9~~ | Resolved 2026-09-22 → D-140: **yes, rarely.** Across 3 213 mod entries from 193 live servers the id-length byte was 4 in 3 209 and **1 in four entries**, so non-4 widths do occur and the parser's `width = idLen & 0x0F` (1–8) is required, not just defensive | – | – |
| ~~Q10~~ | Resolved 2026-09-22 → D-137: 450 sampled populated servers, 447 answered INFO and **all 447 answered RULES** (no timeouts, no errors). Servers ignoring RULES are not a real failure mode at our pacing; the official launcher's message matches a host that is offline or filtered | – | – |
| ~~Q12~~ | Resolved 2026-09-21 in M3: `rusqlite` 0.40.2 (bundled SQLite, WAL) behind one mutex, no JS surface; cache, favourites, history, population and mod lists all live in it | – | – |
| ~~Q13~~ | Resolved 2026-09-21 → D-052: the persona comes from Steamworks (`friends().name()`), no VDF parsing | – | – |
| ~~Q14~~ | Resolved 2026-09-21 → D-058: the NSIS installer puts `steam_api64.dll` beside `dayz-launcher.exe`; the installed app loaded it from there; silent install and uninstall leave nothing behind | – | – |
| ~~Q15~~ | Resolved 2026-09-21 → D-045: `noplayers` + `map` for the 10 maps with most empties, then a capped catch-all; the cap now only truncates fake servers | – | – |
| ~~Q16~~ | Resolved 2026-09-21 → D-057: releasing Steamworks after idle unloads the DLL but host private bytes stay at 61 MB; feature kept on by default at 15 minutes, with a control in Settings; `DAYZ_STEAM_IDLE_SECS` overrides it for experiments | – | – |
| ~~Q17~~ | Resolved 2026-09-21 → D-056: removing `will-change: transform` from the virtual window lowered GPU commit 98 → 92 MB with no scrolling regression; `contain: strict` kept | – | – |
| Q18 | Two idle runs showed renderer memory and host CPU growing (up to 196 MB / 35 s in 8 min); later runs were flat and thread attribution pointed outside our modules. Cause unexplained (D-060). The in-app performance panel and the exported report were removed with the Diagnostics page (D-168), so this is measured from outside now. **2026-09-22, v0.1.19: a 12-minute run after a cold start was flat** — host 73 MB throughout, WebView 221–242 MB, total 294–316 MB with one transient 347 MB sample, host CPU 0.02 s per 20 s after start-up (D-136). No growth in five of seven runs now | monitoring | If it recurs, `launcher.log` (D-158) records start-up, every refresh with per-partition timings, verification summaries and cache work, so the report carries its own timeline — and capture `Get-Process` thread CPU and Steam logs at the time. Measure CPU with per-process `TotalProcessorTime` deltas and **take no screenshots during the window** (capturing the window forces redraws and reads 1–2 % instead of 0.4 %) |
| ~~Q19~~ | Resolved 2026-09-21 → D-067: custom NSIS template (stock 2.11.5 template with the currentUser default changed to `$LOCALAPPDATA\Programs\${PRODUCTNAME}`), verified by a silent install with no remembered location | – | – |
| ~~Q20~~ | In some sessions the WebView's localStorage is not the persistent one: instances started at 19:21 and 19:39 (2026-09-21) read defaults although `EBWebView\Default\Local Storage\leveldb` holds the values, its LOG was not rotated (no open) and its LOCK was free; writes in those sessions never reached disk. The installer-launched instance at 19:19 did open it. Mitigated by D-070 (settings file is the source of truth) and D-112 (the window is created after setup, and the read retries). **No recurrence since**: every start on 2026-09-22, including the v0.1.19 cold starts, read the theme, accent and news marker correctly on the first frame | robustness | `launcher.log` (D-158) records each start with its settings read, so a recurrence is visible without instrumentation. Reproduce with a fresh WebView2 profile and `--enable-logging` on the browser process; check whether WebView2 153 moved DOM storage or fell back to memory after a failed open | **Closed 2026-09-23:** mitigated by D-070 (preferences in `settings.json` rather than `localStorage`) and D-112, and nothing in the log since 2026-09-21. Re-open with a log line if it recurs.
| Q11 | Code-signing certificate: **user decided 2026-09-21 to ship unsigned for now**. Windows SmartScreen will show "unknown publisher" on first run of the installer; revisit when a certificate is available | release | – |
| ~~Q21~~ | Resolved 2026-09-22 → D-117: DayZ announces the game server to Steam once the player is on it; the Friends tab showed a friend's server and Join at 07:48. The `0.0.0.0:0` readings were friends idling in the menu | – | – |
| ~~Q23~~ | Resolved 2026-09-22 08:5x: the failures (07:50, 08:4x) came from launcher instances started from a developer shell, which runs in another logon session; a game spawned there cannot reach the Steam client's IPC. Instances opened through the desktop (08:02) join normally. Not a launcher defect; noted in D-127 | – | – |
| ~~Q22~~ | Closed 2026-09-23 → D-205. Not a defect: application data is deleted through the setup on every test reinstall, which is what the checkbox is for. That accounts for the missing favourites and join history in every observation this question was built on, and for Q25 with it. The mitigations it produced along the way stand on their own — the prune guard (D-159), the settings-shape check (D-162), the recovery path that moves the three files together or nothing (D-187), the durable checkpoints and the row counts logged at every start (D-193) | – | – |
| ~~Q24~~ | Resolved 2026-09-25 → D-235: `prune` returns the ids it deleted and the host emits them as `servers:pruned`; the store drops them from `rows` and every per-id cache. The heap measurement at 19 000 rows is still to be taken on the Windows machine | – | – |
| ~~Q25~~ | Closed 2026-09-23 → D-194: not a bug, and not one event but three ordinary ones. **The whole app-data folder was removed at 2026-09-22 00:19:54 UTC** — both roots carry that creation time to the same second and the oldest row in the database is four seconds younger, so it was recreated from nothing. **The three joins were cleared by hand**, which the log says at 12:24:09 UTC, and no game has been launched since. **No favourite was ever written to this file**: the b-tree root of `favourites` holds two non-zero bytes in 4 096, against `history`'s root which still holds all three deleted cells verbatim | – | – |
| Q26 | Does Steam's master-server `hasplayers` filter count sessions created with `ISteamGameServer::CreateUnauthenticatedUserConnection` ("a fake user (ie, a bot) which will be listed as playing on the server, but skips validation")? If it does, rule R0's floor costs an operator one SDK call rather than a licensed account per entry. Every farm measured so far sits in `noplayers` (D-046), so nobody is doing it, but the header leaves it open. Settling it needs a server binary under our control registering such a session and a master-server query for it — not something the launcher can test | open 2026-09-24 (D-233) |
| Q27 | Which of the 2026-09-25 detection proposals hold on live data (D-238)? None of these could be measured where they were written — the container has no UDP and no route to Steam or DZSA. **(a) R12's false-positive rate**, shipped: `node tools/player_shape_probe.js 150` must report `zeroLength2: 0` on honest lists. **(b) More entries than slots** as Synthetic: accept if `SELECT COUNT(*) FROM servers WHERE verified_players > max_players AND max_players > 0` and the same over `population` are both 0 and the probe's `overMax` is 0. *Measured 2026-09-26 (audit row 2):* `servers` 0; `population` 1 sample — 212.22.93.158:2305, max 1 today, counted 2 once on 2026-09-23 — and the table does not keep the max at sample time, so the SQL half cannot pass as written. **(c) A sub-second cluster beside an old session** (≥ 3 entries under 1 s and one ≥ 600 s) as Synthetic, which would also catch a T2 fixed to write exactly 0.0: accept if the probe's `subSecondCluster` is 0 — unless DayZ lists players still loading at 0.0. **(d) Join order**: honest lists descend (docs/11 §3b); accept "any ascent over 1 s in ≥ 5 entries is Synthetic" if the probe's `ascending` is 0 across ≥ 300 lists. **(e) R11 as an invariant** (every session old enough to have existed at the previous check must have been in it), which a prototype caught 97 % of random appenders with, against 9–33 % for R11's one-in-five floor, and which would let comparisons span refreshes: measure the unexplained count on honest pairs first. **(f) Steam's own per-server count** (S-81: an anonymous CM `GetServerList` returns players and bots "as Steam knows it"), the only defence found against a stateful proxy that ages its fake sessions: compare it with verified head-counts right after a Refresh, accept if within ±4 for ≥ 98 % of verified rows. **(g) An age limit on R6's vouch rescue** (sort as 0 once the last head-count is over 30 minutes old): count the rows it would affect first. **(h) Near-miss clones for R10** (NFKC, zero-width and look-alike folding): list the matches the new key adds; accept if ≥ 95 % are already untrusted. **(i) One server id or one game address behind several rows** (the same `steam_id`, or one `ip:game_port` under different query ports), a relay-mirror signal. ~~**(j) R5's `≤ 2 distinct durations` clause**~~ *resolved → D-268: durations are compared in tenths; a live list held three groups that joined within half a second of each other (S-92), and the cache held no synthetic verdict, so nothing was lost* **First probe, 2026-09-25 (D-243; 150 verified servers, 259 lists, two passes 5 min apart):** (a) `zeroLength1` 0, `zeroLength2` 0 — R12 confirmed; (b) `overMax` 0, the SQL half not yet run; (c) `subSecondCluster` 0 — holds on this sample, one more before a verdict hides servers on it; (d) **rejected**: one verified 12-player server (147.135.104.68:2303) listed out of join order in both passes with nothing else fake about it, so order is not a signal; R11's margin is wide — median 95 % of sessions carried over, p5 67 %, against its 20 % floor — but one server carried over none, and the probe now names such servers and whether they restarted | detection | `tools/player_shape_probe.js` on the Windows machine; the SQL above against `%LOCALAPPDATA%\com.dayzlauncher.desktop\cache.db`; for (f), `steam-user` 5.3.0 anonymous `getServerList("\\appid\\221100", 20000)` joined on `addr` |
| Q28 | Do the memory budgets of docs/05 §6 hold in the running app at the row counts of 2026-09-25 (71 397 rows before the D-245 prune, ~40 000 after)? The performance audit measured the pieces in Node — the renderer holds 41 MB of rows and a 36 MB transient payload at 71k — but not the processes; the last in-app measurement (D-179) was taken at 19 000 rows | performance | Measure all-process and host private bytes on the reference machine after a full refresh on v0.1.47, and record a §6 row |
| Q29 | Should a row judged **unverifiable** (R4: INFO answers with players, PLAYER times out) get the patient 2.5 s PLAYER retry that docs/11's R4 row describes? Only rows that come back **offline** get it (the `offline` list in `verify_pass`, found in the D-256 audit). An honest server that drops one PLAYER read is dimmed until the next pass; a retry costs one more query of up to 2.5 s per such row per pass. D-050 counted 6 honest firewalled servers in 2 858; the T3 farms that answer INFO and never PLAYER are many more | R4's accuracy, or docs/11 | Measure on a live pass how many R4 rows turn verified on a 2.5 s retry and what the retry adds to the pass time. Then either retry them, or correct docs/11's R4 row to say there is no retry. A behaviour and load change, so it waits for a decision |
| ~~Q30~~ | Resolved 2026-09-26 → D-265: whatever the answer, such an item now fails a minute after its kick with the remedy instead of waiting out the 15-minute stall. Was: Does calling `DownloadItem` again make Steam fetch a Workshop item that its own books list as INSTALLED but whose folder is gone? The sync kicks every item once at the start, and `tick_sync` re-kicks only "subscribed" and "needs_update". Such an item has shown as "pending" since D-245, so it gets no second kick and waits for the 15-minute stall and its remedy (S-79: unsubscribe and resubscribe, or clear the download cache). The comment in `item_progress` claimed it was re-kicked; D-256 corrected it | sync after a mod folder is deleted by hand | On the Windows machine, with Steam running: subscribe a small item (1819514788, "Ear Plugs", as `tests/live_join.rs` does), delete its content folder, and call `DownloadItem` for it repeatedly. If the folder comes back, add "pending with no folder" to the re-kick. If it does not, the stall's remedy stays the answer |
| ~~Q31~~ | Resolved 2026-09-25 → D-264: the window's big and small icons are now loaded from the exe's icon group at the window's DPI. Was: Is the taskbar icon sharp at 125 % and 150 % scaling? Tauri hands the window one image, the ICO's first entry — 32 px since D-260, 16 before — as its small icon only (S-85), and Windows scales it to the 24, 32, 40 or 48 px the taskbar and Alt-Tab want. Windows would pick the matching size from the exe's own icon group if the big icon were set from it | how the icon looks on the taskbar | Look at the taskbar and Alt-Tab at 100 % and 150 % on the Windows machine after the next release. If soft, set the big icon from the resource at window creation: `LoadImageW(GetModuleHandleW(NULL), MAKEINTRESOURCEW(32512), IMAGE_ICON, 0, 0, LR_DEFAULTSIZE \| LR_SHARED)` and `SendMessageW(hwnd, WM_SETICON, ICON_BIG, hicon)` behind `#[cfg(windows)]`, then look again |
